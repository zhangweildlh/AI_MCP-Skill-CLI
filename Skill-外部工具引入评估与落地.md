---
name: external-tool-onboarding
description: 外部工具与外部资产的引入评估与接入落地通用工作流。当需要把一个第三方开源项目、命令行工具(CLI)、模型上下文协议服务器(MCP
  Server)、或外部提示词资产(SKILL/prompt/subagent)引入到本地智能体环境时触发。典型触发语：「要不要装这个工具」「把这个仓库接进来」「评估一下这个
  MCP」「给这个工具写个技能」「这个和我现在的工具重复吗」「接入路径怎么选」。覆盖：能力盘点、与既有工具的重叠裁决、接入形态选型、上游原生资产优先复用、安全审计、配置写入、验证与回滚、上游同步策略。不适用于：已接入工具的日常调用、纯业务代码修改、版本控制日常操作。
license: Apache-2.0
metadata:
  author: workflow-distill
  version: 1.0.0
  agent_created: true
  maturity: verified
  pattern-key: tooling.external-tool-onboarding
  compatibility: 需具备通用文件读写工具、Shell 执行能力，以及可选的代码托管平台命令行工具（用于拉取上游仓库资产）。所有路径一律运行时解析，不得写死。
---

# External Tool Onboarding — 外部工具引入评估与接入

## 一、定位与边界

把「要不要引入某个外部工具 / 外部资产」以及「引入之后怎么落地」这件反复发生的事，固化为一条**先评估、后接入、再验证**的确定性流水线。

**核心主张：默认不装。** 引入任何外部件都会带来体积、攻击面、维护成本与能力冗余。本工作流的第一产物不是配置文件，而是**一份「装 / 不装」的裁决结论**。

**适用对象**（统称「外部件」）：
- 第三方开源项目 / 命令行工具（CLI）
- 模型上下文协议服务器（MCP Server）
- 外部提示词资产：技能文件（SKILL）、子代理（subagent）、命令模板（command）

**不适用**：已接入工具的日常调用、业务代码修改、版本控制日常操作。

**零硬编码约定**：本文件所有路径均为占位符，运行时解析。
- `<SKILLS_DIR>` — 用户级技能目录
- `<CLIENT_CONFIG>` — 智能体客户端的外部服务配置文件
- `<TOOL_ROOT>` — 本机第三方工具安装根目录
- `<PROJECT_ROOT>` — 当前项目根目录

---

## 二、总体流程（七阶段，按实际作业顺序）

```
阶段0 需求定义  →  阶段1 上游侦察  →  阶段2 重叠裁决  →  阶段3 形态选型
      ↓                                                        ↓
阶段6 上游同步  ←  阶段5 验证与回滚  ←  阶段4 安全审计与落地
```

**门禁原则**：阶段 2 未出结论不得进入阶段 3；阶段 4 安全审计未通过不得写任何配置；阶段 5 未验证通过不得宣告完成。

---

## 三、阶段 0 — 需求定义（先问「解决什么问题」）

引入动作开始前，必须用一句话写清：

1. **待解决的具体问题**是什么（不是「这个工具看起来不错」）。
2. **现在是怎么绕过去的**（当前的替代做法及其痛点）。
3. **成功判据**是什么（引入后哪个可观测指标改善）。

三问答不出来 ⇒ **直接结束，判定「不引入」**。这是本工作流最高频的正确结论。

---

## 四、阶段 1 — 上游侦察（原生资产优先，避免重复造轮子）

**铁律：写自研适配层之前，先去上游仓库找官方已有的资产。**

实践反复证明：许多工具的上游仓库自带官方技能文件、提示词模板或集成说明，直接适配增强比从零编写更快、更准，且能持续跟随上游演进。

### 侦察清单（逐项核查，不可跳过）

| 核查项 | 目的 | 常见位置 |
|---|---|---|
| 官方技能 / 提示词资产 | 能否直接复用而非自研 | `skills/`、`prompts/`、`.agent/`、`docs/integration` |
| 附属资源文件 | 避免只搬主文件、漏装脚本与引用 | 技能目录下的 `scripts/`、`references/`、`assets/` |
| 真实协议形态 | 判定它是 CLI 还是服务端 | `README`、入口文件、`package`/`manifest` 声明 |
| 能力清单与参数 | 支撑后续重叠分析 | `--help` 实测输出优先于文档 |
| 活跃度与体积 | 维护成本预估 | 提交频率、发布节奏、安装体积 |

### 实测优先于文档

文档常滞后。**以本机实际执行 `--help` / 子命令探测的输出为准**，与文档冲突时以实测为准并记录差异。

**发现官方资产时**：基于官方版本做适配增强（本地化、补充本机约定），而非另起炉灶；同时记录官方版本号或提交号，供阶段 6 同步比对。

---

## 五、阶段 2 — 重叠裁决（最容易被跳过、也最省钱的一步）

把外部件的能力清单，与**智能体已内置的原生工具**及**已接入的既有工具**逐项比对，产出一张三列对照表：

| 外部件能力 | 现有等价能力 | 裁决 |
|---|---|---|
| 能力 A | 原生工具已完全覆盖 | **冗余** — 不引入 |
| 能力 B | 现有方案能做但笨重 | **增量有限** — 计入成本再定 |
| 能力 C | 现有方案完全做不到 | **真增量** — 构成引入理由 |

**裁决规则**：
- 「真增量」为 0 ⇒ **不引入**，流程结束。
- 「真增量」仅 1~2 项且可用现有组合绕过 ⇒ 倾向不引入，明确写出取舍理由。
- 存在不可替代的「真增量」⇒ 进入阶段 3。

> 高频教训：不少外部件的能力与智能体原生的文件读写、检索、Shell 执行**大面积重合**，引入后长期闲置，只增加了启动开销与攻击面。

---

## 六、阶段 3 — 接入形态选型（取最小可行形态）

按「改动最小、耦合最低」原则，从上往下选第一个能满足需求的形态：

| 序 | 形态 | 适用条件 | 代价 |
|---|---|---|---|
| 1 | **纯提示词接入**（技能 / 命令模板） | 外部件是提示词资产，或本机已有可驱动它的通用工具 | 最低，零运行时 |
| 2 | **命令行委托**（Shell 直接调用 CLI） | 外部件是 CLI，且智能体具备 Shell 能力 | 低，无需常驻进程 |
| 3 | **客户端原生接入**（写入 `<CLIENT_CONFIG>`） | 外部件本身就是标准协议服务端 | 中，占用启动资源 |
| 4 | **经代理聚合器接入** | 需要与多个服务统一管理、按需加载 | 中，多一层链路 |
| 5 | **自研薄封装层** | 协议不匹配且前四种均不可行 | **最高，需长期维护** |

### 关键判据：协议是否匹配

**代理聚合器只做转发，不做协议转换。** 若外部件是 CLI（标准输入输出的一次性命令）而目标链路要求的是常驻协议服务端，二者存在**协议断层**——此时代理无法直接承接，要么改走形态 2（命令行委托），要么必须自研薄封装（形态 5）。

**决策捷径**：若形态 2 已能满足全部需求，则形态 3/4/5 一律不做。多路径并存会带来配置漂移与拆除成本。

### 多路径并存的处置

若前期已试验多条路径，最终**只保留一条**，其余必须彻底拆除：
1. 删除封装层目录；
2. 从配置文件中移除对应条目；
3. 重连 / 重启客户端使配置生效；
4. 复查确认残留项已清空。

---

## 七、阶段 4 — 安全审计与落地（审计不过不写盘）

### 4.1 安装前安全审计（强制前置）

任何写盘、安装、执行动作**之前**逐项核查：

- [ ] **凭据泄漏**：是否要求明文密钥？密钥文件是否会被版本控制跟踪（检查忽略规则）？
- [ ] **权限范围**：是否申请超出需求的文件系统 / 网络 / 系统权限？
- [ ] **执行来源**：安装脚本是否从不可信源下载并执行代码？
- [ ] **数据外发**：是否存在遥测、上报或云端回传？可否关闭？
- [ ] **依赖链**：依赖数量与来源是否可控？

**任一项存疑 ⇒ 暂停并向用户说明风险，等待明确授权后再继续。**

> 高频真实风险：外部技能 / 项目目录中携带明文密钥文件，且该文件被版本控制跟踪——一旦推送即造成公开泄漏。接入前务必核查忽略规则。

### 4.2 配置写入原则

- **先读后写**：改配置文件前必须先完整读取现有内容。
- **合并不覆盖**：新条目**并入**现有结构，严禁整体覆盖导致其他条目丢失。
- **格式保真**：沿用文件既有的缩进、字段顺序与命名风格。
- **单一事源**：同一外部件只在一处定义，不得在多个配置文件中重复登记。
- **优先级明示**：若该外部件同时支持命令行参数、环境变量、本地配置三种来源，明确记录生效优先级，避免后续排障困惑。

### 4.3 提示词资产落地原则

若产物是技能 / 命令模板，写入 `<SKILLS_DIR>` 时：
- 目录名与技能名一致，小写连字符；
- **附属资源必须同步安装**（脚本、引用文档、示例）——只搬主文件是高频漏装点；
- 描述字段脱离原始上下文仍能自洽表达触发条件；
- 不得写入本机绝对路径、账户名、仓库名等特定值。

---

## 八、阶段 5 — 验证与回滚（未验证不算完成）

### 5.1 验证三连

1. **存在性**：外部件可被发现（命令可解析 / 服务可枚举 / 技能可列出）。
2. **可用性**：跑一条**最小真实用例**（非 `--version`），确认返回预期结果。
3. **无回归**：确认既有工具与配置未受影响（原有条目仍在、原有能力仍可用）。

### 5.2 回滚预案（接入前先写好）

落地之前就要写清回滚三步：删除什么目录、移除哪个配置条目、如何使客户端重新加载。**没有回滚预案不得落地。**

---

## 九、阶段 6 — 上游同步策略（一次性决策，长期受益）

复用上游资产时，**接入当次**就要定下同步方式：

| 策略 | 优点 | 缺点 | 适用 |
|---|---|---|---|
| **快照复制** | 简单、可自由本地化改造 | 上游更新需手工比对 | 需要深度本地化改造 |
| **子模块 / 子树引用** | 上游更新可拉取 | 本地改动易与上游冲突 | 基本原样使用 |
| **仅记录来源** | 零维护 | 完全不跟随上游 | 一次性、上游已停更 |

无论选哪种，都必须**记录上游来源与版本号 / 提交号**，否则日后无法判断本地版本落后多少。

---

## 十、完整示例（占位符形式，可直接套用）

```text
【阶段0 需求定义】
  问题：需要对代码改动做规则化静态扫描
  现状：靠人工通读 diff，覆盖不全
  判据：能自动列出违反规则的文件与行号

【阶段1 上游侦察】
  gh repo view <OWNER>/<REPO>                 # 活跃度、体积
  gh api repos/<OWNER>/<REPO>/contents/skills # 是否有官方技能资产
  <TOOL> --help                               # 实测能力，优先于文档
  → 发现官方技能资产，含 scripts/ 附属目录 ⇒ 适配增强，不自研

【阶段2 重叠裁决】
  规则化整库扫描  → 原生工具做不到 → 真增量 ✓
  文件内容检索    → 原生检索已覆盖 → 冗余
  → 存在真增量，继续

【阶段3 形态选型】
  外部件是 CLI，本机具备 Shell ⇒ 选形态2「命令行委托」
  ⇒ 形态3/4/5 一律不做

【阶段4 安全审计与落地】
  凭据：委托模式无需密钥 ✓   权限：仅读取项目目录 ✓
  安装脚本来源：官方发布渠道 ✓
  → 审计通过，写入 <SKILLS_DIR>/<name>/SKILL.md（含 scripts/ 附属资源）

【阶段5 验证】
  存在性：技能可列出 ✓
  可用性：对一个真实改动跑一次扫描，返回结构化结果 ✓
  无回归：既有技能与配置未变 ✓
  回滚：删除 <SKILLS_DIR>/<name>/ 即可

【阶段6 上游同步】
  策略：快照复制（需中文本地化）
  记录：<OWNER>/<REPO> @ <COMMIT_SHA>
```

---

## 十一、常见错误与对策

| 错误 | 后果 | 对策 |
|---|---|---|
| 跳过重叠裁决直接安装 | 引入与原生能力大面积重复的冗余件，长期闲置 | 强制产出三列对照表 |
| 不查上游就自研适配层 | 重复造轮子，且无法跟随上游演进 | 阶段 1 侦察清单逐项核查 |
| 只搬主文件，漏装附属资源 | 运行时报缺文件，功能残缺 | 落地后复查 `scripts/`、`references/` |
| 误以为代理聚合器能做协议转换 | 反复调试不通，浪费大量时间 | 先判定协议形态是否匹配 |
| 整体覆盖写配置文件 | 其他已接入条目全部丢失 | 先读后写、合并不覆盖 |
| 多条接入路径并存 | 配置漂移，排障困难 | 只保留一条，其余彻底拆除 |
| 以 `--version` 冒充功能验证 | 实际不可用却已宣告完成 | 必须跑最小真实用例 |
| 忽略明文密钥与忽略规则 | 密钥随版本控制公开泄漏 | 阶段 4.1 强制审计 |
| 未记录上游版本号 | 无法判断本地落后程度 | 阶段 6 强制记录 |

---

## 十二、统计与成熟度

- **Pattern-Key**：`tooling.external-tool-onboarding`
- **成熟度**：`verified`
- **Recurrence-Count**：5
- **相异任务数**：5（分属命令行工具接入、协议服务端接入、外部技能资产复用、聚合器路径选型、上游资产架构解耦五类相异场景）
- **证据窗口**：30 天内
- **蒸馏来源**：由 `workflow-distill` 从跨会话历史中按 Pattern-Key 去重计数后晋升生成

> 本技能为通用方法论，**零项目特定内容、零硬编码路径**，可直接移植到任意智能体的用户级技能目录使用。
