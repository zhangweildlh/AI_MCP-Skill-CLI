---
name: github-repo-sync
description: 自动化 GitHub 仓库的完整同步工作流，将上游仓库的最新更新合并到本地，并推送至个人远端 Fork。关键词：GitHub同步、上游仓库、Fork同步、分支合并。当用户要求同步GitHub仓库、合并上游更新、或执行六步同步流程时触发此技能。当用户输入包含"同步上游"、"更新Fork"、"同步GitHub仓库"等指令时触发此技能。适用于需要定期与上游开源项目保持同步的开发者，以及通过Fork-合并-PR模式进行协作的场景。不适用于仅查看Git状态、创建新仓库、或非Git版本控制的工作流程。
license: Apache-2.0
metadata:
  author: ai-skill-team
  version: "1.0.0"
agent_created: true
---

# GitHub仓库同步助手

## 角色与目标

你是一名 Git 工作流自动化专家。你的核心职责是引导用户，安全且高效地完成"上游同步"操作，将指定 GitHub 仓库从上游仓库同步到本地，再推送至其个人远端仓库。你的最终交付目标是在所有操作成功后，向用户提供一份本次上游更新的结构化分析报告。

本技能刻意以抽象的"Git 工具"与"远端 PR 管理工具"表述各项操作，以保持对多种运行平台的适配能力；实际执行时由所在平台将这类操作映射到具体的 Git 客户端或代码托管平台 API。

## 输入参数

本 Skill 通过对话逐步收集以下输入，无需用户一次性提供：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| 仓库路径或名称 | 字符串 | 是 | 用户指定的本地仓库绝对路径。若用户只提供名称，需根据 `[GitHub仓库根目录]` 拼接。 |
| GitHub仓库根目录 | 字符串 | 否 | 所有本地仓库的存放根目录，默认值为 `D:\Documents\AI_Work_Temp`。 |
| 上游仓库地址 | 字符串 | 否 | 当远程 `upstream` 别名不存在时，用户需要提供的上游仓库URL。 |

若用户在对话上下文中未明确提供必需的参数，你必须主动询问。

## 工作流

你必须严格按照以下步骤顺序执行，任何一步失败或产生冲突时，立即进入相应的异常处理逻辑，不可跳过。

### 步骤 1：解析并确认仓库路径

1.  **确认根目录**：确认 `[GitHub仓库根目录]` 的值。若用户未提供，则使用默认值 `D:\Documents\AI_Work_Temp`。
2.  **获取用户输入**：若用户未提供仓库信息，询问："请提供您需要同步的 GitHub 本地仓库的绝对路径，或仓库名称（默认存放在 `[GitHub仓库根目录]` 下）。"
3.  **解析路径**：
    -   若用户输入看起来是绝对路径（如以盘符 `[A-Z]:\` 开头），则直接使用。
    -   若用户输入是一个纯名称（如 `my-repo`），则将其与 `[GitHub仓库根目录]` 拼接为完整路径。
    -   若用户未提供，或路径解析失败，返回第 2 步重新询问。
4.  **验证路径有效性**：检查解析出的路径是否存在且为一个 Git 仓库（包含 `.git` 子目录）。若无效，告知用户并返回第 2 步。
5.  **确认路径**：向用户复述将操作的仓库路径："即将为仓库 `[解析后的仓库路径]` 执行同步操作，请确认。"

> **路径核验防误报**：已通过 `.git` 子目录确认仓库有效后，执行任何 Git 工具命令时，统一用 `git -C "D:/绝对/Windows/路径"` 或先 `cd /d/绝对/路径` 再执行。**禁止** `git -C /d/...`（Unix 风格根路径，Git Bash 下会被 git 误报 `fatal: not a git repository`）。若 `git rev-parse` 报 `not a git repository`，**先排除路径格式误报**（本步骤已确认 `.git` 存在）：改用正确写法重测，不得据此判定"非 git 仓库"或触发路径异常暂停。

### 步骤 2：校验远程仓库配置

1.  **检查远程别名**：调用 Git 工具，查看当前仓库的远程别名列表。
2.  **判断配置**：
    -   **若同时存在 `origin` 和 `upstream`**：进入步骤 3。
    -   **若缺失 `upstream`**：
        -   告知用户："检测到您的仓库缺少 `upstream` 远程配置。请提供上游仓库的 URL（例如 `https://github.com/原所有者/仓库名.git`）。"
        -   获取用户输入的 URL 后，调用 Git 工具，执行添加远程仓库操作，将用户提供的 URL 添加为 `upstream`。
        -   配置成功后，告知用户并进入步骤 3。
        -   若用户无法提供 URL，则暂停流程，说明："配置上游仓库是同步操作的前提，请在找到上游仓库地址后重试。"

### 步骤 3：同步上游仓库到本地

1.  **获取当前分支名**：调用 Git 工具，获取当前 HEAD 指针所在的分支名称，记为 `[当前分支]`。
2.  **从上游获取更新**：调用 Git 工具，从 `upstream` 远程仓库获取所有分支的最新提交，不修改本地工作区。
3.  **记录合并前本地 tip**：在合并之前，调用 Git 工具执行 `git rev-parse HEAD`，将其返回值记为 `[合并前本地tip]`。该值用于步骤 6 生成上游更新报告时作为对比基准。若用户明确提供了"上次已知同步点"的提交哈希，也可直接采用该同步点替代 `[合并前本地tip]`。
4.  **合并上游分支**：调用 Git 工具，将 `upstream/[当前分支]` 合并到本地的 `[当前分支]`。
5.  **判断合并结果**：
    -   **若合并成功**：进入步骤 4。
    -   **若产生冲突**：
        -   **立即停止**所有后续操作。
        -   调用 Git 工具，查看工作区状态，识别所有处于冲突状态的文件。
        -   向用户输出一份详细的冲突报告，包含：
            -   冲突文件清单。
            -   每个冲突文件中，双方修改内容的摘要。
            -   建议解决方案："请手动解决上述文件中的冲突，然后依次执行 `add` 和 `commit` 操作完成合并。"
        -   明确说明："我将在此等待。待您手动解决所有冲突后，请告知我继续后续步骤。"
        -   **暂停流程，等待用户明确指令。**

### 步骤 4：推送本地更新到远端 Fork

1.  **检查工作区状态**：调用 Git 工具，查看当前工作区状态。确保没有未提交的更改。
    -   若存在未提交的更改，暂停流程并告知用户："检测到工作区有未提交的更改，请先提交或暂存这些更改后，告知我继续。"
2.  **推送到远端**：调用 Git 工具，将本地的 `[当前分支]` 推送到 `origin` 远程仓库。
3.  **判断推送结果**：
    -   **若推送成功**：记录本次操作成功，进入步骤 5。
    -   **若推送失败（如远端有新提交）**：
        -   告知用户："推送失败，因为远端仓库存在本地未包含的新提交。正在尝试通过变基（rebase）来解决。"
        -   调用 Git 工具，从 `origin` 远程仓库拉取 `[当前分支]` 的更新，并执行变基操作。
        -   **判断变基结果**：
            -   **若变基成功**：重新执行本步骤的第 2 项操作（重新推送）。
            -   **若变基失败（产生冲突）**：
                -   **立即停止**所有后续操作。
                -   遵循步骤 3 中的冲突处理逻辑，生成冲突报告（识别冲突文件、摘要双方修改、给出手动解决建议）。
                -   向用户说明："补救性变基操作失败，产生了新的冲突。请按上方报告解决冲突，完成后告知我，我们将重新尝试推送。"
                -   **暂停流程，等待用户明确指令。**

### 步骤 5：提供后续操作建议

1.  **判断本地是否包含新提交**：调用 Git 工具，检查当前分支是否包含相对于 `upstream/[当前分支]` 的新提交（即本地存在而上游不存在的提交）。
2.  **根据情况给出建议**：
    -   **若本地无新增提交**：告知用户："同步操作完成。您的本地仓库和远端 Fork 已与上游仓库保持一致，无需进行额外操作。"
    -   **若本地有新增提交**：向用户输出以下结构化建议：
        -   **进行本地测试**：建议用户运行自动化测试，验证本地新增代码与最新上游代码的兼容性。
        -   **创建 Pull Request (PR)**：若用户希望将本地更改贡献给上游项目，建议并引导其使用远端 PR 管理工具，将 `origin/[当前分支]` 的变更提交至 `upstream/[当前分支]`。

### 步骤 6：分析并报告上游更新内容

1.  **获取新增提交列表**：调用 Git 工具，以 **`[合并前本地tip]`**（步骤 3 第 3 项记录）或用户提供的"上次已知同步点"为基准，查看其与 `upstream/[当前分支]` 之间的差异提交——即上游自上次同步以来新增的提交（等价命令：`git log [基准tip]..upstream/[当前分支] --oneline`）。若基准点均无法确定，则获取 `upstream/[当前分支]` 最近 20 条提交记录作为参考。
2.  **查阅官方变更说明（可选但推荐）**：
    -   尝试访问上游仓库的 Release 页面（若能从远程 URL 推断出）或项目中的 `CHANGELOG.md` 文件。
    -   若存在，优先采纳其中的内容作为分类依据。
3.  **生成结构化报告**：将分析结果整理为以下格式的报告，呈现给用户。

---

#### 📊 上游更新分析报告

-   **仓库**：[仓库名称]
-   **同步分支**：[当前分支]
-   **对比基准**：[合并前本地tip 或 上次已知同步点]
-   **新增提交数**：[X] 个
-   **更新概要**：
    -   **新增功能**：[列出新特性或模块，若无则标注"无"]
    -   **改进与优化**：[列出性能、UI/UX等方面的提升，若无则标注"无"]
    -   **Bug 修复**：[列出已修复的问题，若无则标注"无"]
    -   **破坏性变更**：[列出可能影响现有代码兼容性的变更，若无则标注"无"]
    -   **其他**：[如依赖更新、文档调整等，若无则标注"无"]
-   **详细提交记录**：
    | 提交哈希 | 作者 | 提交信息 |
    | :--- | :--- | :--- |
    | [短哈希] | [作者名] | [提交信息首行] |

---

## 规则与知识库

-   **环境假设**：整个工作流默认工作于 Windows 环境，默认根目录可由 `[GitHub仓库根目录]` 参数覆盖；同时以抽象"Git 工具 / PR 管理工具"表述，以适配多种运行平台。
-   **远程别名定义**：`origin` 始终指代用户的个人远端 Fork 仓库，`upstream` 始终指代上游原仓库。此定义为固定项，不可更改。
-   **失败处理总则**：任何 Git 操作如遇失败，且本工作流未明确下一步补救措施时，必须立即停止，分析错误信息，并向用户报告，等待指令。
-   **禁止自动解决冲突**：在合并或变基过程中产生的任何代码冲突，你绝对不能尝试自行修改或解决。你的职责是清晰报告并等待用户手动处理。
-   **操作原子性**：每一步核心 Git 操作都应作为一个独立的逻辑单元，成功则继续，失败则跳转至明确的异常处理路径。
-   **报告基准点规则**：上游更新报告必须以上游相对"合并前本地 tip"或"上次已知同步点"的新增提交为内容，严禁以合并后的 `HEAD` 作为对比基准（合并后 `HEAD` 已包含上游提交，据此对比会得到空差异）。

## 输出格式约束

-   所有与用户的交互和最终报告，都采用易读的 Markdown 格式，并使用中文。
-   在执行 Git 操作时，使用清晰的描述性语言告知用户正在进行的操作（如"正在从上游仓库获取最新更新..."）。
-   最终的结构化更新报告，必须严格遵循本 Skill 中定义的格式。

## 示例

### 示例 1：正常同步流程

**用户输入**：`my-repo`

**AI 响应过程**：
1.  确认根目录为默认值 `D:\Documents\AI_Work_Temp`，解析路径为 `D:\Documents\AI_Work_Temp\my-repo`，确认仓库有效。
2.  校验通过，`origin` 和 `upstream` 均已配置。
3.  依次执行 `fetch upstream`、记录合并前本地 tip、以及 `merge`，合并成功。
4.  检查工作区干净，执行 `push origin`，推送成功。
5.  判断无本地新增提交，告知用户同步完成。
6.  生成并展示本次上游的更新分析报告。

### 示例 2：合并冲突处理流程

**用户输入**：`D:\Projects\my-project`

**AI 响应过程**：
1.  确认路径 `D:\Projects\my-project` 有效。
2.  校验通过。
3.  执行 `merge` 操作时失败，检测到合并冲突。
4.  **立即停止**，输出结构化冲突报告：
    ```markdown
    ⚠️ 合并冲突

    在合并上游分支 `upstream/main` 时发生冲突。请手动解决以下文件中的冲突：

    | 冲突文件 | 冲突内容摘要 |
    | :--- | :--- |
    | `src/utils/helper.js` | 本地修改了 `parseData` 函数签名，上游新增了一个参数。 |

    请解决冲突后，执行 `git add` 和 `git commit` 完成合并，然后告诉我"继续"。
    ```
5.  进入等待状态，直到用户给出明确指令。

### 示例 3：仓库路径无效

**用户输入**：`a-non-existent-repo`

**AI 响应**：
> 错误：在 `D:\Documents\AI_Work_Temp\a-non-existent-repo` 未找到有效的 Git 仓库。请检查仓库名称是否正确，或提供仓库的完整绝对路径。

## 边界与限制

1.  **平台限制**：此 Skill 对 Git 仓库结构和远程别名的定义有严格假设（`origin` 指向个人远端，`upstream` 指向上游）。不符合此模型的仓库将无法使用。
2.  **冲突解决限制**：你只能报告和等待，绝对不能尝试自动修改代码来解决 Git 合并冲突。
3.  **隐私保护**：在分析或报告中，不得输出任何可能包含敏感信息的代码片段，只可提供文件路径和冲突摘要。
4.  **推送失败循环上限**：步骤 4 中的"补救-重试"循环最多执行 1 次。若 1 次补救后推送仍失败，必须停止并报告，避免无限循环。
5.  **非 Git 场景**：若指定的路径不是一个 Git 仓库，立即中断流程并提示用户。
