---
title: "WorkBuddy 记忆文件维护说明（权威手册）"
topic: "memory-maintenance-manual"
tags: [memory, manual, maintenance, authority]
related:
  - "MEMORY.md"
  - "Memory-全局禁令与环境约束.md"
  - "Memory-代码纪律与Git操作.md"
  - "Memory-GitHub全流程操作.md"
  - "Memory-避坑铁律.md"
  - "Memory-入口能力与图谱.md"
scope: "记忆体系维护（本手册自身 file_number: 0，内部章节编号独立，不参与 N-M 编号体系）"
created: "2026-08-28"
updated: "2026-09-10"
parent: null
file_number: 0
---

# WorkBuddy 记忆文件维护说明（权威手册）

> **本文件是 WorkBuddy 记忆系统维护的唯一权威说明。**
> 任何 Agent 只需阅读本文件，即可自主闭环地完成：
> **关联审计 → 审查 → 维护 → 纠偏（修复断链/错链、迁移错放内容、去重、重编号与索引同步）**。
> 本文件与 `memory-mgr.py` 等工具协同，但本文件优先级高于工具输出——当工具输出与本文件"已知缺陷与误报清单（§6）"冲突时，以本文件为准。

---

## 0. 必读纪律（开工前必看）

| # | 纪律 | 说明 |
|---|------|------|
| 0.1 | **路径核验优先** | 任何读/写/改之前，先用 `ls` 或 `Read` 核验绝对路径真实存在，禁止凭记忆拼路径。 |
| 0.2 | **严禁盲修** | `check` 命令存在**已知误报（§6）**，**绝对禁止**直接执行 `check --fix` 让程序自动删除"断链"。误报会把真实存在的文件当断链删掉。 |
| 0.3 | **备份优先** | `memory-mgr.py` 改文件前自动生成 `.bak`；人工用 `Edit`/`Write` 改前也先备份原文件。 |
| 0.4 | **主↔子回链是设计中枢，不是 BUG** | 主文件索引指向子文件、子文件以 `[→主文件]` 回链，这是**合法的双向中枢模式**，`check` 报的"循环引用"绝大多数是误报（§6）。 |
| 0.5 | **单一事源** | 同一事实只在一处权威定义；他处用单向链接引用，禁止重复定义、禁止循环引用（A→B 且 B→A）。 |
| 0.6 | **先读全文件再交叉分析** | 纠偏前必须读完相关文件，确认"权威位置"与"错放位置"，再动手。 |
| 0.7 | **meta scope 例外** | `Memory-Data` 目录与 `<<主记忆文件绝对路径>>` 属元数据范畴，**排除在 AGENTS.md worktree 纪律之外**，可在主工作树直接修改，无需创建 worktree、无需 git 提交即可本地维护。 |
| 0.8 | **子记忆文件范围** | `Memory-Data` 目录中**仅有**以 `Memory-` 作为文件名开头的 `.md` 文件是子记忆文件；其他文件与本记忆系统无关。 |
| 0.9 | **记忆文件格式强制要求** | 修改 / 维护主记忆文件与任一子记忆文件时，**必须遵循 §10 的 14 条格式强制要求**（层级递进与语法、序号连续唯一、常用前置、单一事源、无重复矛盾、无歧义禁用词、引用单向且后序引用前序、引用精准唯一等）。违反即判不合规，按 §7 纠偏。 |
| 0.10 | **归属判定前置** | 新信息写入前必须先按 §3.8 判定归属：**命中 1 个子文件 → 并入**（再定位章节）；**0 命中 → 按三门槛评估新建**（须先定义四要素）；**多归属或无法唯一判定 → 暂停并询问用户**，不得自行裁决。判据来源为各子文件 YAML 的 `scope_in` / `scope_out`（规范见 §2.2；运行时视图 = 主文件写前路由卡）。 |
| 0.11 | **四要素只维护一处** | 各子文件的定位 / 角色 / 主题 / 收录范围**只在其自身 YAML 的 5 个字段中维护**（§2.2）。主文件写前路由卡、主索引区与本手册均**不承载明细**——改四要素**只改子文件 YAML**，再跑 `index` 自动重生成；**禁止**手工编辑主文件的两个工具写入区（`ROUTE_CARD` / `MAIN_INDEX`，会被整区覆盖）。 |
| 0.12 | **编号只由工具生成** | 章节编号（`## N-M` / `### N-M-X` / `#### N-M-X-Z`）**一律由 `memory-mgr.py rewrite` 生成**，禁止手工改号、手工调序。编号规则与 N 分配见 **§2.3**；`file_number` 由 `add` 自动分配（取当前最大 N + 1）。手工改号会导致编号与 YAML `file_number` 不一致、跨文件锚点失同步。 |
| 0.13 | **编号完整性必须自检** | 任何编号变更后，**必须**跑 `check`（维度11 编号完整性）+ `断链检测.py`（锚点唯一性），两者全绿方可交付。完整保障制度（10 项判据 + 4 条纪律）见 **§3.9**。 |
| 0.14 | **占位符代入优先（移植纪律）** | 本手册路径一律用占位符 `<<主记忆文件绝对路径>>` / `<<子记忆文件目录绝对路径>>` 书写；跨机器 / 换目录后，须先按 §7.8 代入真实 P、D，再执行任何 `check` / `rewrite` / `add`；禁止直接沿用含旧路径的手册命令。 |

---

## 1. 系统架构全景

```
<<主记忆文件绝对路径>>\
  MEMORY.md                          ← 主文件（YAML FM + 高频纪律速览 + 主索引表 MAIN_INDEX_START/END）

<<子记忆文件目录绝对路径>>\
  Memory-全局禁令与环境约束.md        ← 子记忆文件（file_number: 1）
  Memory-代码纪律与Git操作.md        ← 子记忆文件（file_number: 2）
  Memory-GitHub全流程操作.md         ← 子记忆文件（file_number: 3，最大文件）
  Memory-避坑铁律.md                 ← 子记忆文件（file_number: 4）
  Memory-入口能力与图谱.md           ← 子记忆文件（file_number: 5）
  ── 以上 5 行为当前快照；实际集合以目录内 Memory-*.md 动态发现为准，数量随时增减 ──
  ── 每个子记忆文件 = YAML FM（含 file_number）+ 头部速查索引表 + 编号正文（N-M-X-Z）──
  WorkBuddy记忆文件说明.md           ← 本维护手册（file_number: 0；内部章节编号独立，不参与 N-M 体系）
  memory-mgr.py                      ← 主维护工具（零依赖标准库）
  断链检测.py                        ← 只读分析工具（锚点级）
  memory-state.json                  ← 状态文件（自动生成）
  memory-changelog.json              ← 变更日志（自动生成，自包含不依赖 git）
```

> **子记忆文件权威定义（必须牢记）**：子记忆文件 = 目录 `<<子记忆文件目录绝对路径>>\` 内、**文件名以 `Memory-` 开头**的全部 `.md` 文件。其**数量动态增减**（体系扩展时新增、整理时删除是常态）；主文件 `MEMORY.md` 与各子文件**随时可能被修改**。上图子文件为当前快照，实际集合以目录内 `Memory-*.md` 动态为准；`memory-mgr.py` 用 `Memory-` 前缀动态发现，无需手列清单。本手册**不依赖任何外部方案/规格文档**，仅凭本文件即可闭环运维。

**数据流（中枢模式）**：
- 主文件是入口：**写前路由卡**（由子文件 YAML 四要素生成，见 §2.2）回答"新信息该进哪个子文件"；**主索引表**按"适用场景"路由到各子文件的精确章节。
- 子文件是权威内容载体：每个子文件 = YAML Front Matter + 头部速查索引表 + 正文（连续编号章节）。
- 双向链接合法：主→子（索引/引用）、子→主（`[→主文件]` 回链）、子→子（单向引用）。**只有"子↔子互相引用形成环"才是真违规**。

---

## 2. 文件地图与四要素规范（归属判定的权威依据）

### 2.1 路径地图（绝对路径，权威）

| 文件 | 绝对路径 | 备注 |
|------|---------|------|
| 主文件 | `<<主记忆文件绝对路径>>` | 入口 + 高频纪律 + 写前路由卡 + 主索引；两个工具写入区 `<!-- ROUTE_CARD_START/END -->`（四要素路由卡）与 `<!-- MAIN_INDEX_START/END -->`（主索引）均由工具重建，**禁止手工编辑** |
| 子记忆文件集 | `<<子记忆文件目录绝对路径>>\Memory-*.md`（**目录内动态发现**） | 数量动态增减；每个含 YAML（5 四要素 + `file_number`，规范见 §2.2 / §2.3）。**本表不列具体清单**——运行时视图见主文件写前路由卡，权威数据源为各文件 YAML |
| 本手册 | `<<子记忆文件目录绝对路径>>\WorkBuddy记忆文件说明.md` | `file_number: 0`；内部章节编号独立（§0~§11），**不参与** N-M 编号体系 |
| 主维护工具 | `<<子记忆文件目录绝对路径>>\memory-mgr.py` | 持续维护 CLI，零外部依赖（仅标准库） |
| 链接分析 | `<<子记忆文件目录绝对路径>>\断链检测.py` | — | 只读；链接级（含 P-1 越界引用扫描） |
| 状态文件 | `<<子记忆文件目录绝对路径>>\memory-state.json` | — | 章节-行号映射，自动生成 |
| 变更日志 | `<<子记忆文件目录绝对路径>>\memory-changelog.json` | — | 变更追踪，自包含 |


> **路径占位符说明**：本表路径以占位符 `<<主记忆文件绝对路径>>`（主文件）与 `<<子记忆文件目录绝对路径>>`（子记忆文件目录，内含子文件 / 本手册 / 维护工具 / 状态文件）书写；实际部署时按 §7.8 代入真实路径。

### 2.2 子记忆文件四要素（字段规范 · 归属判定的唯一权威定义源）

> **用途**：新信息入库前，按各子文件的「收录（in-scope）」锁定目标子文件——**命中 1 个** → 并入该文件（再定位章节）；**命中 0 个** → 评估新建；**命中 ≥2 个** → 暂停并询问用户。完整判定流程见 §3.8。
> **单一事源**：各子文件的定位 / 角色 / 主题 / 收录范围，**只在其自身 YAML Front Matter 的 5 个字段中权威定义**（就近维护、随文件生命周期走）。主文件「写前路由卡」与本节**均不承载明细数据**——明细由工具生成，彻底消除人工同步成本。

**① 字段规范（子文件 YAML 必填 6 项 = 5 四要素 + 1 文件编号）**

| 字段 | 语义 | 写法要求 |
|------|------|---------|
| `file_number` | **文件编号（N）**：该文件在编号体系中的 N 值 | 非负整数；手册=0、子记忆文件从 1 起；**由 `add` 自动分配（取当前最大 N + 1），禁止手工指定**；参与章节编号（§2.3），不参与归属判定 |
| `positioning` | **定位**：这个文件是什么 | 一句话，单行不换行 |
| `role` | **角色**：承担哪一层职能 | 取自层级枚举：硬约束层 / 编码规范层 / 作业流程层 / 经验教训层 / 能力台账层 |
| `theme` | **主题**：覆盖的领域范围 | 用 ` / ` 分隔 3~6 个主题词 |
| `scope_in` | **收录范围**（in-scope） | 用 `；` 分隔具体条目，逐条可判、可核 |
| `scope_out` | **不收录范围**（out-of-scope） | 用 `；` 分隔，且**每项须以 `→` 指向应去的子文件**（防边界漂移） |

**② 生成链路（单向数据流，无人工同步）**

```
子文件 YAML 的 5 个四要素字段  ←── 唯一数据源（人工就近维护，改四要素只改这一处）
                        （第 6 字段 `file_number` 不参与路由卡生成，规范见 §2.3）
        │
        ├─ memory-mgr.py index ──► 主文件 <!-- ROUTE_CARD_START/END --> 写前路由卡（派生视图）
        └─ memory-mgr.py check 维度10 ──► 四要素缺字段告警（WARN 级）
```

- 主文件路由卡**禁止手工编辑**：`index` 会整区重建，手工修改将被覆盖（同主索引区规则，见 §3 约定 6、7）。
- 本手册**不列四要素明细**：要看明细，读主文件路由卡（运行时视图），或直接读目标子文件 YAML（权威源）。
- 四要素是**子文件**的必填项；主文件 `parent: null`，不需要四要素。

**③ 边界判据（易混项裁决）**

| 易混对 | 判据 |
|--------|------|
| 能力台账（子文件5） vs 作业流程 | 子文件5 回答"**有什么工具、单个工具怎么调**"；若内容回答"**多个工具按什么步骤协同、失败如何降级、分几个场景**"，则属作业流程，子文件5 不收录，须按 §3.8 第 3 步评估新建 |
| 规则条文（子文件1、2） vs 坑案例（子文件4） | 前者回答"**必须怎么做**"（规范）；后者回答"**曾经踩过什么坑、怎么修**"（案例）。同一事项的规则与案例分置两文件，各自权威 |
| GitHub 流程（子文件3） vs 通用代码写法（子文件2） | 凡**绑定 git / gh 命令或平台机制**的步骤 → 子文件3；与平台无关的编码纪律 → 子文件2 |

> **新增子文件时**：必须先按 §3.8 第 3 步定义上述 5 个字段并写入新文件 YAML，再执行 `add`（工具参数见 §7.6）。字段缺失由 `check` 维度 10 报警，路由卡对应单元格显示 `[待补]`。

### 2.3 章节编号规范（v5.0.0 重构 · 编号体系唯一权威定义源）

> **适用范围**：全部**子记忆文件**（`Memory-*.md`）。本手册自身编号独立（§0~§11），**不适用**本节。

**① 编号格式（三层，一律用连字符分隔）**

| 层级 | 语法 | 语义 | 实例 |
|---|---|---|---|
| 章（H2） | `## N-M 标题` | N = 文件编号；M = 本文件内章序 | `## 3-10 gh 能力速查` |
| 节（H3） | `### N-M-X 标题` | X = 本 M 内节序 | `### 3-10-8 发布与制品` |
| 小节（H4） | `#### N-M-X-Z 标题` | Z = 本 X 内小节序 | `#### 3-13-3-1 git 推送失败的两类情形` |

**H1 不参与编号**；体系中**不再使用**"第X篇"这类篇标题。

**② 编号分配规则**

- **N（文件编号）**：手册 = 0；子记忆文件从 1 起顺序分配（当前：1 = 全局禁令、2 = 代码纪律、3 = GitHub、4 = 避坑铁律、5 = 入口能力）；**新建文件取当前最大 N + 1**（故从 6 起延续）；删除文件只腾空该 N，**不动其他文件编号**。
- **M / X / Z**：各自在**父级内**从 1 开始、连续、不重复、不跳号；**只在本文档内有意义，不跨文件继承**。
- **H4 存在则编、不存在不造**（当前仅 `Memory-全局禁令与环境约束.md` 与 `Memory-GitHub全流程操作.md` 含 H4）。

**③ 分隔符必须用连字符 `-`（硬性）**

原因：GFM 锚点规则会**删除点号**。若写成 `## 3.10`，编辑器算出的锚点是 `310-...`，而工具按保留连字符算出 `3-10-...`——两者不一致，会产出「工具校验通过、编辑器点击不跳」的**假通过**。写成 `## 3-10` 则两侧一致。

锚点换算示例：`## 1-2 全局禁令（最高优先级）` → `#1-2-全局禁令最高优先级`。

**④ 唯一性要求**

编号须**不重复、不跳跃、不乱、唯一**。由 `check` 维度 11 自动校验（判据见 §3.9）；**禁止手工改号**（§0.12）。

**⑤ 与原编号体系的差异（迁移说明）**

原体系 `## 1`~`## 29` 为**跨文件连续**编号——任一文件增删都会引起后续文件编号连带变动。新体系改为「文件号前缀 + 文件内局部序号」，实现**文件间编号解耦**：任一子文件的存续、灭失、内部重排，均不影响其他文件的编号。主索引表与各文件头部速查索引表由工具重建，自动跟随。

---

## 3. 核心约定（纠偏时必须遵守）

1. **绝对路径双轨制**：主文件在 C 盘、子文件在 D 盘，跨盘符**必须**用绝对路径（`file:///D:/...` / `file:///C:/...`）。相对路径无法跨盘。
2. **章节编号（v5.0.0 重构）**：`## N-M` / `### N-M-X` / `#### N-M-X-Z`——N 为文件编号，M / X / Z 为各自父级内从 1 起的顺序号；**编号只在本文档内有意义，不跨文件连续**；分隔符**必须用连字符 `-`**（点号会被锚点规则删除）；H1 不参与编号。完整规范见 **§2.3**。
3. **锚点格式（GFM）**：全小写、空格换连字符、去特殊字符（**点号会被删除**，故编号必须用连字符）。例：`## 1-2 全局禁令` → `#1-2-全局禁令`；`### 1-2-1 禁止强推` → `#1-2-1-禁止强推删除自家main`。
4. **单一事源**：同一事实只在一处权威定义；他处用**单向**链接引用，禁止重复定义、禁止 A↔B 循环引用。
5. **YAML 字段**：所有文件均须 8 个必填字段 `title` / `topic` / `tags` / `related` / `scope` / `created` / `updated` / `parent`（子文件 `parent: "MEMORY.md"`，主文件 `parent: null`）；**子文件另须 6 个字段** = 5 个四要素字段 `positioning` / `role` / `theme` / `scope_in` / `scope_out`（规范见 §2.2）+ 1 个编号字段 `file_number`（规范见 §2.3）。字段缺失分别由 `check` 维度 10 / 维度 11 报警。四要素是主文件写前路由卡的唯一数据源，**只改子文件 YAML 一处即可**，无需同步手册或主文件；`file_number` **由 `add` 自动分配（取当前最大 N + 1）**，禁止手工指定。
6. **主文件的工具写入区（路由卡区 + 主索引区，均禁止手工编辑）**：主文件有两个由工具重建的区块——`<!-- ROUTE_CARD_START -->`…`<!-- ROUTE_CARD_END -->`（写前路由卡，由 `index` 从子文件 YAML 四要素生成，见 §2.2）与 `<!-- MAIN_INDEX_START -->`…`<!-- MAIN_INDEX_END -->`（主索引表）。**主索引区只收子记忆文件条目——设计需要，非缺陷**：`index` / `sync` / `add` 重建该区域时，**仅遍历 `Memory-*.md` 子记忆文件生成条目**——主索引的职能即"子记忆文件路由表"，这是**设计需要**。由此得出四条推论（前三条针对主索引区，第四条对两区同时成立）：
   - **不得手工编辑这两个区间**。区间内容由工具整区重建，手工修改将在下次 `index` 时被覆盖；
   - 区间内指向**非子记忆文件**的条目（含指向本手册的条目）在重建时会被清除，属**预期行为**，不得据此判定工具异常；
   - **不得依赖主索引承载本手册入口**。本手册入口须固化在子记忆文件章节内（见 `Memory-入口能力与图谱.md` 的「memory-mgr.py 维护工具」章节），或依赖 SOUL.md「记忆文件维护门禁」中给出的硬编码绝对路径；
   - **路由卡区的唯一数据源是子文件 YAML 的四要素字段**（见 §2.2）。改四要素**只改目标子文件 YAML**，再跑 `index` 重生成；直接改主文件路由卡或本手册都会造成双源，前者被覆盖、后者与本手册"不列明细"的约定冲突。
7. **子文件头部索引区**：子文件内 `> **本文件速查索引**` 到 `<!-- INDEX_END -->` 之间是工具写入区。
8. **归属判定前置（新信息入库必做）**：任何新信息写入主文件或子文件之前，**必须先按 §3.8 判定归属**；判定结果不唯一（0 命中且不满足新建门槛，或多归属）时**暂停并询问用户**，不得直接写入。

### 3.8 归属判定规则（新信息入库决策树）

> **权威性**：本节是"新信息该放到哪里"的**唯一定义处**；判据来源是各子文件 YAML 的 `scope_in` / `scope_out` 字段（字段规范见 §2.2；运行时视图 = 主文件写前路由卡）。**禁止**跳过本节直接执行 `add`（工具调用见 §7.6）。

**输入**：一条待入库的新信息（来自素材文件、对话结论、工作日志、外部资料等）。
**输出**：① 并入既有子文件（含目标章节）；② 新建子文件（含新文件四要素）；③ 暂停并询问用户。

**第 0 步 · 预处理**：把新信息提炼为「主题短语 + 3~6 个关键词」，剔除一次性任务痕迹（日期、临时路径、单次执行结果）。

**第 1 步 · 匹配既有子文件**（判据 = 各子文件 YAML 的 `scope_in`，经主文件写前路由卡「收录（in-scope）」列呈现）
- 命中 **1** 个 → 进入第 2 步；
- 命中 **0** 个 → 进入第 3 步（新建评估）；
- 命中 **≥2** 个 → 进入第 4 步（多归属，暂停询问）。

**第 2 步 · 定位到章节**（仅在目标子文件内部）
1. 用该子文件头部速查索引表定位候选 `## N-M` / `### N-M-X`（大文件先用 `get-offset`，禁止全量灌入上下文）；
2. 校验单一事源（§3.4）：
   - 目标章节**已有同一事实** → **不新增**，改为就地**补充 / 修订**该处，避免重复定义；
   - 无同一事实但章节主题覆盖 → 在该章节内**增补段落或 `### N-M-X`**；
3. 目标子文件**无任何章节覆盖该主题** → 在该文件内**新增 `## N-M`**（走 §7.6 的 `add` / `rewrite` 流程，编号由工具生成，禁止手工指定）；
4. 出现 **≥2 个章节均可承载且无法择一** → 进入第 4 步。

**第 3 步 · 新建子文件评估**（三个门槛须**同时**满足）

| 门槛 | 判据 | 不满足的处置 |
|---|---|---|
| ① 独立性 | 主题不落入任一既有子文件的 in-scope（已由第 1 步 0 命中确认） | 回到第 2 步重新定位 |
| ② 可复用性与体量 | 内容是**跨会话可复用的规则 / 流程 / 事实**，且体量足以独立成篇 | 并入主题最近的子文件相应章节 |
| ③ 无冲突 | 不构成对既有子文件权威章节的重复定义 | 按 §7.3 改为单向引用 |

**通过三门槛后，新建前必须先定义「四要素」**（写入新子文件 YAML 的 5 个字段；规范见 §2.2）：
1. **定位**：一句话——这个文件是什么；
2. **角色**：承担哪一层职能（硬约束层 / 编码规范层 / 作业流程层 / 经验教训层 / 能力台账层）；
3. **主题**：覆盖的领域范围；
4. **收录范围**：明确列出 in-scope 与 out-of-scope；out 项须**指向应去的子文件**，防止边界漂移。

> 四要素未定义完成，**不得执行 `add`**。新增后**无需**手工同步手册或主文件——`add` 内部会调 `index`，把新子文件的四要素自动并入主文件写前路由卡。

**第 4 步 · 暂停与询问（用户裁决前置）**

**触发条件**（满足任一条即暂停，不得自行裁决）：
1. 第 1 步命中 0 个，但第 3 步三门槛未同时满足（含"体量不足以独立成篇"却无合适归属）；
2. 多归属：命中 ≥2 个子文件或 ≥2 个章节，且无法用各子文件的 `scope_out` 排除；
3. 与既有权威章节存在潜在重复，但无法判定哪一处为权威；
4. 内容涉及隐私 / 凭证 / 敏感信息（先过「入库隐私闸门」，权威定义见 `Memory-全局禁令与环境约束.md`，本节不重复定义）；
5. 内容涉及体系外文件（越界引用风险，见 §6 D-8）。

**暂停时的输出要求**（缺一不可）：
- **事由**：卡在哪一步、哪一条判据；
- **解释**：为何无法唯一判定（列出各候选的命中点与排除障碍）；
- **候选方案**：给出 2~3 个可选归属，标注**推荐项**及其理由；
- **影响面**：各方案对章节编号、索引、跨文件锚点的影响。

**用户裁决后**：按裁决执行，并在 §7.6 的操作记录中注明"归属经用户裁决"。

### 3.9 编号完整性保障制度（10 项判据 + 4 条纪律）

> **目的**：保障编号**不重复、不跳跃、不乱、唯一**（§2.3 ④）。编号是章节定位锚的唯一来源，一旦错乱将连带锚点失效。

**① 10 项判据（工具自动校验）**

| # | 判据 | 级别 | 校验方 |
|---|---|---|---|
| 1 | `file_number` 存在（每个子记忆文件 YAML 必有） | ERROR | `memory-mgr.py` 维度11 |
| 2 | `file_number` 全局唯一（无两份文件同 N） | ERROR | `memory-mgr.py` 维度11 |
| 3 | `file_number` 取值合法（非负整数） | ERROR | `memory-mgr.py` 维度11 |
| 4 | 章编号前缀与 YAML `file_number` 一致（`## N-M` 的 N 必须等于该文件 YAML 值） | ERROR | `memory-mgr.py` 维度11 |
| 5 | 章 M 在本文件内从 1 起连续、不重复、不跳号 | ERROR | `memory-mgr.py` 维度11 |
| 6 | 节 X 在所属 `## N-M` 内从 1 起连续 | ERROR | `memory-mgr.py` 维度11 |
| 7 | 小节 Z 在所属 `### N-M-X` 内从 1 起连续 | ERROR | `memory-mgr.py` 维度11 |
| 8 | 层级从属正确（`###` 必挂 `##` 之下、`####` 必挂 `###` 之下） | ERROR | `memory-mgr.py` 维度11 |
| 9 | 编号格式合法（匹配 `^\d+(-\d+)*\s`） | ERROR | `memory-mgr.py` 维度11 |
| 10 | **锚点唯一性**（同一文件内锚点不重复，防层级坍缩碰撞） | ERROR | `断链检测.py` |

**职责划分依据（单一事源）**：`memory-mgr.py` 负责**格式级 + 结构级**（它生成编号，生成与校验同源）；`断链检测.py` 负责**链接级**（已有 `anchor_set`，天然可查重复）。

**② 4 条纪律（人工必守）**

| # | 纪律 | 说明 |
|---|---|---|
| 1 | **新建必写 `file_number`** | 由 `add` 自动分配（取当前最大 N + 1），禁止手工指定；手工创建文件须先跑 `number --init` 补齐。 |
| 2 | **编号变更必须走 `rewrite`** | 禁止手工改号（§0.12）。`rewrite` 自动同步跨文件与同文件锚点，并重建索引。 |
| 3 | **双绿方可交付** | 任何编号变更后必须跑 `check` + `断链检测.py`，两者全绿才算完成。 |
| 4 | **编辑器抽样验证** | 编号变更后须在 Markdown 编辑器中**实际点击 ≥3 条锚点链接**确认能跳转——工具校验不了编辑器行为，此项专治「工具绿、点击死」。 |

**③ 编号变更标准流程**

```bash
uv run ... memory-mgr.py --no-interactive rewrite --dry-run   # 预览新旧编号映射与锚点映射
uv run ... memory-mgr.py --no-interactive rewrite             # 执行（含跨文件 + 同文件锚点同步）
uv run ... memory-mgr.py --no-interactive index               # 重建主索引与各文件头部索引
uv run ... memory-mgr.py --no-interactive check               # 维度11 编号完整性
uv run ... python 断链检测.py                                  # 锚点唯一性 + 链接可达性
# [人工] 编辑器抽样点击 3 条锚点链接
```

---

## 4. 工具总览

### 4.1 memory-mgr.py（主维护工具，零依赖标准库）

**统一调用模板**（非交互、Agent 调用标准写法）：
```bash
cd "<<子记忆文件目录绝对路径>>"
uv run --project D:\Tools\Assembly\python\myenv python memory-mgr.py ^
  --main-file "<<主记忆文件绝对路径>>" ^
  --sub-files-dir "<<子记忆文件目录绝对路径>>" ^
  --no-interactive <子命令> [选项]
```

**子命令速查**：

| 子命令 | 用途 | 典型用法 |
|--------|------|---------|
| `check` | 双向完整性检查（**12 维度**；v4.5.0 增维度9 回链存在性，v4.7.0 增维度10 四要素完整性，**v5.0.0 增维度11 编号完整性、维度12 清单一致性**） | `check`（**只读报告，绝不自动改**） |
| `validate` | 综合校验（check + 索引一致性 + 12 条纪律 + **§10 格式附加校验**） | `validate`（附加校验为提示级，不参与通过判定） |
| `index` | 重建主索引 + **写前路由卡**（v4.7.0）+ 各子文件头部索引 | `index`（全量）/ `index --file "Memory-x.md"` |
| `add` | 新增子文件（自动编号+索引+主索引+**路由卡**；v4.5.0 起自动补 tags、补子→主回链、剥离冗余 H1；v4.7.0 修复 `--title`/`--summary` 空转并新增**四要素参数**；**v5.0.0 起自动分配 `file_number` = 当前最大 N + 1**） | `add --topic "主题" --content "..." --positioning "..." --role "..." --theme "..." --scope-in "..." --scope-out "..." --dry-run` |
| `number` | **（v5.0.0 新增）** 批量分配 / 校正子记忆文件的 `file_number`（手册=0，子记忆文件自 1 起） | `number --init --dry-run` → `number --init` |
| `remove` | 删除子文件（清引用+重编号） | `remove --file "Memory-x.md" --dry-run` → `--force` |
| `rewrite` | 重算所有章节编号为 `N-M-X-Z`（**含 H4**；含跨文件 + 同文件锚点同步） | `rewrite --dry-run` → `rewrite` |
| `get-offset` | 返回章节行号范围（配合 Read 精准读） | `get-offset --file "Memory-x.md" --section "## N-M 完整标题"` |
| `sync` | 同步 memory-state.json 与实际文件（v4.5.0 起**逐文件列明变更类型**，并声明主文件是否被索引重建改动） | `sync` |
| `changelog` | 查看变更历史（自包含；`--show <commit>` 走 git show） | `changelog --days 7` |
| `diff` | 比较版本差异（git 优先，否则基于 changelog） | `diff --before <日期> --after <日期>` |
| `restore` | 回滚到 git 历史版本（需 `--force`） | `restore --file "Memory-x.md" --commit <hash> --force` |
| `help` | 帮助 | `help <子命令>` |

> **重要**：`add`/`remove`/`rewrite` 末尾会自动调用 `check`；`get-offset` 支持前缀匹配（`--section "## 1-1"` 可匹配 `## 1-1 总则`）。
>
> **编号体系（v5.0.0 重构）**：`rewrite` 按各文件 YAML 的 `file_number` 生成 `N-M-X-Z` 编号。**已废弃**跨文件连续编号与 `SUB_FILE_ORDER` 硬编码顺序清单——子文件排序一律按 `file_number`（该字段就近维护于各文件 YAML，工具零硬编码文件名）。规范见 §2.3，保障制度见 §3.9。
>
> **主索引重建的设计副作用（必读，依据 §3.6）**：`index` / `sync` / `add` 都会重建主索引，且**只保留子记忆文件条目**。若此前在 `MAIN_INDEX` 区间内手工加入过指向非子记忆文件的条目（如本手册入口），重建后**会被清除**——属预期行为，不作异常处理。执行上述命令后只需复核一件事：本手册入口在子记忆文件（`Memory-入口能力与图谱.md` 的「memory-mgr.py 维护工具」章节）内是否仍可达（主索引内缺失不必补回）。
>
> **写前路由卡（v4.7.0 新增，四要素工具化的落地）**：`index` 会在主文件 `<!-- ROUTE_CARD_START -->`…`<!-- ROUTE_CARD_END -->` 区生成「子记忆文件定位与收录范围」表，数据**全部来自各子文件 YAML 的四要素字段**（`positioning` / `role` / `theme` / `scope_in` / `scope_out`）。因此改四要素**只改子文件 YAML 一处**，再跑 `index` 即可，手册与主文件**均无需人工同步**。首次运行时工具会自动清理旧版手工维护的同名路由卡区（幂等，只清一次）；四要素缺字段时该单元格显示 `[待补]`。

### 4.2 断链检测.py（只读·链接级分析）

- **用途**：量化各文件链接的真实状态（锚点存在性 + 越界引用 + 回链存在性 + 无锚点链接），打印报告，**不写文件**。
- **调用**：`uv run --project D:\Tools\Assembly\python\myenv python 断链检测.py`
- **v4.5.0 输出分段**：
  | 分段 | 检查内容 | 与 `memory-mgr.py` 的关系 |
  |------|---------|--------------------------|
  | 断链锚点 | 带 `#锚点` 的链接是否真实存在 | 与 `check` 维度4 互补（本脚本给候选目标） |
  | **P-1 越界引用** | 指向 `Memory-Data` 目录内非 `Memory-*.md`、且不在白名单（本手册）的引用 | **`check` 的盲区**（见 §6 D-8），本脚本已覆盖 |
  | **P-2 回链存在性** | 子文件是否含指向 `MEMORY.md` 的链接 | 与 `check` 维度9 同口径（P-2 为脚本侧复核） |
  | **P-3 无锚点链接** | 无 `#` 的纯文件链接清单（仅供核对，非缺陷） | 补 `check` 与旧版脚本的漏检面 |
- **局限（必须知道）**：
  1. 旧版**只校验带 `#锚点` 的链接**、跳过纯文件链接；v4.5.0 已增 **P-3** 单列纯文件链接清单，不再静默跳过（"文件不存在"型断链仍以 `check` 维度1/3 为准）。
  2. ~~**硬编码 5 个子文件名**~~ **已修复（v4.2.0，对应 §6 D-6）**：现改为扫描目录内 `Memory-*.md` **动态发现**，子文件增减后自动适配，与 `memory-mgr.py` 的发现规则一致。
  3. 跨文件指向 `MEMORY.md` 的断锚：主文件锚点已由 `main()` 注入 `all_anchors['MEMORY.md']`，实测指向主文件的跨文件锚点可正常校验；仅在目标文件既非子文件、也非主文件时才会落入"未知文件"分支（`target_anchors` 为空）而可能误报，当前未触发。

---

## 5. 标准审计流程（每次维护后 / 每周）

> 顺序执行；务必区分"真问题"与"§6 已知误报"。

```
步骤1  运行 check，记录输出
        uv run ... memory-mgr.py --no-interactive check
        → 见 §6 过滤误报，仅保留"真实断链/真实错链"

步骤2  运行 validate，看 12 条纪律
        uv run ... memory-mgr.py --no-interactive validate
        → 重点看 纪律4(链接)、纪律2(单一事源)、纪律3(章节编号规范)

步骤3  运行锚点补查（覆盖 check 漏掉的无锚点链接）
        uv run ... python 断链检测.py
        → 仅作锚点级参考；文件存在性以步骤1/人工为准

步骤4  Agent 核对清单（必做，工具无法替代）
        执行者 = Agent（含人工）；分「A 基础一致性」「B §10 可自动化」「C §10 须判读」三段。

  A. 基础一致性核对
        □ 主索引每条"目标文件"是否真实存在？（ls 核验）
        □ **清单一致性（v5.0.0 新增，维度12 已自动化）**：主文件索引引用的文件集 ↔ 目录内
          `Memory-*.md` 实体集，是否双向一致？
          · 有引用无实体 → 先判"删除"还是"更名"：查 memory-changelog.json 有无该文件 remove 记录；
            有 remove 记录 = 已删除（清理引用）；无记录且同时出现"无人引用的新文件" = 疑似更名
            （做内容指纹确认后改挂引用）；两者皆无 = **判不了，暂停询问用户**，不得擅改。
          · 有实体无引用 → 该子记忆文件未纳入体系，须补入主文件（跑 index）与本手册。
        □ **编号完整性（v5.0.0 新增，维度11 已自动化）**：`check` 维度11 全绿 +
          `断链检测.py` 锚点唯一性无告警（10 项判据见 §3.9）
        □ **越界引用检查（工具盲区，必查，见 §6 D-8）**：主索引 / 各子文件是否引用了
          **非子记忆文件**？即 `Memory-Data` 目录内文件名**非** `Memory-` 开头的 .md，
          或本体系之外的任何文件。`check` **不会**报此类问题（它只校验指向 `Memory-*`
          子文件的链接是否有效），必须扫描排查（已由 `断链检测.py` 的 P-1 项输出，
          亦可手工执行下述命令复核）：
            grep -ohP 'file:///<<子记忆文件目录绝对路径>>/[^)#]+' \
              "<<主记忆文件绝对路径>>" Memory-*.md | sort -u
          → 输出中凡非 `Memory-*.md` 且非本手册 `WorkBuddy记忆文件说明.md` 者，即为越界，须删除。
        □ 主索引"章节位置"编号是否真有对应 `## N-M` / `### N-M-X`？（get-offset 或 Read 核验）
        □ 章节标题文字是否与子文件实际一致？（编号对但标题过时也算失同步，见 D-9）
        □ 子文件 related 字段引用的文件是否都存在？有无内容错放/重复（单一事源违反）？

  B. §10 可自动化 / 半自动化条目（工具已实现，直接读输出，无需人工逐行扫）
        □ 第3条 层级标记：`validate` 的「§10 格式强制要求」附加校验输出「缩进伪装层级」命中
            （标题跳级由 `validate` 纪律1 覆盖，二者同源不重复）
        □ 第6条 常用前置：`validate` 附加校验输出「索引表缺失」（文首 40 行内无
            速查索引 / 主索引 / 索引表字样即报）
        □ 第9条 禁用词：`validate` 附加校验输出命中词 + 文件:行号
            禁用词 = 视情况 / 一般 / 通常 / 酌情 / 尽量 / 尽可能 / 适当 / 必要时 / 原则上 / 建议
        □ 第10条 引用方向：`validate` 附加校验汇总前向引用（X > 所在章号）；
            Agent 依第10条导航豁免判定：属速查索引表 / 映射表 / "见 §X"导航指引者豁免，
            其余前向引用须改为后序引用或删除

  C. §10 须判读条目（工具无法替代，Agent 按下述要点逐项判定）
        □ 第1条 格式保真与标题适配：逐条比对「标题 ↔ 其所辖内容」——
            ① 仅当标题与内容不匹配时才改写标题；② 已匹配则保持原样，禁止顺手改标题；
            ③ 只调标题/排序/引用，不改文件基本格式（Markdown/TXT 形态）
        □ 第2条 层级递进：父标题是否真正统摄子标题、子标题是否细化父标题；
            不得平铺罗列同级条目，不得以缩进/空行/手动编号伪装层级
        □ 第5条 强相关相邻与线性排序：同类 / 同流程 / 因果链章节是否紧挨并按工序先后排序；
            仅当工序本身为循环迭代时允许循环排序，且须在该处显式标注「必要循环」
        □ 第12条 引用精准唯一：① 引用目标须能在正文精确命中唯一一节，禁"详见相关章节"式模糊；
            ② 若目标文件含速查索引表 / 映射表，引用文字须与该表第二列逐字一致；
            ③ 禁裸编号（`12.3`）与"章标题+裸节号"。辅助扫描（可选）：
               grep -nP '详见(相关|上述|前述)章节|参见上文|如前所述' MEMORY.md Memory-*.md
               grep -nP '(?<!见 )\b§?\d+\.\d+\b(?!\s*[章節])' MEMORY.md Memory-*.md  # 粗筛裸编号

步骤5  形成"待纠偏清单"，按 §7 逐条处理
```

**check 输出解读对照表**：

| check 输出 | 是否真问题 | 处理 |
|-----------|-----------|------|
| `[维度1] 主文件断链: <非 Memory- 文件>` | **已消除（v4.2.0 修复 D-1）** | 旧版因 `'Memory-' in url` 误匹配 `Memory-Data` 目录名而误报；已改按文件名 `startswith('Memory-')` 判定。若仍出现，按真断链处理（§7.1）。 |
| `[维度3] xxx 断链: file:///...不存在路径` | **多半是真** | 见 §7.1 修复（先 `ls` 确认目标确实不存在）。 |
| `[维度4] 锚点不存在` | **是真** | 见 §7.2 修复锚点。 |
| `[循环引用] ... -> MEMORY.md -> ...` | **已消除（v4.2.0 修复 D-2）** | 旧版把 `MEMORY.md` 纳入环图，致主↔子中枢模式被误报；现环图仅含子文件节点。若仍出现，即真「子↔子」环，须拆除其一。 |
| `[维度6] 疑似重复内容` | **待核实** | 可能是真重复，也可能是相似表述；人工读原文裁决（§7.3）。 |
| `[维度9] xxx: 缺少指向主文件的回链` | **是真（WARN 级）** | 缺回链会破坏主↔子双向闭环，在文件末尾补 `[→主文件](file:///.../MEMORY.md)`，并按 §7.6 验证。 |
| `[维度10] xxx: 缺少四要素字段 [...]` | **是真（WARN 级）** | 该子文件 YAML 缺 `positioning` / `role` / `theme` / `scope_in` / `scope_out` 之一，主文件路由卡对应单元格显示 `[待补]`，归属判定失去依据。按 §2.2 字段规范补齐后重跑 `index`（§7.7）。 |
| `[维度11] xxx` 各类编号问题（`file_number` 缺失 / 重复 / 非法、章前缀与 `file_number` 不符、M / X / Z 不连续、层级从属错位、格式非法） | **是真（ERROR 级）** | 编号完整性问题，判据见 §3.9 ①。**禁止手工改号**——按 §3.9 ③ 流程跑 `rewrite`（以 YAML `file_number` 为准重算编号并同步锚点），再跑 `index` + `check` + `断链检测.py` 复核。 |
| `[维度12] 清单不一致：引用了不存在的文件 xxx` | **是真（ERROR 级）** | 主文件索引引用了目录中不存在的实体 → 按 §5 步骤4 A 段判定"删除 or 更名"；判不了即**暂停询问用户**，不得擅改。 |
| `[维度12] 清单不一致：xxx 未被引用` | **是真（WARN 级）** | 目录内存在子记忆文件实体但主文件索引未收录 → 跑 `index` 重建即可；若重建后仍缺失，核对该文件 YAML 是否合规（`file_number` 是否已写、文件名是否 `Memory-` 前缀 + `.md` 后缀）。 |

---

## 6. 已知缺陷与误报清单（关键，必读）

> 本节是"工具输出 vs 事实"的裁决表。凡下表列出的现象，**以本节为准**，不要据此自动修复。

| 编号 | 缺陷/误报 | 现象 | 性质 | 处理 |
|------|----------|------|------|------|
| D-1 | `check` 维度1 按 `'Memory-' in url` 过滤（旧逻辑） | 主文件指向本目录内、文件名非 `Memory-` 开头的合法文件（如 `WorkBuddy记忆文件说明.md`）曾被误报为断链 | **已修复（v4.2.0）** | 根因：`url` 中 `Memory-Data` 目录名误命中 `'Memory-'` 子串。已改按文件名 `filename.startswith('Memory-')` 判定子文件；重跑 `check` 已无维度1 误报。 |
| D-2 | `check` 循环引用把 `MEMORY.md` 纳入图节点（旧逻辑） | 8 条"循环引用"均经主文件中转（主↔子回链）曾被误报 | **已修复（v4.2.0）** | 中枢模式合法。已改"循环检测图仅含子文件节点，排除 MEMORY.md 中枢"，与 `validate` 纪律5 一致；重跑 `check` 已无循环引用误报。 |
| D-3 | `Memory-入口能力与图谱.md` 断链 README | 指向 `D:/Documents/AI_Work_Temp/2026-08-27-17-23-58/终版/README.md`（日期目录已不存在） | **真（已修复）** | 已改指向 `WorkBuddy记忆文件说明.md`（本文件）。 |
| D-4 | 主索引 `## 30` 失效引用 | 主索引第 93 行称 `WorkBuddy记忆文件说明.md` 位于 `## 30`，但无子文件有 ## 30 | **真（已修复）** | 已改为指向真实章节 `## 29 memory-mgr.py 维护工具`。 |
| D-5 | `内容完整性核验.py` 崩溃（旧） | 依赖缺失的一次性备份 `.bak`，运行即 `FileNotFoundError` | **已加固（v4.2.0）** | 已加缺失备份守卫：备份不存在时打印清晰提示并优雅退出，不再崩溃；子文件清单亦改为动态发现。本工具仍定位为迁移期一次性工具，日常勿调用。 |
| D-6 | `断链检测.py` 硬编码文件清单（旧） | 仅查 `#锚点` 链接、**原硬编码 5 个子文件名**、不扫磁盘新增文件 | **已加固（v4.2.0 + v4.5.0）** | v4.2.0：子文件清单改为扫描目录内 `Memory-*.md` 动态发现。v4.5.0：新增 P-1 越界引用、P-2 回链存在性、P-3 无锚点链接三段输出，不再静默跳过纯文件链接。文件存在性与全量断链仍以 `memory-mgr.py`（动态发现）为准。 |
| D-7 | `check` 同文件 `#anchor` 链接误判 | 形如 `[文本](#锚点)` 的同胞链接会被维度3当成"文件缺失" | **潜在假阳性（未触发）** | 当前无此类链接；若出现，属工具 BUG，勿据此修复。 |
| D-8 | `check` **无法检测越界引用**（工具盲区） | 主索引 / 子文件引用了非子记忆文件（`Memory-Data` 内非 `Memory-` 开头的 .md，或本体系外文件）时，`check` 仍报"所有检查通过" | **工具盲区（非 BUG）** | `check` 维度1/3 只校验"指向 `Memory-*` 子文件的链接是否有效"，**不校验"是否引用了不该引用的文件"**。**已由 `断链检测.py` P-1 段自动扫描（v4.5.0）**，§5 步骤4 A 段命令可复核。发现即删除该引用行；若该知识本体系需要，应先由用户授权迁移内容到子文件后再建索引，**严禁直接读取/引用体系外文件**。 |
| D-9 | 主索引"章节标题文字"过时（编号对、标题旧） | 主索引写 `## 28 mimo.code协同`，实际文件 `## 28` 已是 `DeusData本地代码知识图谱` | **真（v4.3.0 已修复）** | `check` 只校验编号与链接，不校验标题文字 → 亦属工具盲区。核验办法：提取子文件真实 H2（`grep -oP '^## \d+.*' <子文件>`）与主索引逐条比对。发现即按实际标题更新主索引。 |
| D-10 | `remove --dry-run` 单独使用仍交互追问（v4.5.0 测试发现，编号 F-7） | 非交互环境执行 `remove --file X --dry-run` 时仍弹 `确认删除 X？(y/N)`，抛 `EOFError` | **易用性缺陷（低危，未修复）** | 预览开关与免确认开关是**两个独立判据**。现处置：预览一律写成 `remove ... --dry-run --force`（见 §7.6）。 |
| D-11 | `add` 不为素材中无编号的 `##` 自动补编号（v4.5.0 测试发现，编号 F-8） | 直接投喂"`## 执行原则`（无编号）"类素材，会产出"无编号 H2 混入编号体系"的不合规结果 | **规范缺口（中危，已用手册步骤兜底）** | 现处置：`add` 前须按 §7.6「素材规范化」三步把素材转成合规正文（去文档标题 H1 / H2 顺序编号 / 查单一事源冲突）。 |
| D-12 | `add` 的 `--title` / `--summary` 声明了但静默失效（v4.7.0 工具化改造中发现） | 传 `--title "X"` / `--summary "Y"` 时，生成文件的 YAML 中不出现 `title` / `summary` 对应值（`title` 退化为 topic） | **真（v4.7.0 已修复）** | 根因：`main()` 以**位置参数**调用 `mgr.add(...)`，只按序传了前 6 个形参，`title` / `summary` 从未被传递。已改为**关键字传参**并补齐新增的四要素参数。 |
| D-13 | `strip_heading_number` 不认连字符编号（v5.0.0 编号重构中发现） | 正则 `^\d+(?:\.\d+)*\s*` 只识别点号分隔编号；对 `3-10 标题` 会误剥成 `-10 标题`，导致锚点映射全线错乱 | **真（v5.0.0 已修复）** | 正则改为 `^\d+(?:[-.]\d+)*\s*`，同时兼容点号与连字符。此为本次重构的**最高危缺口**——若不修，`rewrite` 会把所有锚点算错。 |
| D-14 | `rewrite` 不处理 H4（四级标题）（v5.0.0 编号重构中发现） | 重编号循环只匹配 H2 / H3，`#### N-M-X-Z` 保持旧格式不变，与新体系不符 | **真（v5.0.0 已修复）** | 增加 H4 处理分支：Z 在所属 `### N-M-X` 内从 1 递增。 |
| D-15 | `_sync_cross_file_anchors` 只收集 H2 / H3 标题（v5.0.0 编号重构中发现） | H4 编号变化后，其锚点不会被同步，指向 H4 的链接会断 | **已加固（v5.0.0）** | 标题收集增加 H4 项。实测当前**无任何链接指向 H4**，影响为零；加固属防未来。 |
| D-16 | 子文件排序依赖 `SUB_FILE_ORDER` 硬编码清单（v5.0.0 前） | 工具内写死 5 个文件名；新增子文件未登记时落入字典序兜底分支，第二次新增会出现文件名插队致既有新文件编号跳变（G6） | **真（v5.0.0 已删除）** | 改为按各文件 YAML 的 `file_number` 排序，实现零硬编码。又因新编号体系不再跨文件连续，排序已不参与编号生成，兜底分支危害归零。 |

> **内部矛盾提示**：`check` 报 8 条循环引用，但 `validate` 的"纪律5-无循环引用"却通过——因为 `validate` 的环检测**正确排除**了 `MEMORY.md` 节点，而 `check` 没有。二者矛盾印证 D-2 是 `check` 的 BUG。

---

## 7. 纠偏手册（模块化 SOP）

> 每条 SOP 都是"识别 → 决策 → 执行 → 验证"闭环。执行前先 `ls` 核验路径。

### 7.1 修复断链（链接目标文件不存在）

**识别**：`check` 维度3 报告 `[维度3] xxx 断链: file:///...不存在路径`，且 `ls` 确认目标确实不存在。
**决策**：
- 若目标文件**应存在**（只是路径写错）→ 改链接路径为正确绝对路径。
- 若目标文件**已改名/迁移** → 改为指向新位置（如 D-3 把 README 改为指向本说明文件）。
- 若目标文件**已废弃** → 删除该链接（保留同行其余内容）。
**执行**（以 D-3 为例，已修复）：
```
原：> 详细说明见独立文档 [README.md](file:///D:/Documents/AI_Work_Temp/2026-08-27-17-23-58/终版/README.md)。
改：`file:///<<子记忆文件目录绝对路径>>/WorkBuddy记忆文件说明.md`
```
**验证**：`check` 该维度3错误消失（注意 D-1/D-2 误报仍会在，属正常）。

### 7.2 修复错链（锚点 `#xxx` 不存在）

**识别**：`check` 维度4 报告 `[维度4] 锚点不存在`。
**执行**：按 §3.3 重算目标标题的 GFM 锚点（全小写、空格→`-`、去特殊字符），改正链接中的 `#anchor`。
**验证**：`check` 维度4 该条消失；或用 `断链检测.py` 复扫锚点。

### 7.3 迁移错放内容（归属错误 / 单一事源违反）

**识别**：`validate` 纪律2 警告"疑似重复内容"；或人工发现同一规则出现在两个文件；或某内容语义更应归属另一子文件。
**决策**：确定"权威文件"（唯一事源）与"错放文件"。
**执行**：
1. 用 `get-offset` 定位错放章节行号，`Read(offset,limit)` 精准读取。
2. 将内容**剪切**到正确子文件的合适位置（新增 `##N`/`###N.X` 或并入现有章节）。
3. 在**原位置**保留一条**单向链接**指向新位置（不重复内容）。
4. 编号重排：`rewrite --dry-run` 预览 → `rewrite`（自动同步跨文件锚点 + 重建索引）。
5. 索引同步：`index`。
**验证**：`rewrite` 后 `check` + `validate` 纪律2 无新警告；`get-offset` 新位置可定位。

> 示例：若"UV 管理 Python"同时出现在两个子文件，保留 `Memory-全局禁令与环境约束.md ## 3.3` 为权威，另一处改为 `[详见 ## 3.3](file:///...#33-uv-管理-python...)` 单向链接。

### 7.4 修复主索引错误引用（如 D-4）

**识别**：主索引"章节位置"列写了不存在的 `##N-M`（如 `## 3-99`）。
**执行**（**禁止手工编辑主索引区**，见 §3 约定 6 —— 该区由工具整区重建，手改必被覆盖）：
1. 先核对该子文件的真实 H2：`grep -oP '^## \d+-\d+.*' <子文件>`；
2. 若主索引与真实标题不符 → **跑 `index` 重建**；重建后仍不符 → 核对子文件 YAML 的 `file_number` 与标题编号（跑 `check` 维度11，按 §3.9 ③ 流程处理）；
3. 若"非编号文件"条目指向错误 → 按 §7.6 修正其四要素，再跑 `index`。
**验证**：`get-offset --file "Memory-入口能力与图谱.md" --section "## 5-4"` 可定位即正确。

### 7.5 编号 / 索引失同步

**执行**（一键复位）：
```
uv run ... memory-mgr.py --no-interactive rewrite --dry-run   # 预览新编号与锚点映射
uv run ... memory-mgr.py --no-interactive rewrite             # 执行（同步锚点+索引）
uv run ... memory-mgr.py --no-interactive index               # 重建索引（已含于 rewrite，可再跑确保）
uv run ... memory-mgr.py --no-interactive sync                # 同步状态文件
uv run ... memory-mgr.py --no-interactive check               # 验收：12 维度（含维度11 编号完整性）
uv run ... python 断链检测.py                                  # 验收：锚点唯一性 + 链接可达性
```
> `rewrite` 按各子文件 YAML 的 `file_number` 确定文件顺序（**零硬编码文件名**），生成 `N-M-X-Z` 编号，并自动同步跨文件与同文件锚点。规范见 §2.3，保障制度见 §3.9。**收尾须人工在编辑器中抽样点击 ≥3 条锚点链接**（§3.9 ②-4）。

### 7.6 新增 / 删除子文件

> **决策前置（不可跳过）**：`add` 之前必须先完成 §3.8 归属判定——仅当"第 3 步三门槛同时满足 + 四要素已定义"时才可新建；判定不唯一时按 §3.8 第 4 步暂停并询问用户。**禁止**由"想新建"直接跳到命令。

**新增（新建子文件）**：
1. **素材规范化**（把外部素材转为合规正文；缺此步会产出不合规结构，见 §6 D-11）：
   - ① 去掉素材的文档标题 H1；**若素材含"第X篇"类篇标题，一并删除**（v5.0.0 起体系不再使用篇标题）；
   - ② 素材的 `##` **可保持无编号状态**——编号由工具生成：`add` 分配 `file_number` 后，`rewrite` 按 `## N-M` 自动补号（规范见 §2.3）。**禁止手工编号**；
   - ③ 逐条比对 §2.2 与既有章节，确认无同一事源的重复定义（有则改为单行引用）。
2. **预览**：`uv run ... memory-mgr.py add --topic "主题名" --content "<规范化后的文件路径>" --dry-run`
   （在预览输出中确认将分配的 `file_number` = 当前最大 N + 1）
3. **执行**：`uv run ... memory-mgr.py add --topic "主题名" --content "<规范化后的文件路径>" --positioning "..." --role "..." --theme "..." --scope-in "..." --scope-out "..."`
   （自动分配 `file_number`、补 tags 与子→主回链、生成编号、重建索引与写前路由卡、跑 `check`；四要素参数缺失时会打印 WARN）
4. **字段落库**：四要素 + `file_number` 经命令自动写入新文件 YAML（四要素也可事后直接改该文件 YAML）。
   **无需**手工回写本手册 §2.1 或主文件——`index` 已把新子文件自动并入主文件写前路由卡。
5. **验收**：`check` 维度 11 / 维度 12 无 ERROR + `断链检测.py` 锚点唯一性无告警（§3.9 ②-3）。

**删除**：
```
uv run ... memory-mgr.py remove --file "Memory-x.md" --force --dry-run   # 预览受影响引用
uv run ... memory-mgr.py remove --file "Memory-x.md" --force             # 执行（.bak 备份+清引用+重编号+check）
```
> 非交互预览**必须配 `--force`**：`remove --dry-run` 单用仍会弹出 `确认删除 X？(y/N)` 并抛 `EOFError`（§6 D-10）。`add` 只写新文件，不需要 `--force`。
> 删除前务必核对 `--dry-run` 列出的引用清单。

**验证**：`validate` 三项全通过（链接 / 索引一致性 / 12 条纪律）；`check` 维度 11（编号完整性）/ 维度 12（清单一致性）无 ERROR；`断链检测.py` 断链 0、P-1 越界 0、P-2 回链齐全、锚点唯一性无告警；**人工在编辑器抽样点击 ≥3 条锚点链接确认可跳转**（§3.9 ②-4）。

### 7.7 YAML 字段缺失

**识别**：
- 缺 8 项基础字段 → `check` 维度8 / `validate` 纪律6 警告「缺必填字段」；
- 缺四要素字段 → `check` 维度10 警告「缺少四要素字段」，同时主文件路由卡对应单元格显示 `[待补]`。

**执行**：
- 8 项基础字段：在对应子文件 YAML Front Matter 补 `title/topic/tags/related/scope/created/updated/parent`（见 §3 约定5）；
- 四要素字段：按 §2.2 ①「字段规范」补齐 `positioning` / `role` / `theme` / `scope_in` / `scope_out`，**只改该子文件 YAML**（改后跑 `index`，主文件路由卡自动更新）。

**验证**：`check` 维度8 + 维度10 均无警告；`validate` 纪律6 通过、附加校验无新增提示。

---

### 7.8 跨机器 / 换目录移植（占位符代入 SOP）

**何时触发**：主记忆文件路径或子记忆文件目录发生机器 / 路径变更时，按本 SOP 代入真实路径后再执行任何维护。

**事实源**：用户告知（优先）或按 §0.1 用 `ls` 发现并请用户确认的 **P = 主记忆文件绝对路径**、**D = 子记忆文件目录绝对路径**（D 内含子文件 / 本手册 / 维护工具 / 状态文件）。

**步骤**：
0. 取得 P、D 作为唯一事实源；不直接复用旧手册 / 旧状态文件里的路径。
1. 重写 `memory-state.json` 的 `paths` 对象：`main_file=P`、`sub_files_dir=D`、`state_file=D/memory-state.json`（注意 `sync` 不重写该对象，须手工或一次性脚本改；改前先备份）。
2. 将本手册占位符代入真实值：`<<主记忆文件绝对路径>>` → P，`<<子记忆文件目录绝对路径>>` → D（手册同时用了 `\` 与 `/`，两种分隔符都覆盖）。
3. 重写子文件 `.md` 内部链接（回链主文件 + 互链 + 手册链）：跑 `check` 维度3 + `断链检测.py` 列断链 → 按 §7.1 改指向新绝对路径（§0.1 先 `ls` 核验、§0.2 严禁 `check --fix` 盲删）。
4. 验收：跑 §5 审计（`check` + `断链检测.py` + `validate`）三项全绿；人工在编辑器抽样点击 ≥3 条锚点确认可跳转（§3.9 ②-4）。
5. 收尾：删除迁移前备份（如 `memory-state.json.bak-*`）；在 §11 变更日志记录本次移植。

**关键陷阱**：① 分隔符双轨（手册同时用 `\` 与 `/`）必须两种都覆盖；② 必须先完成步骤2（手册代入）再做步骤3，否则 Agent 仍读旧路径自我指涉；③ `%USERPROFILE%` 等环境变量不可依赖（Python 命令行与 `os.path.exists` 均不展开）；④ 状态文件 `paths` 是 `sync` 盲区，必须手工 / 脚本改。

---

## 8. 安全边界（红线）

| 红线 | 说明 |
|------|------|
| **禁止盲 `check --fix`** | `check --fix` 会按"断链"自动删除链接（D-1/D-2 历史误报虽已修复 v4.2.0，但任何工具输出仍可能含边缘误判）→ 破坏系统。纠偏必须人工逐条确认（§7），严禁无人工核对即执行 `--fix`。 |
| **改前备份** | 工具自动 `.bak`；人工改也先复制原文件。 |
| **`--dry-run` 预览** | `add`/`remove`/`rewrite` 先预览再执行。 |
| **破坏性需 `--force`** | `remove`/`restore` 非 `--force` 仅警告退出。 |
| **存在性优先于删除** | 任何"删除/修复"前先 `ls` 确认目标真实状态，不凭工具输出。 |
| **meta scope 直改** | 本目录与主文件可在主工作树直接改，不需 worktree、不需 git 提交（本地维护）。 |
| **主文件工具写入区禁改** | 主文件 `ROUTE_CARD` / `MAIN_INDEX` 两区由工具整区重建，手工编辑会被覆盖；须改数据源（子文件 YAML 四要素）或走 §7 工具流程。 |
| **编号禁手工改** | 章节编号（`## N-M` / `### N-M-X` / `#### N-M-X-Z`）与 `file_number` 一律由工具生成（`number` / `add` / `rewrite`）。手工改号会导致编号与 `file_number` 不一致、锚点失同步，并违反 §2.3 ④ 的唯一性要求。 |
| **编号分隔符禁用点号** | 编号必须用连字符 `-`（如 `## 3-10`），**禁止**点号形态（如 `## 3.10`）——点号会被 GFM 锚点规则删除，造成「工具校验通过、编辑器点击不跳」的假通过（详见 §2.3 ③）。 |
| **不碰大文件全量** | `Memory-GitHub全流程操作.md` 超千行，用 `get-offset`+`Read(offset,limit)` 精准读，不全量灌入上下文。 |

---

## 9. 维护说明的权威性与自包含性

> 本文件（`WorkBuddy记忆文件说明.md`）是记忆体系维护的**唯一权威说明**，自包含、可独立闭环使用。

**为何不依赖外部方案/规格文档**：
- 本体系起初由拆分实施方案、工具规格说明书、使用手册三份文档构成；但实施方案与规格说明书属于"建设期设计文档"，将随维护移交而**移除**，仅保留本使用手册。
- 本手册因此**不引用、不依赖**任何已移除的方案/规格文档；所有运维知识（架构、文件地图、约定、工具、审计流程、缺陷裁决、纠偏 SOP）均已内聚于本文件 §0~§10。
- 若将来磁盘上仍残留旧方案文档，一律视为历史快照，**不作为权威依据**；以本文件与 `memory-mgr.py` 实际行为为裁决基准。

**子记忆文件动态约束（演进现实）**：
- 子记忆文件 = 目录 `<<子记忆文件目录绝对路径>>\` 内、文件名以 `Memory-` 开头的 `.md` 文件。
- **数量动态增减**：体系扩展会新增子文件、整理会删除子文件，这是常态；切勿假设固定 5 个。
- **文件随时修改**：主文件 `MEMORY.md` 与任一子文件均可能被随时编辑。
- **工具须动态发现**：`memory-mgr.py` 用 `Memory-` 前缀扫描目录动态获得子文件集；任何硬编码文件名清单的工具（见 D-6）在增减场景下必然失效。

---

## 10. 记忆文件格式强制要求

> **适用范围（强制）**：修改、维护**主记忆文件 `MEMORY.md` 与任一子记忆文件 `Memory-*.md`** 时必须遵循；本手册自身的内容组织同样适用。违反任一条即判定为格式不合规，按 §7 纠偏。
> **本节定位**：本节是"记忆文件格式强制要求"在本文件内的**唯一权威定义处**（依第 7 条单一事源），他处只能引用本节条目编号（如"依 §10 第 4 条"），**不得重述**。

| # | 要求 | 判定标准（可执行） |
|---|------|------------------|
| 1 | **格式保真与标题适配** | 保持目标文件 Markdown / TXT 形态与整体风格不变。**当且仅当**原标题与其所辖内容不匹配时，须改写该标题使之与内容严格匹配、适用；内容与标题已匹配则标题**保持原样**。仅调整标题、内容排序与引用关系，不改变文件基本格式。 |
| 2 | **层级递进** | 内容须分多级标题（`#`→`##`→`###`→`####`），父包子、子细化父；**不得平铺**，**不得靠缩进伪装层级**。 |
| 3 | **层级标记（标准 Markdown 语法）** | 一律用 `#` / `##` / `###` / `####` 标准标题语法划分层级；**禁止**用缩进、空行、手动编号等非标题手段伪装层级结构。 |
| 4 | **序号连续唯一** | 各层级序号**在本文件内连续且唯一**（子记忆文件用 `## N-M` / `### N-M-X` / `#### N-M-X-Z`，规范见 §2.3；N = 文件编号，**不跨文件连续**；主文件用其自身编号体系）；**不得**重号、跳号、缺号。 |
| 5 | **强相关相邻与线性排序** | 同类、同流程、因果链上的章节，须按**前后工序顺序紧挨排列**并实现线性排序；禁止循环排序。**唯一例外**：工序本身即为循环迭代（如「代码审计 ↔ BUG 修复」）时允许循环排序，且须在该处**显式标注「必要循环」**。 |
| 6 | **常用前置** | 高频内容、工作流置于文件**靠前位置**（文首设速查索引），实现高效检索与调用。 |
| 7 | **单一事源** | 任一事实源 / 事项 / 要求 / 流程 / 操作，**仅在一处权威定义**；他处只能引用，**不得重述**。 |
| 8 | **无重复矛盾** | **不得**出现重复或相互矛盾的叙述。 |
| 9 | **无歧义 + 具名禁用词** | 措辞唯一可执行，**不得**有歧义或多解；凡需条件判断处须给出明确的**是非标准或阈值**，**不得**用留有余地的说法替代。**具名禁用词（在强制条款中出现即违规）**：视情况、一般、通常、酌情、尽量、尽可能、适当、必要时、原则上、建议。 |
| 10 | **引用单向且后序引用前序（导航豁免）** | 引用只能单向；只允许「**后序（晚出现）章节 引用 前序（早出现）章节**」；**禁止**前序引用后序（前向引用）。**唯一豁免**：**导航型前向引用**——速查索引表 / 映射表，以及"见 §X"类章节指引，其本质是指引读者从"适用场景"跳到"对应章节"，须确认被引章号 > 引用章号（真前向）且目标精准唯一；除该豁免外**严禁**任何反向 / 前向引用。 |
| 11 | **禁止无必要循环引用 / 调用（必要循环豁免；主↔子豁免）** | 严禁无必要的循环引用 / 调用（A→B 且 B→A）；**必要的循环引用 / 调用不在此列**——例如循环迭代的「代码审计工序」与「BUG 修复工序」因工序本身需反复迭代而互为前后，此类必要闭环允许存在，且整体豁免第 10 条的前向引用限制。**主记忆文件与子记忆文件之间的双向 / 循环引用（主→子索引、子→主回链）为本体系设计中枢，明确豁免**（见 §0.4）。除此之外**严禁**任何反向 / 前向引用。 |
| 12 | **引用精准唯一且格式一致** | 引用须精准、明确、唯一，指到可在正文**精确命中**的具体标题；**不得**"详见相关章节"式模糊指向。跨章节引用格式基准：以反引号包裹、含**完整标题前缀**的节级标题（如 `### 12.3 阶段 2 整改`）；当目标文件含速查索引表 / 映射表时，全文跨章节引用格式须与索引表**第二列逐字一致**，**禁止**裸编号（`12.3`）或"章标题 + 裸节号"形态。同节内引用具名条目（如列表项「阶段 2」）不受上述格式基准约束，但须保证该具名在全文唯一。 |
| 13 | **文件内 vs 跨文件** | 第 10、11 条的引用方向约束**仅作用于同一文件内部**；跨文件引用（子文件之间、主文件与子文件之间、文档之间）**不受方向约束**，但须满足第 12 条的精准唯一。 |
| 14 | **第 7 条作用域（本文件内）** | 同一事实源 / 事项 / 要求 / 流程 / 操作，在**本文件内**只允许一处权威定义，他处只能引用、**禁止重述**。**本节（§10）即本文件内"记忆文件格式强制要求"的唯一定义处。** |

**与工具的对应关系（依第 7 条单一事源，此处只列映射关系，判定标准见上表，不重述）**：

| 条目 | 覆盖方式 | 说明 |
|---|---|---|
| 3、6、9、10 | `validate` 的 **§10 格式强制要求附加校验**（提示级 WARN，不参与通过 / 失败判定） | 缩进伪装层级、索引表缺失、禁用词、前向引用；判读流程见 §5 步骤4 B 段 |
| 4 | `validate` **纪律3** | 序号连续唯一（按 §2.3 的 `N-M-X-Z` 体系校验，**本文件内**连续；完整判据见 §3.9 ①） |
| 7 | `validate` **纪律2** | 单一事源 |
| 8 | `validate` **纪律2** 重复检测 | 无重复矛盾 |
| 11 | `validate` **纪律5** | 循环引用；依本节第 11 条主↔子豁免，主→子索引与子→主回链不计违规 |
| 1、2、5、12 | **无工具校验，须 Agent 判读** | 判读要点见 §5 步骤4 C 段 |

---

## 11. 版本与维护记录

| 版本 | 日期 | 更新内容 |
|------|------|---------|
| **v5.0.0** | 2026-09-10 | **编号体系重构（去跨文件连续 + 去硬编码）**：① 章节编号改为 `## N-M` / `### N-M-X` / `#### N-M-X-Z`（**连字符分隔**；N = 文件编号，M / X / Z 为父级内从 1 起的局部序号；**不再跨文件连续**——文件间编号解耦）；② 新增 YAML 字段 `file_number`（方案 C，就近维护于各文件；`add` 自动分配 = 当前最大 N + 1），子文件必填字段由 5 项增至 6 项；③ **废弃** `SUB_FILE_ORDER` 硬编码清单——`_list_sub_files()` 与 `rewrite` 一律按 `file_number` 排序，实现**零硬编码文件名**（同时消解 D-16 / G6 两项缺口）；④ 新增 **§2.3 章节编号规范**（编号体系唯一权威定义源，含"分隔符必须用连字符"的 GFM 论证）；⑤ 新增 **§3.9 编号完整性保障制度**（10 项判据 + 4 条纪律 + 标准流程）；⑥ §0 新增纪律 0.12（编号只由工具生成）、0.13（编号完整性必须自检）；⑦ `check` 新增 **维度11 编号完整性**（`file_number` 存在 / 唯一 / 合法、章前缀一致、M / X / Z 连续、层级从属、格式合法）与 **维度12 清单一致性**（主文件引用 ↔ 目录实体双向 diff），维度总数 10 → **12**；⑧ `memory-mgr.py` 新增 `number --init` 命令；⑨ 修复 **D-13**（`strip_heading_number` 正则不认连字符，本次重构最高危缺口）、**D-14**（`rewrite` 不处理 H4）、**D-15**（锚点同步漏 H4）；⑩ 删除全部「第X篇」篇标题（原 7 处，并连带消除 4 处"第五篇 / 第六篇"错引）；⑪ §1 / §2.1 去硬编码章节范围，§5 审计流程、§6 缺陷日志、§7.5 / §7.6 SOP、§8 红线（新增「编号禁手工改」「编号分隔符禁用点号」）、§10 第 4 条与工具映射表同步；⑫ 手册内引用子文件旧章节号处改**语义引用**（去编号，免疫后续重编号）；⑬ **全量迁移已闭环**（阶段 1~6 全程执行）：5 个子文件 `file_number` = 1~5、本手册 = 0；166 个标题重编号（H2 29 / H3 125 / H4 12）；**108 处链接显示文本同步**（原锚点同步只改 URL 锚点、不改显示文本，属盲区，本次一并修复）；锚点可跳转性 **114/114 = 100%**；`check` + `断链检测.py` + `validate` 三项全绿；与迁移前快照逐行对比**零数据丢失**；⑭ 修正 §7.4 内部矛盾（原文建议手工编辑主索引区，违反 §3 约定 6「禁止手工编辑」→ 改为跑 `index` 重建）；§2.2 生成链路图补注 `file_number` 不参与路由卡生成。 |
| v4.7.0 | 2026-09-10 | **四要素工具化：彻底消除手册与主文件的人工同步成本**：① 四要素（定位 / 角色 / 主题 / 收录 in / 不收录 out）**下沉为各子文件 YAML 的 5 个字段**（`positioning` / `role` / `theme` / `scope_in` / `scope_out`），成为**唯一权威定义源**；② **§2.2 重写**为「字段规范 + 生成链路 + 边界判据」三部分，**不再列四要素明细**；③ `memory-mgr.py` 新增 `_build_route_card_block()` / `_generate_route_card()`，`index` 自动在主文件 `<!-- ROUTE_CARD_START/END -->` 区生成**写前路由卡**（并自动清理旧版手工同名区，幂等）；④ `check` 新增**维度 10 四要素完整性**（缺字段 WARN，路由卡显示 `[待补]`）；⑤ `add` 新增 `--positioning/--role/--theme/--scope-in/--scope-out` 参数，并**修复 D-12**（`--title` / `--summary` 因位置传参而空转）；⑥ 同步 §0（新增 0.11、修正 0.10 判据来源）、§3 约定 5/6、§4.1、§5 解读表、§6 D-12、§7.6、§7.7、§8。 |
| v4.6.0 | 2026-09-10 | **归属判定机制落地（回应 v4.5.0 归档判据测试的 G1~G5 缺口）**：① §2 升级为「文件地图与子记忆文件定义」——新增 **§2.2 子记忆文件定位与收录范围（归属判定的唯一权威定义）**，为 5 个子文件逐一给出 定位 / 角色 / 主题 / 收录(in-scope) / 不收录(out-of-scope)，并裁决"能力台账 vs 作业流程"这一易混边界；② §3 新增约定 8「归属判定前置」，落地 **§3.8 归属判定规则（新信息入库决策树）**——4 步流程（预处理 → 匹配子文件 → 定位章节 → 新建评估）+ 新建三门槛 + 新建必先定义四要素 + **第 4 步暂停与询问条款**（5 类触发条件 + 输出四要件：事由 / 解释 / 候选方案 / 影响面）；③ §0 新增导航条目 0.10；④ §7.6 增加**决策前置**与**素材规范化三步**，并标注 `remove --dry-run` 须配 `--force`；⑤ §6 新增 D-10 / D-11（源自 v4.5.0 测试的 F-7 / F-8）；⑥ 修复 §5「check 输出解读表」中与 D-1 / D-2「已修复」事实矛盾的过时条目，并补维度 9 解读。 |
| v4.5.0 | 2026-09-10 | **§10/§11 整体交换 + 条目落地为工具能力**：① 原 §10（版本与维护记录）与 §11（格式强制要求）物理交换，现 **§10=记忆文件格式强制要求、§11=版本与维护记录**（含 11.1 维护者须知）；② §5 步骤4 由"人工核对清单"改为"**Agent 核对清单**"，分 A 基础一致性 / B §10 可自动化 / C §10 须判读三段，纳入第 1、2、3、5、6、9、10、12 条；③ **消除编号冲突**——`validate` 原有"纪律6"指「YAML 必填字段」，故新增校验定名为 **「§10 格式强制要求附加校验」**（提示级，不参与通过判定），覆盖第 3（缩进伪装）、6（索引表缺失）、9（禁用词）、10（前向引用）条；④ `memory-mgr.py` 落地 C-1~C-6（`check` 维度9 回链存在性、`add` 自动补 tags/回链/剥冗余 H1、`sync` 输出忠实、`validate` 附加校验）；⑤ `断链检测.py` 落地 P-1 越界引用 / P-2 回链存在性 / P-3 无锚点链接；⑥ §0.9、§4 工具总览、§6 D-6/D-8 同步。 |
| v4.4.0 | 2026-09-10 | **新增「记忆文件格式强制要求」14 条（今 §10）**（表格化 + 可执行判定标准），并在 §0 必读纪律新增导航条目 0.9；修正 §4.2 断链检测.py 局限第 2 条（原"硬编码 5 个文件"已与 §6 D-6 修复事实矛盾）与第 3 条；§3.6 / §4.1 明确"主索引只收子记忆文件条目"属**设计需要**并给出三条推论（含"不得依赖主索引承载本手册入口，入口须固化于子记忆文件 `## 29` 或 SOUL.md 硬编码路径"）。 |
| v4.3.0 | 2026-09-10 | **越界引用纠偏 + 补工具盲区规则**：删除主索引中指向非子记忆文件的越界引用行（该 npm 规则本体系已有权威单一事源 `Memory-全局禁令与环境约束.md` 的 `### 3.4 Node.js / npm / npx 全局安装规范`，故不迁移、直接删除）；修正主索引入口文件章节标题过时（`## 28` 实为 DeusData 本地代码知识图谱、`## 29` 实为 memory-mgr.py 维护工具，原写"mimo.code协同"已无对应章节）。新增 §5 步骤4「越界引用检查（工具盲区，必查）」及扫描命令；新增 §6 D-8（check 无法检测越界引用）、D-9（章节标题文字过时亦属盲区）。全体系 `## 1`~`## 29` 已逐条核验与主索引一致。 |
| v4.2.0 | 2026-09-09 | **执行工具修复**：D-1（维度1 改 `filename.startswith('Memory-')` 判定）已消误报；D-2（循环检测排除 MEMORY.md 中枢节点）已消误报；D-5（`内容完整性核验.py` 加缺失备份守卫、动态发现子文件）已防崩溃；D-6（`断链检测.py` 改动态扫描 `Memory-*.md`）已随增减适配。重跑 `check`/`validate`/`断链检测.py` 均通过。§6 已标"已修复/已加固"，§8 红线同步更新。 |
| v4.1.0 | 2026-09-09 | 修正为自包含权威手册：§9 去除对将被移除的方案/规格文档依赖；§1/§9 明确子记忆文件动态增减约束（目录 `Memory-Data`、文件名 `Memory-` 开头）；§6 D-1 补"与增减无关、须动态发现"，D-6 强化"硬编码清单在增减场景必然失效"；新增「维护者须知 / 演进约定」（原 §10.1，今 §11.1） |
| v4.0.0 | 2026-09-09 | 全面重写：模块化/结构化/纪律化；补"已知缺陷与误报清单(§6)"与"纠偏手册(§7)"；纳入真实缺陷 D-3/D-4 修复说明；明确工具 BUG（D-1/D-2/D-7）与失效工具（D-5）处置 |
| v3.1.4 | 2026-08-28 | 旧版使用说明（已被 v4.0.0 取代） |

**配套真实纠偏（本次已执行）**：
- `Memory-入口能力与图谱.md` L94：README 死链 → 改指向本说明文件（D-3）。
- `MEMORY.md` 主索引 L93：`## 30` 失效引用 → 改为 `## 29`（D-4）。

### 11.1 维护者须知 / 演进约定（确定的下一步）

1. **本手册即唯一权威**：不依赖、不引用任何已移除的拆分实施方案 / 工具规格说明书；运维知识全部内聚于本文件 §0~§11。
2. **子记忆文件动态增减是常态**：新增/删除 `Memory-*.md` 后，以 `memory-mgr.py`（动态发现）重新 `check` / `validate` / `index` 即可，切勿假设固定数量。
3. **工具 BUG 以 §6 裁决为准**：D-1/D-2 已在 v4.2.0 修复；C-1~C-6（`memory-mgr.py`）与 P-1~P-3（`断链检测.py`）已在 v4.5.0 实施；**四要素工具化（写前路由卡 / 维度10 / `add` 四要素参数）与 D-12 修复已在 v4.7.0 实施**。本手册**不依赖**代码修正即可自洽运行——工具缺席或报错时按 §5/§7 手工闭环。
4. **工具定位**：`断链检测.py` 已于 v4.2.0 改动态发现、v4.5.0 增 P-1/P-2/P-3，作为链接级补充手段可依赖（文件存在性与全量断链仍以 `memory-mgr.py` 为准）。
5. **主/子文件随时修改**：每次实质维护后跑 §5 审计流程，发现 D-3/D-4 类"断链/失效引用"按 §7 即时纠偏。


**维护者**：WorkBuddy ｜ **最后更新**：2026-09-10
