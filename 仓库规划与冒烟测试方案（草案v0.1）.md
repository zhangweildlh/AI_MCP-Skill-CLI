# AI_MCP-Skill-CLI 仓库规划 与 Skill 冒烟测试方案（草案 v0.1）

> 说明：本方案基于对你目录 `D:\Documents\AI_MCP-Skill-CLI` 的**全量扫描 + 逐文件阅读 + 内容分析**得出，是草案。关键决策点见文末「待讨论事项」，你确认后我再据此执行（创建/转化仓库、落地冒烟测试）。
> 本文中带括号的英文单词为 GitHub 标准术语，便于你后续对照官方文档。

---

## 一、全量扫描与分析结论

### 1.1 资产清单（= 计划进仓库的"真技能"）

| 类型 | 名称 | 文件数 | 说明 |
|---|---|---|---|
| 单文件技能 | `Skill-元技能，Skill创建助手.md` | 1 | name: skill-creator，通用模板，无隐私 |
| 单文件技能 | `Skill-元技能，Skill校验器.md` | 1 | name: skill-checker，通用模板，无隐私 |
| 单文件技能 | `Skill-GitHub仓库同步助手.md` | 1 | name: github-repo-sync，**硬编码**本机路径 `D:\Documents\AI_Work_Temp` |
| 单文件技能 | `Skill-滴答清单智能任务解析创建器.md` | 1 | name: ticktick，**深度个性化**：含家人真实姓名、工作项目名、地点 |
| 单文件技能 | `Skill-多文件分析+知识图谱构建.md` | 1 | name: multi-file-analysis，通用，示例含"中建三局"等公开示例 |
| 单文件技能 | `Skill-扫描Skill技能生成xml技能标签.md` | 1 | name: find-skill-to-xml，**硬编码** `D:\Documents\AI_MCP-Skill-CLI` |
| 单文件技能 | `Skill-推广文章撰写.md` | 1 | name: promotion-writer，通用，无隐私 |
| 目录技能 | `anysearch-skill/` | 9 | 统一搜索 CLI；`.env` 含**真实 API Key**；`runtime.conf` 含本机路径（已被自身 `.gitignore` 忽略） |
| 目录技能 | `github-personal-manager/` | 3 | 个人 GitHub 操作；**硬编码**用户名 `zhangweildlh`、邮箱、git/gh 路径、真实 Fork 列表 |
| 目录技能 | `ref-material-writing/` | 35 | 参考资料驱动写作；**大量硬编码**本机路径；含 `.workbuddy/` 私有日志 |
| 目录技能 | `web-search/` | 9 | 联网搜索；`.env` 含**真实 API Key**；**硬编码**本机路径 |
| 目录技能 | `tender-review-kit/` | 39 | 招标文件审标；干净，但 `SKILL-zw.md` 含本机路径与另一私有项目 `OfficeCLI` 路径 |
| 根模块 | `@文件读写通用模块.md` | 1 | **硬编码** `D:\Documents\Downloads`、本机 venv 路径 |
| 根模块 | `@Skill 调用子 Skill 和外部工具的范式.md` | 1 | 通用机制说明，无隐私 |
| 根模块 | `Deepseek++系统提示词-2.md` | 1 | **引用** `Memory-Data/长期记忆_Deepseek++.md`（你的私有记忆库） |

### 1.2 高危隐私 / 硬编码风险清单

| 级别 | 位置 | 内容 | 处置建议 |
|---|---|---|---|
| 🔴 致命 | `we-mp-rss/data/server.log` | 真实 `CODEBUDDY_GATEWAY_PASSWORD=...` 网关密码明文 | 必须物理删除该日志 + 轮换网关密码；**绝不入库** |
| 🔴 致命 | `anysearch-skill/.env`、`web-search/.env` | 真实可用 `ANYSEARCH_API_KEY=as_sk_...` | 立即到 anysearch.com 吊销/轮换；改为 `.env.example` 占位；加入 `.gitignore` |
| 🟠 高 | `Skill-滴答清单…md` | 家人真实姓名（何晓勤/唐淼/小芝麻/张梵净）、工作项目（成都北站总包、中铁九天总包）、地点 | 私有仓库可保留；若日后转公开必须脱敏 |
| 🟠 高 | `github-personal-manager/` | 用户名 `zhangweildlh`、邮箱 `157947621@qq.com`、git/gh 绝对路径、4 个真实 Fork URL | 路径与身份需参数化/占位 |
| 🟠 高 | 多个技能/模块 | 本机绝对路径 `D:\Tools\Assembly\...`、`D:\Documents\AI_MCP-Skill-CLI`、`D:\Documents\Downloads`、`D:\Documents\AI_Work_Temp`（共约 30+ 处） | 改为相对路径或环境变量占位 |
| 🟡 中 | `tender-review-kit/SKILL-zw.md` | 本机路径 + 另一私有项目 `OfficeCLI` 绝对路径 | 不入库（删或 gitignore） |
| 🟡 中 | `Deepseek++系统提示词-2.md` | 引用私有记忆库 `Memory-Data/` | 记忆库不入库即可，引用路径无害 |

### 1.3 必须排除 / 不入库的内容

- `Memory-Data/`（18 个 `.bin`/`.pickle`/`.sqlite3`）—— 你的**私有记忆数据库**，绝不入库。
- `Syncfolders_Database_db` —— 同步工具数据库，不入库。
- 3 个 `Skill-…_废弃(…).7z` 压缩包 —— 历史废弃物，不入库（建议本地删除）。
- `we-mp-rss/`（11,809 文件，94% 是 `.venv`+`node_modules`，且含真实网关密码）—— **强烈建议排除在本仓库之外**（详见待讨论事项）。
- `mimo_mcp.py`（根目录可执行脚本）—— 性质待你确认；若含密钥/私有逻辑，不入库。
- 各 Skill 运行时生成的临时文件（如 `ref-material-writing/assets/_流水线状态.md`、`.workbuddy/`）—— 用 `.gitignore` 排除。

---

## 二、仓库创建 / 转化方案（草案）

### 2.1 总原则

1. **私有仓库(private repository)**：你已明确要私有，云端备份但不公开。
2. **原地建库**：本地目录 `D:\Documents\AI_MCP-Skill-CLI` 即仓库根，不移动任何文件，直接 `git init`。
3. **遵循你既有 GitHub 规则**：`main` 分支禁止强制推送(push)/删除；修改走**功能分支(feat)** → 开**拉取请求(PR)** → 合并(merge) 回 `main`；云端走**你的远端仓库(origin)**。
4. **远端创建用 `gh` CLI**（已连接 GitHub）：一条命令建私有仓库，避免网页手点。

### 2.2 分步流程（"电脑菜鸟"人话版）

**第 1 步 · 预处理（脱敏 + 清理）**
- 把 `.env` 里的真实 Key 换成占位模板，并补 `.env.example`；
- 确认 `Memory-Data/`、`we-mp-rss/data/`、`.7z`、临时文件已被忽略或移出；
- （可选，按你的决策）把本机绝对路径改为相对路径/占位符。

**第 2 步 · 写 `.gitignore`**（仓库根）
统一忽略：`Memory-Data/`、`Syncfolders_Database_db`、`*.7z`、`*.zip`、`__pycache__/`、`*.pyc`、`.venv/`、`node_modules/`、`*.log`、`.env`、`_流水线状态.md`、`.workbuddy/`、`we-mp-rss/`（若决定排除）。
各技能目录已有的 `.gitignore`（`anysearch-skill`、`tender-review-kit`）保留并补全。

**第 3 步 · 建私有远端仓库**
`gh repo create AI_MCP-Skill-CLI --private --description "..." `（在你账号 `zhangweildlh` 下）。

**第 4 步 · 本地初始化 + 首次提交(commit)**
`git init` → `git add .`（配合 `.gitignore`，不会带进密钥/记忆库）→ `git commit -m "chore: 初始化 AI_MCP-Skill-CLI 技能仓库"`。

**第 5 步 · 推送(push) 到 你的远端仓库(origin)**
`git branch -M main` → `git remote add origin <仓库URL>` → `git push -u origin main`。

**第 6 步 · 给 `main` 开分支保护(branch protection)**
在 GitHub 设置里对 `main` 开启"禁止强制推送、禁止删除"，与你其他仓库一致（双保险）。

**第 7 步 · 日常迭代工作流（你以后每天怎么用）**
1. 发现 BUG 或新需求 → 开一个**功能分支(feat)**：`git checkout -b feat/fix-xxx`；
2. 改对应 Skill 文件；
3. 跑一遍冒烟测试（见第三节）确认没改坏；
4. 提交(commit) → 推送(push) 该分支到 origin；
5. 开**拉取请求(PR)**，自己审阅后合并(merge) 回 `main`；
6. 合并后 `main` 自动与云端同步（云端备份完成）。

### 2.3 给"电脑菜鸟"的概念人话

- **仓库(repository)**：一个带"时光机"的文件夹，每次改动都能回到过去任意版本。
- **提交(commit)**：给当前文件夹拍一张"快照"并写一句说明。
- **分支(branch)**：从主线 `main` 分出来的一条"草稿线"，你在上面随便改，改坏了不影响主线。
- **推送(push)**：把你本机的快照传到**你的远端仓库(origin)**（GitHub 云端），即"云端备份"。
- **拉取请求(PR)**：向自己提议"把这条草稿线合回主线吧"，合之前能最后检查一遍。
- **分支保护(branch protection)**：给 `main` 上把锁，防止误删/误覆盖——即便你点错也不会丢东西。

---

## 三、Skill 技能冒烟测试方案（草案）

### 3.1 目标

在"每次提交(commit)/合并(merge)前"用最低成本验证：**每个 Skill 结构合法、能被正确加载、依赖可用、外部接口可达、触发符合预期**——而不是等上线用到时才发现崩了。

### 3.2 五层冒烟测试体系

**Tier 0 · 入库前门禁（防泄密 + 防误提交）**
- 密钥扫描：提交前扫描暂存区，禁止 `ANYSEARCH_API_KEY=`、`CODEBUDDY_GATEWAY_PASSWORD=`、明文 `sk-`/`token:` 等进入版本库（可用 `gitleaks` 或简易正则脚本）。
- `.gitignore` 校验：确认 `Memory-Data/`、`*.env`、`we-mp-rss/`（如排除）确处于 ignored 状态。

**Tier 1 · 结构冒烟（纯静态，秒级）**
- 解析每个 `SKILL.md` / `Skill-*.md` 的 YAML 前置元数据，确认含 `name`+`description` 且非空；
- 校验 `name` 与父目录名一致（目录技能）；
- 检查正文中引用的 `references/`、`scripts/`、`assets/` 文件**真实存在**（无断链）；
- 统计文件清单，确认无 `.env`/密钥文件混入。

**Tier 2 · 合规冒烟（复用你已有的 skill-checker）**
- 对每个 Skill 跑一遍 `skill-checker`（11 维度校验），输出报告；
- 重点关注：frontmatter 完整、description 五要素、无断链、参数自洽。
- 可脚本化：用 `find-skill-to-xml` 的思路批量提取 name/description，再逐个喂给校验器。

**Tier 3 · 运行冒烟（依赖 + 脚本 + 接口）**
- 环境探测：确认 `git`/`gh`/`uv`/`python`/`node` 在 PATH（参考 `@Skill 调用子 Skill…` 的 shell_status 范式）；
- 脚本自检：
  - `anysearch-skill`：`uv run --project <venv> python scripts/anysearch_cli.py --help`；
  - `web-search`：同上 CLI `--help`；
  - `tender-review-kit`：跑自带测试 `python -m pytest tests/`（含 `test_smoke.py`）；
- 接口可达：对 `https://api.anysearch.com/mcp` 发一次最小 JSON-RPC 探活（匿名或带 Key）。

**Tier 4 · 触发冒烟（行为验证）**
- 取每个 Skill 的"触发条件"样例输入，确认预期 Skill 被激活、非预期 Skill 不被误触发；
- 例：`ticktick` 输入"提醒我明天交报告"应触发；`promotion-writer` 输入"写篇推广文案"应触发。

**Tier 5 · 持续集成(CI)（GitHub Actions，云端自动跑）**
- 在 `.github/workflows/smoke.yml` 配置：每次 push 到 `main` 或开 PR 时，自动跑 Tier 0–3（密钥扫描 + 结构 + 合规 + 脚本/接口）；
- 矩阵：按技能目录拆分 job，任一失败则 PR 标红、阻断合并(merge)；
- 你本机"禁止本地编译"，CI 恰好在云端完成校验，契合你的规则。

### 3.3 可立即落地的检查命令（示例）

```powershell
# Tier1 结构扫描（PowerShell，仅扫描一级子目录与根，不递归二级）
# 用 find-skill-to-xml 技能即可产出合规清单 + 重复 name 报告

# Tier3 脚本探活
uv run --project D:/Tools/Assembly/python/myenv python D:/Documents/AI_MCP-Skill-CLI/anysearch-skill/scripts/anysearch_cli.py --help
cd D:/Documents/AI_MCP-Skill-CLI/tender-review-kit; python -m pytest tests/ -q
```

### 3.4 节奏建议

- **每次本地改完**：手跑 Tier 0 + Tier 1（快，30 秒内）。
- **开 PR 前**：跑 Tier 2 + Tier 3。
- **push / PR**：由云端 CI（Tier 5）兜底，无需你手动。

---

## 四、待与你讨论的决策点

> 以下 4 点直接决定方案形态，请逐条回复你的倾向，我据此定稿并执行。

1. **`we-mp-rss`（万级文件、含真实网关密码）怎么处理？**
   - A. 完全排除在本仓库外（推荐，最安全、仓库最干净）；
   - B. 作为独立仓库另建；
   - C. 强行纳入本仓库（需先补全 `.gitignore` 忽略 `.venv`/`node_modules`/`data/`）。

2. **硬编码本机路径 + 个人家人信息，提交前要不要做"脱敏/参数化"？**
   - A. 只处理"真正机密"（密钥、网关密码、记忆库），路径与家人信息保持原样（私有仓库自用最省事，但换机/CI 会失效）；
   - B. 全部参数化脱敏（干净、可移植，但工作量较大，需逐文件改约 30+ 处）。

3. **发现两处真实密钥（AnySearch Key、WorkBuddy 网关密码），是否同意我在入库前把它们从文件中剔除并换成占位符？**（网关密码还需你到 WorkBuddy 设置里轮换；AnySearch Key 需你到 anysearch.com 吊销）

4. **方案确认后，是否现在就由我（通过已连接的 `gh`）帮你：建私有仓库 → 初始化 → 首次提交(commit) → 推送(push) 到 origin，并给 `main` 开分支保护？** 还是你只想先把方案定下来、暂不执行？

---

*草案生成时间：2026-07-17 ｜ 依据：全量扫描 + 10 份根文件精读 + 6 个目录探查代理报告*
