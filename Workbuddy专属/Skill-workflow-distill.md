---
name: workflow-distill
description: 通用工作流蒸馏元技能（与任何具体产品/仓库无关）。从跨项目历史中发现重复的人工工作流，把高置信度的候选打包成可复用的通用 Skill。关键词：工作流蒸馏、流程沉淀、Skill 打包、跨项目复用。当用户明确要求"蒸馏工作流""distill""打包重复流程""把这类任务存成 skill"时触发此技能；当用户说"把这类任务存成可复用 skill"时优先使用。适用于任何项目的流程沉淀与跨会话复用；不适用于单次任务执行、或写死项目特定步骤的场景。
license: Apache-2.0
metadata:
  author: workbuddy-agent
  version: 1.2.0
  agent_created: true
compatibility: 需具备 conversation_search（跨历史语义检索）、Glob（盘点现有 skill）、Read/Write/Edit 文件工具、SkillManage 或等价写文件能力；记忆与产出路径一律运行时解析，不得写死本机路径（**唯一例外**：见 `## 3. 可选输入源耦合`，固定产物目录 `D:\Documents\AI_MCP-Skill-CLI\Memory-Data\task-methodology\` 为用户授权硬编码接入点，仅作读取不写回）。
---

# Workflow Distill — 通用工作流蒸馏

## 0. 速查索引

| 适用场景 | 对应章节 |
|---|---|
| 明确本技能做什么、不做什么 | `## 1. 角色与目标` |
| 判断当前是否应启动蒸馏 | `## 2. 何时使用（触发）` |
| 接入固定目录的过程态经验作为发现源 | `## 3. 可选输入源耦合` |
| 执行盘点 → 发现 → 选型 → 写回 → 摘要主流程 | `## 4. 蒸馏阶段` |
| 创建 / 晋升前逐条打勾放行 | `## 5. 产出质量门禁（采 B4）` |
| 判定候选处于哪一成熟度层级 | `## 6. 成熟度分层（采 B2，可选）` |
| 确认某动作是否被负向护栏禁止 | `## 7. Never 清单（负向护栏，采 A2 / B3 / B4）` |
| 整理目标文件时的强制要求、整改流程与验收 | `## 8. 规范与合规要求` |
| 把本技能移植到其他 Agent | `## 9. Porting Notes（可移植到其他 Agent）` |

## 1. 角色与目标

你是一个**与任何具体产品 / 仓库无关**的工作流蒸馏器。你只做一件事：从反复的同类工作里，提炼出**通用、可复用、零项目特定**的 skill。你不执行具体任务，只盘点、发现、打包。

## 2. 何时使用（触发）

- 用户明确要求："蒸馏工作流" / "distill" / "打包重复流程" / "把这类任务存成 skill"。
- 定时自动化调用：须为触发式，不得常驻。
- **绝不**自作主张自动触发，除非用户本会话已显式开启对应自动化。

## 3. 可选输入源耦合

> **用户授权例外**：本节为与 `task-methodology-consolidation` 技能衔接的**可选**输入源，系用户于 2026-08-09 显式授权之**唯一**硬编码例外；本技能其余路径仍严格遵循「不得写死本机路径」原则（一律运行时解析 / 占位符）。

激活本技能（周期自动化 / 用户显式触发）做工作流蒸馏时，额外将下列固定目录的产物作为候选发现源：

- **固定目录（用户指定硬编码点）**：`D:\Documents\AI_MCP-Skill-CLI\Memory-Data\task-methodology\`
- **匹配文件**：仅 `p2-` 前缀的过程态经验 MD（通用可移植类；`p1-` 项目专项产物不进跨项目下游，其去处与用途由用户自行决定）。命名规则见 `task-methodology-consolidation` 第八节。
- **纳入范围**：`p2-` 文件跨项目通用，全量纳入候选评估，不按文件头 `关联路径` 字段过滤当前 workspace（P2 本身无项目边界）。
- **容错**：固定目录不存在或无 `p2-` 文件时静默跳过。
- **脱敏（下游产出脱敏，非逆流写源）**：本技能在加工与产出阶段对**自身生成的 Skill** 执行脱敏，确保产物不含明文密钥 / 凭证；本操作仅针对下游产出，不回流修改源文件。
- **不写回**：本技能只读取评估，不修改、不删除该目录文件。
- **来源定位**：该目录产物仅为候选发现源之一、非唯一；本技能另有 `conversation_search`、Glob skills、现有 skill 盘点等多元输入源。

## 4. 蒸馏阶段

### 4.1 Phase 1 — Inventory（盘点，复用优先）

- Glob `~/.workbuddy/skills/**/SKILL.md`（`~` 运行时解析为当前用户主目录）。
- 读取每个现有 skill 的 `name` + `description`，记录其已覆盖的范围。
- 候选若已被现有 skill 覆盖 ⇒ 判定为"扩展已有"或"跳过"，**不新建近重复**。

### 4.2 Phase 2 — Discover（发现重复，确定性去重）

- 用 `conversation_search` 多组查询找"被反复要求的同类任务"（含中文等价词）。
- **附加源（用户授权例外）**：同时扫描 `## 3. 可选输入源耦合` 所列固定目录的 `p2-` 文件，`p2-` 跨项目通用、全量纳入候选评估（不按 `关联路径` 过滤当前 workspace，只读不写回）。
- **用 Pattern-Key taxonomy 做确定性 recurrence 计数（替代 SQL `GROUP BY count`，采 B3 / B4）：**
  - 格式 `area.symptom`，**两级、小写、连字符**；symptom 要通用到能复现，**键中不得含文件名 / 版本 / 主机名**。
  - 例：`deps.module-not-found`、`build.type-error`、`config.missing-env`、`vcs.merge-conflict`、`shell.command-not-found`。
  - 先按 `Pattern-Key` 去重（命中则 bump `Recurrence-Count`、更新 `Last-Seen`，不新建）；模糊关键词仅作回退。
- 判定候选：Recurrence **≥2** 即入候选；**≥3 且跨 ≥2 个相异任务且落在 30 天内** 才升 `verified`（成熟度门禁，采 B2 / B3 / B4）。
- **「相异任务数」优先于「频次」**：同一件事反复迭代多轮会把 Recurrence 刷高，但那只证明这件事难，不证明流程通用。**跨任务数 = 1 ⇒ 一律不晋升**，无论频次多少。
- **跨轮次台账（周期自动化必备）**：历史检索接口的返回窗口仅覆盖**近数日**，单轮无法自然攒够"30 天内 ≥3 次"。故每轮须把 Pattern-Key 计数写入**持久化台账**（自动化记忆文件或等价位置），下轮先读台账再累加 `Recurrence-Count`、更新 `Last-Seen`；否则每轮从零计数，永远够不到晋升门槛。
- 低频弱信号 / 仅一次 ⇒ 标 `[unverified]`，仅登记台账，不打包。

### 4.3 Phase 3 — Choose Smallest Form（选最小形态）

对每个高置信候选，取最小合适形态：

- **Skill（默认）**：可复用工作流 / 手册。写成**全局用户级、零硬编码**的 `SKILL.md`。
- **Subagent**：有界专家角色 / 适合委派的研究任务。
- **Command**：参数化提示词（无用户侧命令注册表时，降级为 skill）。
- **Extend existing**：编辑已有 skill / 子代理，而非新增近重复。
- **Skip**：一次性、模糊、敏感、或证据不足的 ⇒ 跳过并说明。
- **门禁达标 ≠ 应该创建**：候选即便满足全部成熟度门禁，若其内容已被**现有 skill、用户权威记忆章节、或两者的组合**覆盖，仍须 Skip 并说明。理由：新建会制造第二份并存叙述，日后必然漂移。判断顺序 = 先查现有 skill，再查权威记忆章节，最后查二者组合。

### 4.4 Phase 4 — Gate & Write（回放门禁 + 写回，采 B5）

写回 / 创建**前必跑回放门禁**，对每条候选校验：

- 无跨项目泄漏（不带入他项目特定 / 隐私 / 定制信息）∧
- 无回归（不推翻既有已验证 skill / 记忆）∧
- **无硬编码**（无任何项目特定写死值、路径、项目名）⇒ 才允许写；否则**拒绝该条**并说明（"拒绝自己的不安全修改"）。

产出（`SkillManage` 或等价写文件，写入 `~/.workbuddy/skills/<name>/SKILL.md`）：

- 名称：小写连字符（如 `pytest-coverage-check`），须与父目录同名。
- frontmatter：必须含 `name`、`description`、`license`、`metadata`（含 `agent_created: true`）；可选 `compatibility`、`allowed-tools`。`description` 须为**单行字符串**、含五要素（核心功能、业务关键词、触发条件两种以上表述、适用场景、不适用场景）、长度 1–1024 字符。
- 正文结构：**强制**含「角色与目标」「工作流 / 分步指令」「输出格式约束」「示例（≥3，含正常 / 边界 / 异常）」「边界与限制」；「规则与知识库」为可选项。正文仅 1 个一级标题，标题层级逐级递减、无越级，禁用 HTML / Mermaid / KaTeX，指令用第二人称「你」。
- **Skill 规范合规（强制闸门）**：产出物须通过「Skill 创建与校验元技能（skill-forge）」的「校验标准 · 11 维度」全量校验，且 `❌ 致命` 与 `⚠️ 严重` 项清零、最终判定 `✅ 校验通过`，方可写回；未通过则循环修正（上限 5 次）。
- **跨项目通用（强制）**：逻辑零项目特定——路径 / 项目名 / 客户名一律运行时解析或占位符；不得出现本机绝对路径、具体仓库名、客户名；所有可变内容经参数传入。

### 4.5 Phase 5 — Summary（人类可读摘要）

- 短名单：候选、证据（session / Pattern-Key 命中）、频率 / 置信度、推荐形态；
- Created / Extended（路径 + 一行用途；若无可打包项，明确说"Created nothing — no repeated workflow worth packaging"，此为完整成功结果）；
- Skipped（刻意未打包者及理由）；
- Needs more evidence（有潜力但证据不足的候选）。

## 5. 产出质量门禁（采 B4）

创建 / 晋升前逐条核对：

- [ ] 方案已实测可用；
- [ ] `description` 脱离原始上下文仍自洽清晰；
- [ ] 代码 / 示例自包含；
- [ ] 遵循小写连字符命名，且 `name` 与父目录同名；
- [ ] 已跑通 `### 4.4 Phase 4 — Gate & Write（回放门禁 + 写回，采 B5）` 的回放门禁三项校验（无泄漏 ∧ 无回归 ∧ 无硬编码）；
- [ ] 已满足 `### 4.4 Phase 4 — Gate & Write（回放门禁 + 写回，采 B5）` 的产出要求（frontmatter、正文结构、skill-forge 强制闸门、跨项目通用）。

## 6. 成熟度分层（采 B2，可选）

- `candidate`（Tier-2 / 新生成）：默认层级，标 `[unverified]`。
- `verified`（Tier-1）：按 `### 4.2 Phase 2 — Discover（发现重复，确定性去重）` 的晋升门禁判定通过后升级。
- `core`（Tier-0）：应用 ≥50 次且成功率 >90% ⇒ 升核心（触发极少）。

## 7. Never 清单（负向护栏，采 A2 / B3 / B4）

- 绝不自动改高风险的全局配置 / 他项目文件 / 凭证。
- 绝不整段搬运他项目记忆或步骤（只取抽象通用规律）。
- 绝不创建近重复的 skill（先盘点、先扩展）。
- 绝不写死本机路径 / 项目名 / 客户名（**唯一例外**：见 `## 3. 可选输入源耦合`，固定产物目录系用户授权硬编码接入点，仅作读取不写回）。
- 无真重复时，"什么都不创建"也是合法且预期的成功结果。

## 8. 规范与合规要求

本技能在**整理 / 整合 / 巩固「记忆」**，以及**蒸馏「规则」/`SKILL`/`subagent`/`command`** 时，必须严格遵循本节各项要求。违背任一条即视为整合失败，须在验收环节纠偏；对目标文件（如 `MEMORY.md`）操作后，文件内容须完全满足本节第 1–12 条。

### 8.1 12 条强制要求（本文件内嵌）

1. 格式保真与标题适配：保持目标文件 Markdown 形态与整体风格不变；当且仅当原标题与其所辖内容不匹配时，须改写该标题使之与内容严格匹配、适用；内容与标题已匹配则标题保持原样。仅调整标题、内容排序与引用关系，不改变文件基本格式。
2. 层级递进：内容须分多级标题（#→##→###→####），父包子、子细化父，不得平铺或靠缩进伪装层级。
3. 层级标记（符合 Markdown 语法）：一律用 # ## ### #### 等标准 Markdown 标题语法划分层级；禁止用缩进、空行、手动编号等非标题手段伪装层级结构。
4. 序号连续唯一：各层级序号（第 N 章 / N.M / N.M.P）全局连续且唯一，不得重号、跳号、缺号。
5. 强相关相邻与线性排序：同类、同流程、因果链上的章节，须按前后工序顺序紧挨排列并实现线性排序；禁止循环排序，唯一例外：工序本身即为循环迭代（如「代码审计 ↔ BUG 修复」）时允许循环排序，且须在该处显式标注「必要循环」。
6. 常用前置：高频内容、工作流置于文件靠前位置（文首设速查索引），实现高效检索与调用。
7. 单一事源：任一事实源 / 事项 / 要求 / 流程 / 操作，仅在一处权威定义；他处只能引用，不得重述。
8. 无重复矛盾：不得出现重复或相互矛盾的叙述。
9. 无歧义：措辞唯一可执行，不得有歧义或多解；凡需条件判断处须给出明确的是非标准或阈值，不得用留有余地的说法替代。具名禁用词（在强制条款中出现即违规）：视情况、一般、通常、酌情、尽量、尽可能、适当、必要时、原则上、建议。
10. 引用单向且后序引用前序（导航豁免）：引用只能单向；只允许「后序（晚出现）章节 引用 前序（早出现）章节」；禁止前序引用后序（前向引用）。唯一豁免：速查索引表 / 映射表类前向引用合法——其本质是指引读者从「适用场景」跳到「对应章节」，须确认被引章号 > 引用章号（真前向）且目标精准唯一；除该豁免外仍严禁任何反向 / 前向引用。
11. 禁止无必要循环引用 / 调用（必要循环豁免）：严禁无必要的循环引用 / 调用（A→B 且 B→A）；但必要的循环引用 / 调用不在此列——例如循环迭代的「代码审计工序」与「BUG 修复工序」因工序本身需反复迭代而互为前后，此类必要闭环允许存在，且整体豁免第 10 条的前向引用限制；除此之外仍严禁任何反向 / 前向引用。
12. 引用精准唯一且格式一致：引用须精准、明确、唯一，指到可在正文精确命中的具体标题，不得「详见相关章节」式模糊指向。跨章节引用的格式基准：以反引号包裹、含完整标题前缀的节级标题（如 `### 12.3 阶段 2 整改`）；当目标文件含速查索引表 / 映射表时，全文跨章节引用格式须与索引表第二列逐字一致，禁止裸编号（12.3）或「章标题 + 裸节号」形态。同节内引用具名条目（如列表项「阶段 2」）不受上述格式基准约束，但须保证该具名在全文唯一。

作用域说明：第 10、11 条的引用方向约束仅作用于同一文件内部；跨文件引用不受方向约束，但须满足第 12 条的精准唯一。第 7 条：在同一文件内，任一事实源仅一处权威定义，他处只能引用不得重述。

### 8.2 适用范围

- 适用于本技能产出或整理的任何 Markdown 文档：含全局 / 项目级 `MEMORY.md`、规则文件、`SKILL.md` 等。
- 目标文件须由本技能整理至**完全满足** `### 8.1 12 条强制要求（本文件内嵌）` 第 1–12 条；旧有不合规处一律整改，不得原样保留。

### 8.3 本技能附加流程

对目标文件按下列阶段 0–4 顺序执行，不得跳阶：

- **阶段 0 基线快照**：读取目标文件全文，记录标题树、现有编号与全部跨章节引用，作为整改前基线。
- **阶段 1 扫描比对**：按 `### 8.1 12 条强制要求（本文件内嵌）` 第 1 条至第 12 条逐条比对，产出违规清单，每条记录「违反条号 + 所在标题 + 原文证据」。
- **阶段 2 整改**：按违规清单逐项修复——改写不匹配标题、重排层级与序号、合并重述内容、消解矛盾与歧义、修正引用方向与引用格式。整改只做合并、改写与重排，不得删除原有事实信息。
- **阶段 3 复扫**：整改后重跑阶段 1；违规清单非空则回到阶段 2，构成「阶段 1 ↔ 阶段 2」**必要循环**，复扫上限 5 轮；5 轮后仍非空则停止整改并在交付物中列出未闭合项。
- **阶段 4 交付**：输出整改前后差异摘要、剩余未闭合项与验收结论。

### 8.4 本技能附加验收项

- [ ] 目标文件已按 `### 8.1 12 条强制要求（本文件内嵌）` 第 1–12 条全量核对，违规清单为空；
- [ ] `### 8.3 本技能附加流程` 阶段 0–4 全部执行，复扫轮次 ≤5；
- [ ] 全文标题层级无越级、序号连续唯一、无悬空编号；
- [ ] 全文跨章节引用均可在正文精确命中，无断链、无前向引用（速查索引表 / 映射表除外）；
- [ ] 整改未删除原有事实信息，功能与步骤零缺失。

## 9. Porting Notes（可移植到其他 Agent）

| 概念 | WorkBuddy | Claude Code | OpenCode(MiMo) | OpenClaw |
|---|---|---|---|---|
| 现有 skill 盘点 | Glob `~/.workbuddy/skills/**` | 读 `~/.claude/skills/**` | `.mimocode/skills/**` | `~/.openclaw/skills/**` |
| 历史检索 | `conversation_search` | 读 `projects/*/memory` + 会话 | SQLite `mimocode.db` | `sessions_*` |
| 写 skill | `SkillManage` / 写文件 | 手写 `SKILL.md` | `.mimocode/skills/<name>/` | 写文件 |
| 用户级存储 | `~/.workbuddy/skills/` | `~/.claude/skills/` | 全局 skills 目录 | `~/.openclaw/skills/` |

> 移植要点：**只搬本文件的"方法论"（蒸馏阶段、Pattern-Key 去重、成熟度门禁、回放门禁、Never 清单、12 条强制要求），换数据源 / 写出适配器**；保持"零硬编码 + 不串味"不变，即可在他 Agent 复现且可复用。
