# tender-review-kit 架构纲领

> **给 AI agent 用的招标文件审标 skill(为投标人服务)。** 审的是招标方发的招标文件,服务的是要去投标的人。
> 输入招标文件 → 产出「投标核对清单」(废标项 + 评分项 + 要点),
> 帮投标人在**动手写投标文件之前**把招标方的游戏规则吃透。**产清单和事实,不下「投/不投」结论。**
>
> 本文是项目纲领——架构、原则、演进方式。**贡献前先读这一份。**

---

## 一、第一原则：判词库是核心,准确第一

这是整个项目最重要的一句话。

**判词库**(`data/keywords.json`)是几百次实战一条条攒出来的**确定性资产**——库里有这个词,程序就逐行扫,每条保证扫到,**这次和下次一模一样**。这才是"准确"的地基。

**提示词没壁垒**:开源出去,别人 5 分钟把提示词复制走;同一句"帮我找废标点",大模型这次找 20 条、下次 18 条——你永远不知道它漏没漏、没法复现、没法验证。

**所以**:
- 项目核心 = **判词库** + 让它**自己长大的"补词引擎"**(`scan_candidates`)
- 提示词只是粘合剂,负责**去噪和判断**(分清"无效投标"vs"无效数据"),是辅助、不是价值所在
- **开源的真正护城河 = 判词库本身**(代码能抄,你攒的判词库抄不走)

## 二、设计定位

- **用户**:要去投标的人(投标人)。**对象**:招标方发的招标文件。**时点**:**动手写投标文件之前**。
- **产出 = 清单 + 事实**,三层结构:废标项 + 评分项清单(80% 价值) / 要点参考(15%) / ≤5 条中性提示(5%)。
- **不做**:投/不投结论、报价策略推演、P0-P4 优先级、外部数据查询——决策是投标人自己的事,工具的本分是让他看见全部要求。
- ⭐ **「招标要求 vs 合同要求」严格分开**:废标清单 = **投标递交时**雷区;**合同条款·要点** = 中标后的约束。两类清单独立,投标团队按需查看(很多人不看合同,我们尊重这个选择)。判词库为此专设 `contract` 类,扫到的合同期判词不混进废标清单。
- **工具无关主干 + Claude 增强**:核心工作流只用「跑脚本 + 读文件 + LLM 判断」这种任何 agent 工具都有的能力,在 Claude / Codex / 国产 agent(workbuddy / 阿里)上跑;Claude / Claude Code 下额外可用 subagent 并行 + 红蓝对抗增强。

## 三、核心分工:判断交大模型,确定性/护栏交程序

| 沉进 Python(`scripts/`) | 留给大模型(读 `references/`) |
|---|---|
| 确定性(同输入同输出) | 要读懂语义("无效投标" vs "无效数据") |
| 能枚举成规则/清单/阈值 | 随文本千变万化、无法穷举 |
| 要保证不漏(护栏类) | 要靠上下文锚点判断 |
| 重复且量大(扫几千行) | — |
| 写进提示词会撑爆上下文、稀释注意力 | — |

**为什么护栏必须是程序**:大模型无法可靠地审计自己有没有偷懒压缩(本项目曾把 331 个 ▲ 压成 12 个)。

## 四、六层能力栈

```
编排层  SKILL.md ········· 触发 + 端到端工作流(工具无关主干 + Claude 增强)
智能层  subagent / 线性专项 ·· 读 references 做语义判断(去噪 / 读条款 / 整理)
知识层  references/ ········ 审标对照总清单 + 各专项工作指南
数据层  data/ ············· ⭐ 判词库【命根子,可 PR】;候选词是 workspace 逐项目产物(含原文,不入库)
程序层  scripts/ ·········· 取数 / 撒网 / 补词 / 查漏 / 校验 / 跨文件 / 出 Excel
反哺层  tests/(+ 规划中的 cases/)·· 回归基准 + 实战沉淀
```

智能层与知识层是「**一套资产、两种跑法**」:线性顺序跑 / Claude 下 subagent 并行,**读同一套 references + data**,只是执行器不同。

## 五、两层防线 + 质量旋钮(核心方法论)

两种漏,两种药,**缺一不可**:

| 防线 | 防什么 | 谁来防 |
|---|---|---|
| **反向校验**(`check_coverage`) | 漏抄:原文有判决词命中,清单没纳入 | **程序**自动跑 |
| **A/B 红蓝对抗** | 判断死角:该不该算废标、隐性门槛、原文矛盾 | **第二个独立的大模型脑子** |

A/B 红蓝对抗怎么做:关键专项派**两个独立 subagent**各做一遍,再比差异。**前提:必须真独立(双盲)**,否则盲区会传染。

**质量旋钮**(按标书重要性拧):
- **重要标书** → 关键专项开 A/B 双跑对抗
- **普通标书** → 单跑 + 程序护栏(反向校验)就够

## 六、六道程序护栏(都是确定性,都防漏)

| # | 程序 | 防什么 |
|---|---|---|
| 1 | `scan_keywords.py` | 撒网:判词库逐行扫,每条命中保证扫到 |
| 2 | `scan_candidates.py` | 补词(程序通道):扫"像判决词、未入库"的新词 → 候选区(人审核入库) |
| 3 | `harvest_ai_words.py` | 补词(AI通道):收割 AI 在判断阶段发现的疑似判词 → 同一候选区 |
| 4 | `check_coverage.py` | 反向校验:命中是否被废标清单覆盖,未覆盖列出 |
| 5 | `check_completeness.py` | 通用"明显偏少"警示 + 评分梯度含"分"字 + ▲ ≥ 撒网 ×80% |
| 6 | `cross_doc.py` | 跨文件矛盾:金额/日期/子系统数量确定性比对 |

## 七、不分 9 类——一张通用清单替代

**老 Python 时代曾按"9 类矩阵"(政府采购 × 服务/货物/工程 × …)分类,但大模型直接读懂文件后,这层分类大半是多余的**。
现在改用 `references/disqualification-checklist.md` 一张"审标对照总清单"(废标点 + 隐性门槛 + 类型特化 + 必拿字段)——Claude 审任何标书时**逐条对照**(有就抓、没有就跳过),不再"先分类再选模板"。

**核心永远是**:**找 ▲ 星号项 + 找商务非标项**。

(老 `types/D.json` 的精华去噪锚点和特殊坑、以及原 `special-mechanisms.md` 整本,均已并入 `disqualification-checklist.md`。)

## 八、演进闭环

```
实战一份标书 → 两条补词通道(程序正则 + AI语义发现) → 候选库 →
人审核入库 → 判词库长厚 → 回归测试守住不退化 → 案例沉淀 cases/(规划中) → 反哺
```

每份扫过的标书去敏后可沉淀进 `cases/`(目录规划中,首批案例入库时建立);`tests/` 用合成样本守住基准不退化。

## 九、当前进度

**已端到端验证**(跨多类标书):取数 → 撒网 → 双线专项(商务废标 + 技术 ▲ + 商务评分 + 技术评分 + 证明清册 + 时间节点)→ 程序护栏 + 人工复核 → Excel 多 sheet。覆盖货物·综合评分、央企货物、政采服务、工程·合理低价多种类型。
**A/B 红蓝对抗实验**:两个独立 subagent 处理同一份 300+ ▲ 标书,覆盖一条不差,各自逮到对方盲区(计分说明 / 隐性门槛升格)。

| 层 | 已就绪 ✓ |
|---|---|
| 编排 | SKILL.md(中文端到端工作流 + 两层防线 + 质量旋钮) |
| 程序 | extract_text / scan_keywords / scan_candidates / harvest_ai_words / check_coverage / check_completeness / cross_doc / build_excel |
| 数据 | keywords.json(120+ 词) ; 候选词在 workspace/<项目>.candidates.json(含原文,不入库,promote 时只转词+scope) |
| 知识 | disqualification-checklist(审标对照总清单) + commercial × 5(含合同条款) + technical × 3 |
| 反哺 | tests 基础(cases 规划中) |

## 十、还要做

- 准确率量化(真值标注 + 测试集)——这是把"准确"从嘴上变成数字
- 更多类型实战覆盖,持续完善 `disqualification-checklist.md`
- 词库扩展(目标 200+,靠贡献闭环自然生长)
