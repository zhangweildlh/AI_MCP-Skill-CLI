# FOR_AI.md — 写给 AI agent 的操作手册

> **如果你是一个 AI agent(Claude Code / Codex / Cursor / 通义灵码 / Workbuddy 等),用户让你"运行这个项目":这份文件就是你的任务说明书。从头到尾读完再开工。**
>
> 你的用户很可能**完全不懂技术**——不会命令行、不知道什么是 Python、分不清路径。
> 你的职责:**把整个审标流程跑完,让用户只做三件事——给你标书、回答你的问题、收 Excel。**

---

## 你要做什么(一句话)

带着用户把一份**招标文件**(PDF/Word)变成一份**投标核对清单 Excel**(废标项 + 评分项 + 证明材料 + ▲ 参数 + 时间节点 + 合同要点,每条带行号出处)。

判断标准:**用户最后拿到 Excel 并且知道怎么看,才算完成。**

## 沟通铁律(对小白用户)

1. **每一步开始前,用一句大白话说你要干什么**。说"我先检查你电脑上有没有装必要的工具",不说"执行 check_env.py 校验依赖"。
2. **安装任何东西之前,先问用户同意**(装/不装,以及不装的后果)。这是本项目 INSTALL.md 的既定原则。
3. **出错你自己排查**,不要把报错堆栈甩给用户。实在卡住,用一句话描述问题 + 给用户两个可选动作。
4. **绝不下"投/不投"结论**(项目铁律,见 SKILL.md)。你产出的是清单和事实,决策永远是用户自己的。

## 执行流程(按顺序)

### 第 0 步:把代码弄到用户电脑上

- 用户发给你的可能只有仓库地址。先确认本地有没有这个项目;没有就下载:
  ```bash
  git clone https://github.com/matongAI-lab/tender-review-kit.git
  ```
  没有 git 就下 zip 解压(`https://github.com/matongAI-lab/tender-review-kit/archive/refs/heads/main.zip`)。
- 放哪由你定一个合理位置(比如用户主目录),**告诉用户放在哪了**即可,不要问小白"想放哪个路径"。
- 之后所有命令都在项目根目录下执行。

> **如果你没有执行命令的能力**(比如你是网页聊天版 AI):诚实告诉用户——"这个工具需要一个能操作你电脑的 AI 助手(比如 Claude Code),我这个版本帮不了你跑程序",并把用户引向仓库 README 的「怎么开始」一节。不要假装能跑。

### 第 1 步:环境自检(首次必跑)

```bash
python scripts/check_env.py
```

- 全 ✓ → 直接进第 2 步。
- 有缺失 → 它会列出缺什么、怎么装、不装的后果。**逐项问用户"装/不装"**,同意后替用户执行安装命令,装完重跑自检确认。
- 用户连 Python 都没有 → 按 INSTALL.md 对应平台的命令帮装(同样先征得同意)。

### 第 2 步:找用户要招标文件

让用户把文件**拖进对话窗口**,或告诉你文件在哪(桌面/下载文件夹,你去找)。支持 .docx / .pdf;.doc 让用户先另存为 .docx。

### 第 3 步:程序自动跑(几秒)

```bash
python run_pipeline.py prep <招标文件路径>
```

产出在 `workspace/`:带行号文本(.lines.txt)、判词命中(.hits.json)、候选新词(.candidates.json)。

### 第 4 步:你来判断(核心环节,认真干)

**按 SKILL.md 的步骤 1-2-4-5 执行**——摸底、定位章节、建工作区、商务/技术分线审。这是你的主要工作量,SKILL.md 写得很细,严格照做,特别是四条铁律(全量不压缩 / 列事实带出处 / 维度不绑值 / 产清单不下结论)。

产出:`workspace/<项目>.工作区.md`。

> 期间适时给用户报进度("商务线审完了,找到 X 条废标项,开始审技术线"),别让用户面对长时间沉默。

### 第 5 步:护栏 + 出 Excel(几秒)

```bash
python run_pipeline.py verify workspace/<项目>.工作区.md
```

- 护栏报 warning(漏覆盖/条数异常)→ **回第 4 步补漏,重跑 verify,直到干净**。这是程序在审计你,别跳过。
- 通过 → 产出 `workspace/<项目>.xlsx`。

### 第 6 步:交付

1. 帮用户**直接打开 Excel**(Windows: `start <文件>` / macOS: `open <文件>`)。
2. 用三五句话告诉用户怎么看:哪个 sheet 是废标项(最要命)、哪个是评分项、哪个是要准备的材料、时间节点在哪。
3. 提醒:清单是"招标方要求的全集",投不投、怎么报价是用户自己的决策。

### 第 7 步:当前补漏 + 新词审批 + 贡献(必须对话式征求同意)

verify 之后如果出现 `workspace/<项目>.pending_words.json`,说明你在审标时发现了判词库里没有的新判决词:

0. **先确认当前标书补漏已处理**:`harvest_ai_words.py` 默认会用 AI 发现词临时回扫当前 lines.txt。这个回扫不写本地词库,也不需要用户同意;用户是否入库只影响以后标书,不能影响当前标书补漏。
1. **把每个新词 + 它的原文语境列给用户看**,问用户:"这几个词要不要收进你的本地词库?收了以后扫别的标书会自动用上。"
2. 按用户的答复执行(全收 / 收一部分 / 全不收):
   ```bash
   python scripts/harvest_ai_words.py workspace/<项目>.工作区.md --accept "词A,词B"
   ```
   用户明确说"全部接受"时才用 `--accept-all`;用户说"都不要"时用 `--reject-all`。
3. 无论本次有没有入库,交付收尾都要**复述一次词库互惠机制和隐私声明**:"本地词库只在你机器上;开源词库靠大家脱敏贡献;贡献完全自愿,不给也能正常用;我们不拿你的标书、Excel、工作区、原文片段、项目名称或行号上下文,只拿你愿意分享的几个判词短语及分类/scope;你贡献的词别人能用,别人贡献的你 `git pull` 也能拉到。"如果已有本地词库,再问:"这里面有没有普遍适用(不含你项目隐私)的词,愿意贡献给开源词库?"用户明确同意才跑:
   ```bash
   python scripts/export_contribution.py --github
   ```

> ⚠ 三条红线:**当前标书补漏不等用户入库**;**绝不擅自 `--accept-all`**(入库要用户拍板);**绝不未经明确同意对外贡献**(导出虽已脱敏,但同意权在用户,且默认只提 Issue 给维护者审核)。用户说"不用问了都收"之类的明确授权除外。

## 常见状况速查

| 状况 | 你的处置 |
|------|----------|
| 用户给的是 .doc | 让用户用 Word/WPS 另存为 .docx 再给你 |
| PDF 是扫描件(取数后 lines.txt 几乎空) | 告诉用户这份是图片版 PDF,目前解析不了,让他找招标方要可复制文字的版本 |
| `python` 命令不存在但确实装了 | Windows 下试 `py`;或 PATH 没勾,按 INSTALL.md Q2 处理 |
| verify 反复报同一处 warning | 回工作区核对行号引用是否覆盖判决词所在行(±0 精确反查),修正出处而不是删条目 |
| 用户问"这个标我能投吗" | 指给他清单里的硬性门槛事实,明确说"投不投是你的决策,我只保证你看见了全部要求" |
| 用户想再审第二份标书 | 直接从第 2 步开始,环境不用重装;本地词库会自动带上 |

## 下次再用

- 老用户回来:跳过第 0、1 步,直接要文件开跑。
- 顺手跑一下 `git pull`,能拉到其他贡献者新攒的判词(告诉用户这是社区互惠机制)。
- **如果你是 Claude Code**:首次跑完交付时,建议用户把本仓库装成常驻 skill(clone 到 `~/.claude/skills/tender-review-kit`,Windows 是 `%USERPROFILE%\.claude\skills\`)——装好后他在任何会话里说"审核这份招标文件"就能直接触发,不用再发咒语。用户同意你就替他装。
