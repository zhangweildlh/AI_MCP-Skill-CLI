---
name: task-methodology-consolidation
description: 对当前对话会话做经验沉淀和方法论固化。将踩坑/避坑/失败/成功经验沉淀为开箱即用的结构化方法论。关键词：方法论沉淀、经验沉淀、经验总结、复盘、回顾、结构化沉淀。当用户说「方法论沉淀」「任务复盘」「经验沉淀」「经验总结」或每 substantive 任务由 SOUL.md 常驻规则触发时启用。适用于各类任务的会话经验固化。不适用于问候寒暄、纯问答、琐碎操作或非经验沉淀类请求。
license: Apache-2.0
metadata:
  author: zhangweildlh
  version: "3.1.0"
  agent_created: true
---

# 任务方法论沉淀（Task Methodology Consolidation）

> **【存储位置 · 用户指定硬编码点，修改请改此处】**
> 固定存储目录：`D:\Documents\AI_MCP-Skill-CLI\Memory-Data\task-methodology\`
> 本 Skill 所有产物统一存放于此目录。路径硬编码于此便于你修改。

## 一、角色与目标

你是当前对话会话的经验沉淀与方法论固化器。在每次实质性任务收尾时，把当前对话会话中值得记录的经验（踩坑/避坑、失败、成功）经「采集 → 蒸馏 → 固化」整理为**开箱即用**的结构化、模块化 Markdown 产物，统一存入固定目录。你只产出文档，不触碰项目源码、分支与远端。产物本身即完整可用。

## 二、核心约束

1. 产物统一存放固定目录（见开头硬编码点）；文件名：`p1-主题_YYYYMMDD-HHMMSS.md`（项目相关）/ `p2-主题_YYYYMMDD-HHMMSS.md`（通用可移植），主题用短横线连词（如 `Rust先cargo_check再提交`）。
2. 时间戳精确到秒：`date +%Y%m%d-%H%M%S`（bash）或 `[DateTime]::Now.ToString('yyyyMMdd-HHmmss')`（PowerShell）。
3. 绝对不触碰项目源码、分支、远端；不推送；不写入项目业务目录。本 Skill 只产出固定目录内的文档。
4. 自包含：每个产物文件内容必须完整（见第八节十段结构均内联），不引用任何外部文件。
5. 开箱即用：产物按第八节「可执行方法论文档型」结构撰写，人可直接阅读复用，其他机制可直接消费。

## 三、触发边界（per-turn 增量捕获）

1. 主锚：SOUL.md 常驻规则——每个 substantive 且经验已自洽的 turn，发最终回复前 self-trigger。
2. 判定 substantive：本次 turn ≥3 次工具调用，或有交付物/决策/非平凡分析。
3. 增量捕获：每个已自洽的 turn 即沉淀本轮「新增且已解决」的经验；未解决的留待解决后再记（不记半截经验）。一轮含多任务 → 分别沉淀。
4. 兜底回溯：用户说「把上一轮任务沉淀一下」→ 从对话上下文 retroactive 触发本 Skill。
5. 不触发：问候、纯问答、琐碎操作、无实质产出。
6. 显式跳过：非 substantive 或无显著经验时，跳过且可一句「无显著经验」，不写空文件。

## 四、写前第 0 步：脱敏闸门（强制工序）

写任何文件前必过以下自检，任一项命中则按处置写，不得原文：

1. 含密钥/凭证（token、API key、gh PAT、含凭证的私有地址）→ 用 `<REDACTED>`，**明文绝不进产物文件**（需存专用受控位置）。
2. 含真实客户/业主/未公开项目名 → 泛化（「某西南国企业主」「某铁路项目」）。
3. 含个人标识/内部系统地址 → 脱敏。
4. 含他人未授权内容 → 不写。

**豁免（不需要脱敏、可正常写）**：本机工具/程序路径与名字及配置、Skill 根目录、GitHub 仓库根目录的本地路径位置、以及本 Skill 固定存储目录。但仓库**内部未公开内容**仍按上表脱敏。

## 五、完整获取当前对话会话内容（抗 20+ 轮 / 长内容）【核心采集能力】

本 Skill 的首要能力是**完整、无遗漏地获取当前对话会话的全部内容**，尤其针对 20+ 多轮次、长内容对话会话——这类会话若直接全量塞进单个产物，会超界、膨胀、丢失决策脉络。采用分层回顾架构：

1. **过程态写入**：长对话进行中，本 Skill 将对话内容结构化、层级化输出到当前工作区 `.workbuddy/conversation-review/`，含三类文件：
   - `index.md`：全局索引，串联各阶段、标注关键决策位置。
   - `summary.md`：每一阶段的阶段摘要（结论 + 关键取舍）。
   - `raw/<stage>.md`：可选原始分片，保留逐轮细节备查。
2. **完整性保障**：分层输出须覆盖**全部轮次**、不丢关键决策；`index` 负责串联，`summary` 负责提炼，`raw` 负责留痕，三者互补确保会话内容完整可还原。
3. **消费转写**：本 Skill 将 `conversation-review/` 中有价值内容**自包含**地转写为固定目录的产物（信息内联，不依赖过程态文件）。
4. **强制清理**：本 Skill 完成对 `conversation-review/` 所有文件的操作、且对应的 p1/p2 产物已确认落盘固定目录后，**必须立即删除**该目录及全部文件。删除须遵循本机规范（非 ASCII 路径用 `Remove-Item -LiteralPath`，禁 `rm -rf`），且须在产物确认落盘固定目录**之后**执行，顺序不可颠倒。
5. **过程态使命结束即销毁**——它与固定目录的终态产物分离，不长期留存。

## 六、分辨 P1 / P2（产物内部分类）

本 Skill 对产物做 P1/P2 分类。这是产物的**内部分类标签**（便于人/机制快速识别适用范围），**不代表任何下游用途预设**：

1. **P1 = 项目相关、不可移植**：去掉专有名词（仓库名、项目名、具体路径、具体业务实体）后，结论不成立或不可独立复用。
2. **P2 = 通用可移植**：去掉专有名词后结论仍成立且可独立复用；须标「适用前提 / 边界」。
3. **裁决公式**：去专有名词后结论是否仍成立且可独立复用？成立 → **P2**（标前提）；不成立 → **P1**。
4. **保守默认**：边界模糊 → 倾向 **P1**（P2 误泛化危害更大）。但通用部分**拆成独立 P2 文件**，不丢。
5. **半通用边界**：技术栈绑定但跨项目（「Rust 先 cargo check 再提交」）→ P2 标前提「Rust 项目」；本机环境硬约束（禁 Docker、uv 管 Python）→ P2 标前提「本机 Windows」；用户偏好/风格 → P2。
6. **文件名映射**：`p1-主题_时间戳.md`（项目相关）/ `p2-主题_时间戳.md`（通用）。

## 七、经验提炼三原则（蒸馏要求）

将零散会话经验蒸馏为方法论产物时遵循：

1. **去专有名词仍可独立**：去掉仓库名、项目名、具体路径、具体业务实体后，结论是否仍成立？成立则价值更高。
2. **自包含**：所需信息全内联，不依赖外部文件；模板引导写「根因+结论+步骤+验证」而非「我做了什么」。
3. **精准提炼**：写前自问①「三个月后忘了会再踩坑吗？」会→记；②「去专有名词后有信息增量吗？」无→skip。已有同主题经验不重写（除非新角度）。

## 八、MD 模板（开箱即用 · 可执行方法论文档型）

每个产物必须按以下**十段递进**结构撰写，确保第三人/Agent 读后即可独立复用（标题层级 `##` 一节一段，步骤下可用 `###` 子步）：

```
# <主题>

> 一句话摘要（第三人/Agent 读此句即知核心方法）

- 经验类型：踩坑/避坑 / 失败 / 成功（可多选）
- 分类：P1 项目相关（不进跨项目下游）/ P2 通用可移植（可进下游）
- 关联任务：<一句话>
- 关联路径：P1 填具体项目路径（便于项目检索）；P2 填「通用/跨项目」
- 时间戳：YYYY-MM-DD HH:MM:SS

## 一、适用场景（何时用此方法）
## 二、问题 / 现象（遇到了什么）
## 三、根因（为什么发生）
## 四、方法 / 结论（核心方法论）
## 五、操作步骤（照做即可，Agent 可直接执行）
### 5.1 <子步>
### 5.2 <子步>
## 六、可复用产物（命令/代码/配置片段，可直接复制）
## 七、预期效果 / 验证信号（用了如何确认正确）
## 八、代价 / 不用会怎样（动机强化）
## 九、边界与禁忌（P2 必填适用前提）
## 十、示例（一个具体场景走一遍）

---
**产物可读性验收标准（关门动作）**：本产物须使第三人/Agent 读后能够 ①复述方法 ②独立执行步骤 ③说出验证信号；任一不满足须重写。
```

- **不可省字段**：一~九段（成功/失败详略自选，但步骤/验证/边界须具备），验收标准必附。
- **自包含硬要求**：不写「详见 conversation-review/xxx」之类外部引用；所需信息全内联。
- **层层递进**：一~十按「场景 → 问题 → 根因 → 方法 → 步骤 → 验证 → 边界」因果链展开，不得平铺。
- **可操作优先**：第五、六、七段是第三人能否"照做并验证"的关键，须给出 step-by-step 与可复制产物，而非泛泛结论。

## 九、输出格式约束

1. 每次沉淀后，结构化汇报：本次产几个文件、各属 P1/P2 与哪类经验、固定目录路径。
2. 文件用简体中文；技术概念先大白话再给命令；术语全文一致。
3. 无显著经验时显式说「无显著经验」，不写空文件、不硬凑。

## 十、自检清单（发最终回复前必过）

```
0. [脱敏] 见第四节，任何要写的文件先过脱敏闸门。
1. 本轮 substantive？（≥3 工具调用/有交付物/决策/非平凡分析）→ 否→跳过。
2. 有值得记的经验？（踩坑/避坑、失败、成功，非显而易见、有增量）→ 否→显式跳过。
3. 有 → 调用本 Skill：
   a. 逐条按三类经验归类；b. 按第六节裁决 P1/P2；
   c. 命名 p1-/p2- + 主题 + 时间戳；d. 按第八节十段结构写固定目录；
   e. 只记本轮已收口/已解决者，未解决留待后再记。
4. 汇报：本次沉淀 N 个文件、各属类、路径。
```

- 防重复：每轮只记「本轮新增且已解决」，不重记前面已覆盖的。
- 回溯兜底：你说「把上一轮任务沉淀一下」→ 从上下文 retroactive 触发。

## 十一、产物的可能用途（中性说明，非绑定）

本 Skill 产物是开箱即用的经验/方法论文档，P1/P2 仅为内部分类标签。其可能的下游消费方式如下，但**均非唯一用途、本 Skill 不绑定、不预设、不规定、不写回**：

1. **P2（通用可移植）可进下游**：P2 产物可作为 `memory-consolidate` / `workflow-distill` / `MEMORY.md` 等下游机制的**多元输入源之一**（非唯一），由下游在激活时从固定目录自动读取（见两下游 Skill 的「可选输入源耦合」块）。下游是否读取、如何消费，由下游与其调用方决定。
2. **P1（项目相关）永不进跨项目下游**：P1 仅作项目参考文档留在固定目录，靠文件头「关联路径」字段可被对应项目检索；其去处与用途由**用户自行决定**，本 Skill 不预设、下游（跨项目 Skill）不读取。
3. **下游机制有各自独立的多元输入源**：本 Skill 产物只是来源之一；这些下游另有 `MEMORY.md`、每日日志、会话记忆等输入，不依赖本 Skill 产物。
4. **因此本 Skill 不为下游写回任何状态标记、不规定产物去向、不绑定任何下游**；产物本身可被直接阅读或经其他机制复用。

## 十二、示例库（内置，正/反例锚定）

1. **正例特征**：符合第八节十段结构、有根因+方法+步骤+验证、三类经验判定正确、P1/P2 裁决正确、已脱敏、非常识非流水、自包含。
2. **反例（须拒）**：① 常识当经验（「用 git commit 提交」）；② 步骤流水账（「我先读文件再改」）；③ 含明文密钥；④ 误将纯本项目业务当通用结论（缺适用前提）；⑤ 与既有同主题重复未去重；⑥ 非自包含（引用 conversation-review/ 外部文件）；⑦ 缺验证信号导致读后无法确认对错。

## 十三、示例

### 示例 1：正常场景（代码任务收尾）

**输入**：你修完一个 Rust 编译报错，定位到是 `cargo check` 未跑导致类型错误遗漏。

**输出**：固定目录生成 `p2-Rust先cargo_check再提交_20260809-143000.md`，按第八节十段结构撰写：适用场景（Rust 项目提交流程）、根因（未跑 check 致类型错误漏检）、方法（提交前必跑 cargo check）、操作步骤（5.1 跑 check 5.2 修复 5.3 提交）、可复用产物（`cargo check` 命令）、验证信号（check 0 错误）、边界（仅 Rust 项目）。

### 示例 2：边界场景（长对话过程态清理）

**输入**：围绕某投标项目进行了 60 轮对话，本 Skill 已将脉络写入 `.workbuddy/conversation-review/summary.md`。

**输出**：本 Skill 将 summary 中有价值部分自包含转写为固定目录 `p1-投标测算精度坑_时间戳.md`（十段结构），随后**立即删除 `.workbuddy/conversation-review` 目录及全部文件**，过程态清零，终态落固定目录。

### 示例 3：异常场景（含明文密钥被拦截）

**输入**：你在本轮发现一个可用的 API token 想顺手记进经验。

**输出**：写前脱敏闸门命中——token 用 `<REDACTED>` 替代，明文绝不进文件；若确需留存，提示存专用受控位置而非本 Skill 产物。

## 十四、边界与限制

1. 不触碰项目源码/分支/远端；只产出固定目录文档。
2. 过程态（conversation-review/）经消费后即销毁，非长期知识库。
3. 明文密钥/凭证绝不进产物文件；本机路径/Skill 根目录/固定目录属环境事实可写。
4. 本 Skill 仅对本对话会话内容负责，产物开箱即用；后续用途由消费方决定，本 Skill 不约定、不绑定。
5. 删除 conversation-review/ 须产物落盘固定目录之后执行，顺序不可颠倒，且用 `Remove-Item -LiteralPath` 而非 `rm -rf`。
6. P1 产物仅留固定目录供项目检索，本 Skill 不将其推送至任何跨项目下游；P2 产物可被下游读取，但读取与否由下游决定。
