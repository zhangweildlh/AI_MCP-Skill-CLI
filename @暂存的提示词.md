# 暂存的提示词

如果任务未完成，则继续执行。若已完成，则回复“已完成”并用大白话报告任务执行情况和结果。

-------------------------

你需要对审计/建议报告中的每一项指控/内容进行逐一分析和质证，证实或证伪。对证实或指控成立的项实施修改。

-------------------------

本仓库完整 PR 闭环：提交 → 推送 → 开 PR → 跑 CI；全绿则合并 main ，然后清理远端/本地分支、工作树与工区；非全绿则根因分析并修复、循环重跑至全绿再合并。本指令为整条链路授权。

-------------------------

任务一：分别分析 `nodejs` 相关目录及各级子目录 `D:\Tools\Assembly\nodejs\node_global` `D:\Tools\Assembly\nodejs\node_global\node_modules` `D:\Tools\Assembly\nodejs\node_modules` 和 UV 管理的 Python 项目目录及各级子目录 `D:\Tools\Assembly\python\myenv`中有哪些可以安全删除的文件和子目录。任务二：分别分析 `nodejs` 相关目录及各级子目录 `D:\Tools\Assembly\nodejs\node_global` `D:\Tools\Assembly\nodejs\node_global\node_modules` `D:\Tools\Assembly\nodejs\node_modules` 和 UV 管理的 Python 项目目录及各级子目录 `D:\Tools\Assembly\python\myenv`中有哪些与 `D:\Tools\MCP_Bridge\dynamic-mcp.json` `C:\Users\15794\.workbuddy\mcp.json` `C:\Users\15794\.workbuddy\models.json` 文件内容和 `C:\Users\15794\.workbuddy\skills` 目录中各 Skill 技能(各级子目录)和 `D:\Tools\Assembly\AtomCode` `D:\Tools\Assembly\FreeLLMAPI` 软件无关、无调用/引用关系、无依赖关系的模块、依赖包、工具/软件，需安全彻底删除。任务三：清理 `nodejs` 相关、`UV` 相关、`python` 相关缓存、临时文件、测试文件、分析文件和垃圾文件。

-------------------------

针对本对话会话任务代码文件，读取并严格遵守记忆文件 `D:\Documents\AI_MCP-Skill-CLI\Memory-Data\Memory-代码纪律与Git操作.md` 纪律，循环迭代整套流程直到当前任务范围、环境和质量门禁下无阻塞BUG(无已知未关闭的 P0/P1/P2 BUG/风险/缺陷，所有必需测试通过，无新增失败、无未验证修复、无 flaky，证据完整)；全程不可跳过任一环节，静态代码审计必须在所有动态测试之前，仅静态组内、动态组内可微调顺序。达到 5 轮仍有问题则停止并输出大白话详细报告。单轮步骤：1.锁定代码基线并记录构建/测试/E2E命令、环境、依赖；2.调用Skill `code-review-combo`代码审计；3.执行内容完整性核验；4.执行全场景覆盖矩阵回归测试；5.执行跨引擎交叉验证；6.执行端到端真实运行态测试；7.汇总合并去重问题、按P0>P1>P2>P3分级，优先编写失败用例、最小化修复；禁止修改无关代码；8.修复后做回归验证：校验修复项、受影响矩阵、跨引擎、E2E烟测。若仍存在P0/P1/P2未关闭、必需测试失败、新增缺陷、修复未验证或flaky，则启动新一轮迭代；全部阻塞项解决才终止。授权读写代码/测试/配置/文档、运行构建测试、启动本地服务、使用本地具有的 MCP 服务和 CLI 工具(比如 )；**禁止跳过失败、禁止伪造证据、禁止篡改测试代码强行满足门禁、禁止以局部回归替代全套复测**。

-------------------------

【开发态→部署态】针对本对话会话任务代码文件，读取并严格遵守记忆文件 `D:\Documents\AI_MCP-Skill-CLI\Memory-Data\Memory-代码纪律与Git操作.md` 纪律。
本项目/任务开发态副本目录为：`<开发态目录占位符>`，部署态副本目录为：`<部署态目录占位符>`。开发态目录是源文件/配置/文档的维护与修复目录；部署态目录是经安装配置后形成的已交付可运行目录。各项测试、E2E、运行态验证均需针对部署态目录进行，以部署态文件为准；静态代码审计和问题归因需针对开发态文件并考虑部署态文件实际情况、考虑开发态文件与部署态文件的差异(包含合理差异、配置差异)。
对部署态文件测试中发现的 BUG/问题，以及可优化、可健壮、可加固项，必须逐项做归因分析并坐实：A. 根植于开发态源文件；B. 部署过程引入的缺陷；C. 误报。对开发态根源问题、可通过源码补强/修改根除的部署复现缺陷，必须在开发态源文件中修复；禁止修改部署产物。修复后必须重新构建部署到部署态目录。完成新部署后，基于新的部署态文件复测，确认修复真实生效。对纯部署流程引入且无法通过源码修复的问题，完整记录根因、证据、影响、缓解方案，不可标记为源码修复完成。
循环迭代整套流程直到当前任务范围、环境和质量门禁下无阻塞BUG(无已知未关闭的 P0/P1/P2 BUG/风险/缺陷，所有必需测试通过，无新增失败、无未验证修复、无 flaky，证据完整，且部署态测试与归因闭环完成)；全程不可跳过任一环节，静态代码审计必须在所有动态测试之前，仅静态组内、动态组内可微调顺序。达到 5 轮仍有问题则停止并输出大白话详细报告。单轮步骤：
1.锁定代码基线并记录开发态/部署态目录、版本/提交、构建/部署/测试/E2E命令、环境、依赖、部署产物标识（哈希/时间/路径），确认开发态与部署态对应关系；
2.调用Skill `code-review-combo`对开发态源文件及与部署态差异相关的代码、配置、构建脚本做静态代码审计；
3.执行内容完整性核验：核对开发态源文件、构建产物、部署态文件、配置、依赖、权限、路径、环境变量、资源文件等完整性、一致性、差异项；
4.执行全场景覆盖矩阵回归测试：以部署态文件为主，覆盖功能、异常、边界、权限、配置、依赖、路径、并发、兼容性等矩阵；
5.执行跨引擎交叉验证；
6.执行端到端真实运行态测试：在部署态真实启动、访问、调用、运行，验证部署态行为；
7.汇总合并去重问题、按P0>P1>P2>P3分级，并对每项做归因（开发态根植/部署引入/误报）；优先编写失败用例、最小化修复；禁止修改无关代码；对开发态根植项和可由开发态补强根除的部署复现项，在开发态源文件中修复；
8.修复后做回归验证：校验修复项、受影响矩阵、跨引擎、E2E烟测；必要时重新构建/部署/发布并复测部署态。若仍存在P0/P1/P2未关闭、必需测试失败、新增缺陷、修复未验证或flaky，则启动新一轮迭代；全部阻塞项解决才终止。
授权读写代码/测试/配置/文档、运行构建测试、启动本地服务、使用本地具有的 MCP 服务和 CLI 工具；**禁止跳过失败、禁止伪造证据、禁止篡改测试代码强行满足门禁、禁止以局部回归替代全套复测**。