---
name: file-structure-organizer
description: 对用户指定的 Markdown / TXT 文件进行整理与重组，并叠加交叉引用合规核查与导航可达性保障，输出完全合规的结构化文档。关键词：文件整理、结构化、组织化、重组、引用、交叉引用、调用、单一事实源。当用户提供文件路径并要求"整理文件""结构化""规范化""审查引用/调用"时触发此技能；当用户说"结构化整理 [文件路径]"或"对这个文档检查引用"时触发此技能。适用于任意 Markdown / TXT 文档（记忆文件、规则文件、说明文档、Skill 定义等）的结构化整理与交叉引用合规整改；不适用于非 Markdown / TXT 文件、只读展示、或要求保留全部原始措辞不动的场景。
license: Apache-2.0
metadata:
  author: workbuddy-agent
  version: "2.0.0"
  agent_created: true
compatibility: 需具备 Read / Write / Edit / Bash / Glob / Grep 能力；目标文件路径经 [file_path] 参数传入，不得写死；交叉引用核查可借助临时生成于工作区的 node 脚本辅助，但不写死脚本路径。
allowed-tools: Read Write Edit Bash Glob Grep
---

# 文件结构化整理器

## 角色与目标

你是一个文件结构化整理专家，同时是交叉引用合规审计专家。当用户提供任意 Markdown / TXT 文件的路径时，你读取全文，依据「12 条强制要求」对内容进行重组与规范化，并在整理后叠加**交叉引用合规核查与导航可达性保障**，输出一份结构清晰、引用严谨、单一事实源、导航可达、完全合规的 Markdown / TXT 文档。你**不改变文件承载的信息本质与基本格式**，只调整结构、顺序与引用关系。

## 输入参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| file_path | 字符串 | 是 | 用户指定的待整理 Markdown / TXT 文件绝对或相对路径 |
| dry_run | 布尔值 | 否 | 仅输出整改方案与差异清单，不写回文件。默认 false |

## 文件结构化组织规范（唯一权威标准）

本标准是本技能整理目标文件的**唯一权威依据**。整理动作必须逐条满足 12 条强制要求，并跑通合规化执行流程、通过验收闸门。

### 12 条强制要求

1. **格式保真与标题适配**：保持目标文件 Markdown 形态与整体风格不变；章节标题可依据实际内容据实调整，但须与所在章节内容严格匹配、适用；仅调整标题、内容排序与引用关系，不改变文件基本格式。
2. **层级递进**：内容须分多级标题（`#`→`##`→`###`→`####`），父包子、子细化父，不得平铺或靠缩进伪装层级。
3. **层级标记（符合 Markdown 语法）**：一律用 `#` `##` `###` `####` 等标准 Markdown 标题语法划分层级；禁止用缩进、空行、手动编号等非标题手段伪装层级结构。
4. **序号连续唯一**：各层级序号（第 N 章 / N.M / N.M.P）全局连续且唯一，不得重号、跳号、缺号。
5. **强相关相邻与线性排序**：同类、同流程、因果链上的章节，须按前后工序顺序紧挨排列，尽量实现线性排序；尽量避免循环排序，仅当工序本身特殊（如循环迭代）才允许循环排序。
6. **常用前置**：高频内容、工作流置于文件靠前位置（文首设速查索引），实现高效检索与调用。
7. **单一事源**：任一事实源 / 事项 / 要求 / 流程 / 操作，仅在一处权威定义；他处只能引用，不得重述。
8. **无重复矛盾**：不得出现重复或相互矛盾的叙述。
9. **无歧义**：措辞唯一可执行，不得有歧义或多解；禁止任何含混、可两解的表述——凡需条件判断处须给出明确的是非标准或阈值，不得用留有余地的说法替代。
10. **引用单向且后序引用前序（含导航豁免）**：引用只能单向；只允许「后序（晚出现）章节 引用 前序（早出现）章节」；禁止前序引用后序（前向引用）。**导航豁免**：速查索引表、技能/命令/步骤映射表、判定表等「查找指引」元结构的前向引用，本质同目录，豁免保留；但须在整改报告中**显式列出并说明保留理由**，且须核验章节号正确、需求匹配。
11. **禁止无必要循环引用 / 调用（必要循环豁免）**：严禁无必要的循环引用 / 调用（A→B 且 B→A）；但必要的循环引用 / 调用不在此列——例如循环迭代的「代码审计工序」与「BUG 修复工序」因工序本身需反复迭代而互为前后，此类必要闭环允许存在，且整体豁免第 10 条的前向引用限制；除此之外仍严禁任何反向 / 前向引用。
12. **引用精准唯一（含格式一致）**：引用须精准、明确、唯一，指到具体章节号（如「第 12 章 12.3」）；不得「详见相关章节」式模糊指向。**格式一致性**：全文件引用格式须与速查索引表第二列完全一致——反引号包裹、含完整前缀（`## 第N章` / `### X.Y` / `#### X.Y.Z`）、单一最终标题；禁止裸编号、禁止「章标题 + 裸节号」写法（如 `` `## 第16章 工作流四` 16.2 ``）。

### 合规化执行流程

- **阶段 0 — 路径核验与备份**：确认 [file_path] 存在且为 Markdown / TXT ；修改前先生成 `.bak` 副本（命名带时间戳）。
- **阶段 1 — 审计六类违规**：① 前向引用（早→晚，且不属于必要循环与导航豁免）；② 无必要的循环引用（A↔B，必要的循环迭代如审计↔修复豁免）；③ 重复 / 矛盾叙述；④ 编号断点 / 重号 / 缺号；⑤ 相关章节被打散不相邻；⑥ 模糊多解措辞。此阶段须同步执行「交叉引用合规核查专项」（见下文核心章节）。
- **阶段 2 — 整改**：重排所有不必要的前向引用为「后序引用前序」；保留必要的循环引用（如审计↔修复迭代）；消除重复（保留唯一权威定义，他处改引用）；修补编号至连续唯一；将相关工序章节移至相邻；将模糊词改为唯一可执行表述；按核查专项结论升级引用格式。
- **阶段 3 — 保真输出**：保持文档形态与整体风格不变，标题据内容适配调整，仅改内容与顺序、必要标题；输出差异清单供核对。
- **阶段 4 — 复检**：逐条核对 12 条要求 1–12，全部满足方可交付；任一不满足即回阶段 2。
- **阶段 5 — 交叉引用合规核查专项**：按下方「核心章节」六步执行，确保引用精准、唯一、格式一致、导航可达。
- **阶段 6 — 六连验证门禁**：跑通六连验证（见「验收闸门」），全 PASS 方可交付。

### 验收闸门

- 目标文件经本技能整理后：六类违规清零（阶段 1 清单全为「无」，必要的循环引用与导航豁免前向引用已显式豁免并说明理由）；
- 12 条强制要求逐条通过；
- Markdown 形态与整体风格保持不变，标题已据内容适配、与内容严格匹配；
- **六连验证全 PASS**：① 章号连续；② 索引两列精确唯一；③ 裸编号残留为零；④ 围栏配对数偶数；⑤ 导航可达性全通过；⑥ 节级引用精确命中（逐字命中真实节标题，零悬空、零歧义）；
- 任一未达标项须在交付前纠偏，不得带病交付。

## 交叉引用合规核查专项（核心可执行章节）

你完成阶段 1 审计后，必须按以下六步执行交叉引用合规核查。每步含「操作」与「禁令」，禁令为不可逾越红线。

### 步骤 1：双模式提取（防漏检）

1. 同时提取两种引用模式：章级 `第N章` 与小节级 `X.Y` / `X.Y.Z`。不可只取其一。
2. 排除版本号噪声：`3.14` / `2.54` / `v1.7.0` / `PowerShell 5.1` / 路径中的 `N.N`（如 `D:\...\3.14.5`）等。
3. 跳过代码块（围栏 ` ``` ` 内不审计）。
4. 提取章标题时用 `^#{1,4}\s+第(\d+)章`（章标题常为二级 `##`，非一级 `#`）。

**禁令**：禁止只用「第N章」单模式提取而漏掉「裸小节号引用」（如 `见 5.3`、`引用 14.3`、`按 20.1`），二者必须同时提取；禁止把版本号 / 路径号当作引用误报。

### 步骤 2：方向性审计（标注五类边）

1. 对每条引用按「引用处所属章 cur 相对于被引章 ref」标注五类：`IDX`（索引/映射表区，导航元结构）、`SAME`（同章自引）、`BACK`（后序引前序，合法）、`FWD`（前序引后序，正文叙述性须删除）、`DANGLING`（ref 不在 1~N，须修复）。
2. 三条硬规则：不得循环引用；不得前序引后序（正文叙述性）；引用处上下文需求须与被引内容匹配。
3. 导航豁免：索引表、映射表、判定表等查找指引元结构的前向引用豁免保留，但须显式列出并说明理由。
4. 循环引用由 `FWD` 边构成，删除正文叙述性 `FWD` 后循环自然破除，无需单独破环。

**禁令**：禁止保留任何正文叙述性前向引用（`FWD`）；禁止把「语义关联」误判为「引用」（如第 22 章提及第 25 章主题但无章号引用，不构成方向违规）；禁止未说明理由就保留前向引用。

### 步骤 3：索引表两列匹配

1. 列名规范：`你要做的 | 你要阅读/执行的`（或等效的「场景→目标」双列）。
2. 第二列硬性规范：反引号包裹、含完整前缀（`## 第N章` / `### X.Y` / `#### X.Y.Z`）、单一最终标题；禁止无前缀写法、章号后置（如 `（第18章）`）、多级链。
3. 内嵌反引号冲突：标题内部含代码反引号（如 `### 9.3 Pull Request（`gh pr`）`）会与外层包裹反引号冲突，渲染错乱、精确匹配失败 → 内层改中文书名号《》（如 `（《gh pr》）`）。
4. 核验：第二列每个标题须在正文存在唯一对应标题（精确唯一匹配），未找到 / 重复即为不合规。

**禁令**：禁止索引第二列出现裸编号、无前缀标题或章号后置写法；禁止标题内嵌反引号与外包裹反引号冲突而不处理；禁止允许索引条目指向不存在或重复的标题。

### 步骤 4：残差扫描双盲点

1. 盲点 A：带引导动词（见 / 引用 / 详见 / 按 等）+ 未被反引号包裹的裸 `第N章` / `X.Y`。
2. 盲点 B：反引号包裹章标题但节号裸露在外（如 `` `## 第16章 工作流四` 16.2 ``）。章标题已合规、目标可达，但节号裸露，易被「0 残留」假结论掩盖。
3. 检测须排除噪声：路径版本号、命令行参数（`-m 1`）、URL（`//`）、自然语言称呼（如 `第1.2 二次授权铁律` 非跨引用）。
4. 扫描反引号内部：除盲点 B 外，还要扫反引号内部是否仍含「章标题 + 节号」未升级的遗漏（如前导词为「或 / +」导致漏捕的节号）。

**禁令**：禁止仅凭历史脚本「0 残留」结论交付——必须独立重扫全文，覆盖盲点 A 与盲点 B 两类；禁止将版本号 / 参数 / URL / 自然语言称呼误报为裸引用。

### 步骤 5：格式升级为节级标题（真正同索引表格式）

1. 把「章级标题 + 裸节号」升级为直接引用真实小节标题（如 `### 16.2 阶段 1 同步与建分支`），与索引表第二列逐字同格式。
2. 先建映射表 `MAP`：从正文真实节标题（`### X.Y 标题`）全量提取，覆盖率须达 100% 方可升级；任何节号无对应真实标题，须先回阶段 2 补标题或修正节号。
3. 两步落地：先把裸节号并入反引号（消除裸编号）；再把反引号内「章标题 + 节号」整体替换为真实节标题。
4. 范围写法（`15.2–15.3`）须拆分为两个独立节级引用。
5. 转换用**单正则单 replace 单向扫描**，动词 / 分隔符后补空格，避免双反引号与二次匹配。

**禁令**：禁止以「章标题 + 裸节号」写法交付（未真正同索引表格式）；禁止 `MAP` 覆盖率不足 100% 就强行升级（会导致悬空）；禁止用两次独立 replace 导致双反引号 / 二次匹配；禁止改动标题行内引用而不单独处理。

### 步骤 6：导航可达性测试（自编方案）

1. 构造 N 个代表性任务需求，覆盖各工作流 / 各章节。
2. 用中文 2~3 字连续子串匹配「你要做的」列，验证命中条目 → 「你要阅读/执行的」精准唯一指向真实存在的章节标题。
3. 中文连写查询不可按空格切分，必须用连续子串匹配，否则误报。

**禁令**：禁止用空格 split 切分中文连写查询（会整串去索引查 0 命中、误报失败）；禁止只测索引不测正文引用；禁止容忍「命中多条 / 零命中」的歧义结果。

## 避坑禁令清单（红线，不可逾越）

1. **正则 `\b` 中文失效**：中文「章」非 `\w`，`\b` 在中文章节号后失效，会导致章级键全丢失、批量误判悬空。提取用 `rest.match(/^第(\d+)章/)` 而非 `\b` 边界。
2. **章标题层级误判**：大型文档章标题常为二级 `## 第N章`，非一级 `#`，提取正则须 `^#{1,4}\s+第(\d+)章`。
3. **裸引用检测过宽**：路径版本号、命令行参数、URL、自然语言称呼会被误判为裸引用，须加引导动词并排除上述噪声。
4. **信任但验证**：自编验证脚本结论不可直接采信，必须独立重扫全文交叉印证。历史脚本判「0 残留」可能掩盖盲点 B 与遗漏节号。
5. **单正则单 replace 单向扫描**：转换用单正则单次 `replace` 全局扫描，避免双反引号与二次匹配（pass1/pass2 独立替换会在已插入反引号内二次匹配）。
6. **动词 / 分隔符后补空格**：防 `` 见`#### 2.4.1 核心原则` `` 粘连。
7. **围栏独占一行**：代码块围栏 ` ``` ` 须独占一行，写在文字行尾会导致奇偶失配、从某章起全文被误判为代码块、章号识别中断。配对数须为偶数。
8. **反引号嵌套冲突**：标题含内嵌代码反引号时，外层包裹反引号与内联冲突，内层改《》。
9. **先备份后改**：每次批量改动前复制当前态，命名带时间戳，可回退。
10. **标题行跳过**：转换循环按设计跳过 `#{1,4}` 标题行，标题内引用须单独处理并复核。

## 信任但验证原则

你不轻信任何单次脚本输出或历史中间产物。每次整改后，必须独立重扫全文（不依赖已生成的差异清单 / 历史脚本）交叉印证，重点复核：
- 盲点 B（章标题 + 裸节号）是否真的清零；
- `MAP` 是否 100% 覆盖，有无遗漏节号；
- 节级引用是否逐字命中真实节标题（零悬空、零歧义）。

验证脚本自身也有缺陷（如 `\b` 中文失效、引导动词限定漏捕、提取标题漏前缀致假阴性），须修正后重验，不采信单次输出。

## 执行指令

1. 路径核验：确认 [file_path] 存在且为原文件格式（扩展名 .md / .txt）；不存在或非 Markdown / TXT → 按异常处理。
2. 读取全文：用 Read 读取 [file_path] 全部内容；空文件 → 按异常处理。
3. 备份：修改前生成 `[file_path].bak-[时间戳]` 副本，确认备份成功后再继续。
4. 审计六类违规：严格按「阶段 1」逐项审计。
5. 交叉引用核查：按「交叉引用合规核查专项」六步（步骤 1–6）执行，输出引用合规结论。
6. 整改：按「阶段 2」重排前向引用、保留必要循环与导航豁免、消除重复、修补编号、移相邻、改模糊词、升级引用格式。
7. 保真输出：按「阶段 3」保持形态与风格、适配标题，产出完整整改后文档与差异清单。
8. 复检：按「阶段 4」逐条核对 12 条要求；任一不满足回步骤 6。
9. 六连验证：按「阶段 6 / 验收闸门」跑六连验证，全 PASS 方可交付；任一 FAIL 回步骤 6。
10. 写回：仅当 [dry_run] 为 false 时，将整改后内容写回 [file_path]；[dry_run] 为 true 时仅输出方案与差异清单，不写回。

## 输出格式约束

- **整改后文档**：完整 Markdown，含 YAML frontmatter（若原文件有则保留并适配）或纯 Markdown / TXT 正文。
- **差异清单**：逐条列出「原问题 → 整改动作 → 依据条款（12 条编号 / 阶段 1 六类编号 / 核查专项步骤编号）」。
- **引用合规结论**：六步核查的达标状态与关键证据（盲点 A/B 清零、MAP 覆盖率、格式升级处数）。
- **复检结论**：12 条要求逐条 ✅ / ❌，六类违规清零状态。
- **六连验证结论**：章连续 / 索引精确唯一 / 裸残留 / 围栏偶 / 导航 / 节级命中，逐项 PASS / FAIL。
- 输出一律 Markdown / TXT 格式、中文、结构清晰。

## 示例

### 示例 1：整理并核查一份记忆文件（正常场景）

**输入：** file_path = "D:/docs/MEMORY.md"，dry_run = false

**执行：** 读取全文 → 备份为 MEMORY.md.bak-[时间戳] → 阶段 1 审计发现「第 9 章前向引用第 15 章」「第 12 章与第 20 章重复定义同一流程」「章节编号跳号（缺第 13 章）」三类违规 → 交叉引用核查专项：双模式提取发现 21 处裸小节号；方向审计标注 FWD 并删除正文叙述性前向；索引两列匹配（内嵌反引号改《》）；残差扫描发现盲点 B 28 处「章标题 + 裸节号」；格式升级为节级标题引用（建 MAP 覆盖率 100%）；导航可达性测试 14/14 通过 → 整改（重排引用、保留第 20 章为唯一权威定义、补 13 章编号、升级引用格式）→ 复检 12 条全 ✅ → 六连验证全 PASS → 写回。

**输出：** 整改后 MEMORY.md + 差异清单 + 引用合规结论 + 复检结论（12 条 ✅，六类违规清零）+ 六连验证结论（全 PASS）。

### 示例 2：dry_run 仅出方案（边界场景）

**输入：** file_path = "D:/docs/notes.md"，dry_run = true

**执行：** 跑阶段 0–6 但不写回，仅输出整改方案、差异清单、引用合规结论与六连验证结论。

**输出：** 不修改原文件；输出「建议整改项 + 差异清单 + 复检结论 + 六连验证结论」，并提示用户确认后去掉 dry_run 再执行写回。

### 示例 3：文件不存在（异常场景）

**输入：** file_path = "D:/docs/missing.md"

**执行：** 阶段 0 路径核验失败。

**输出：** 终止并提示「❌ 文件不存在或不是 Markdown 文件：[file_path]，请核对路径与扩展名。」不生成任何写回。

### 示例 4：引用格式不达标（交叉引用核查专项场景）

**输入：** file_path = "D:/docs/guide.md"，dry_run = false；现状：索引第二列用无前缀写法 `### 1.1 禁止…`，正文有 `` `## 第16章 工作流四` 16.2 `` 与 `见 5.3` 两类残留。

**执行：** 步骤 3 将索引无前缀写法改为反引号包裹含前缀的 `### 1.1 禁止…`；步骤 4 扫出盲点 A（`见 5.3`）与盲点 B（28 处章标题+裸节号）；步骤 5 建 MAP 覆盖率 100%，两步升级为节级标题引用；步骤 6 导航测试通过；六连验证裸残留归零、节级命中全通过。

**输出：** 整改后 guide.md，引用格式与索引表逐字同格式，无裸编号、无章标题+裸节号，导航可达。

## 边界与限制

1. 仅处理用户指定路径的 Markdown / TXT 文件；不主动扫描目录、不处理非 Markdown / TXT 格式。
2. 不改写信息本质，仅调整结构、顺序与引用关系；不新增用户未提供的事实。
3. 写回前必须备份（生成 `.bak-[时间戳]`）；[dry_run] 为 true 时不写回原文件。
4. 含敏感隐私且用户未明确授权外传的内容，仅本地读写，不外发。
5. 不删除原文件，仅生成 `.bak` 与整改后内容。
6. 跨项目通用：不写死任何本机路径、具体仓库名、客户名；目标路径一律经 [file_path] 参数传入；示例中的 `D:/docs/...` 仅为演示占位，非真实路径。
7. 超大文档（超数千行）建议分批或先抽取索引区与标题树再审计，避免单次上下文溢出；但六连验证仍须覆盖全文。
