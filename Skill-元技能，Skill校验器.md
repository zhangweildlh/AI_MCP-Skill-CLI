---
name: skill-checker
description: 对 Skill 定义内容进行全量合规性校验，输出结构化校验报告与修正后的完整 Skill 定义。当用户要求检查/审核/审查/校验 SKill 时触发此技能。当用户说“检查下面这份 SKill 定义”或者“审查这份 SKill”时触发此技能。适用于 Skill 定义提交审阅、质量检查等场景。不适用于普通文本审阅、代码审查等场景。
license: Apache-2.0
metadata:
  version: "2.3"
  category: 质量校验
compatibility: 面向网页版 AI Agent（如 DeepSeek 网页版），并且兼容所有支持 Agent Skills 标准的 AI 代理客户端；通过对话窗口传入待校验内容
---

# Skill定义校验器

## 概述

本 Skill 用于对符合网页版 AI Agent（如 DeepSeek 网页版）或者 Agent Skills 标准的 Skill 定义内容进行全维度合规性校验，保障 Skill 格式规范、逻辑自洽，可跨客户端正常加载与执行。校验完成后，输出结构化校验报告与一份修正后的完整 Skill 定义。

> **符号约定**：正文中所有中文叙述统一使用中文全角标点符号（，。：；（）「」）；YAML 和代码块内容使用英文半角标点符号。

## 运行模式

本 Skill 通过对话窗口以交互方式运行。用户将待校验的 Skill 定义全文粘贴到对话中，本 Skill 即时返回校验结果。

## 输入参数

| 参数名 | 类型 | 必填 | 说明 | 默认值 | 合法范围 |
|--------|------|------|------|--------|----------|
| skill_content | 字符串 | 是 | 待校验的 Skill 定义全文（Markdown 格式，含 YAML frontmatter） | 无 | 最小长度 1，最大长度由 `max_content_length` 控制 |
| max_content_length | 整数 | 否 | 允许的最大输入字符数 | 15000 | 1000–50000 |
| strict_mode | 布尔值 | 否 | 是否启用严格模式（可选字段缺失也报告为问题） | false | `true` 或 `false` |

## 执行指令

你是专业的 Skill 定义校验器。用户通过对话窗口将待校验的 Skill 定义全文传入 `skill_content` 参数。你需要对该内容进行全维度合规性校验，并严格按下述 11 个维度逐项检查。

校验完成后，必须依次输出以下两部分内容：
1. **结构化校验报告**：包含基本信息、校验结果汇总、逐维度检查结果、问题汇总。
2. **完整修正后的 Skill 定义**：基于问题项修正后的完整 Skill 内容。若原始 Skill 完全合规，此部分仅输出「✅ 无需修改 Skill」。

### 校验维度

按以下维度顺序逐项检查。每个检查项均为可客观验证的是非问句，给出明确判定标准。

#### 维度 1：Frontmatter 结构完整性

检查 YAML frontmatter（`---` 包裹块）的字段完整性和格式正确性。

| 编号 | 检查项 | 判定标准 | 严重级别 |
|------|--------|---------|---------|
| 1.1 | 文件开头前 20 行以内是否存在以 `---` 包裹的 YAML 前置元数据？ | 前 20 行内存在 `---` 行，且文件中存在另一个 `---` 行（作为闭合），包裹的内容为 YAML 前置元数据 | ❌致命 |
| 1.2 | 是否包含必填字段 `name`？ | 在 frontmatter 中搜索 `name:` 键 | ❌致命 |
| 1.3 | 是否包含必填字段 `description`？ | 在 frontmatter 中搜索 `description:` 键 | ❌致命 |
| 1.4 | `name` 是否仅含小写字母 `a-z`、数字 `0-9` 和连字符 `-`？ | 正则检查：`^[a-z0-9-]+$` | ❌致命 |
| 1.5 | `name` 长度是否在 1–64 字符之间？ | 字符计数 | ❌致命 |
| 1.6 | `name` 是否不以连字符开头？ | 检查首字符是否不为 `-` | ❌致命 |
| 1.7 | `name` 是否不以连字符结尾？ | 检查尾字符是否不为 `-` | ❌致命 |
| 1.8 | `name` 是否不含连续连字符 `--`？ | 子串搜索 `--` | ❌致命 |
| 1.9 | `description` 长度是否在 1–1024 字符之间？ | 字符计数 | ❌致命 |
| 1.10 | `description` 是否为单行字符串（非 YAML 多行格式如 `|` 或 `>`）？ | 检查 `description:` 后是否紧接管道符或折叠符 | ⚠️严重 |
| 1.11 | 若存在 `compatibility` 字段，其长度是否在 1–500 字符之间？ | 字符计数 | ⚠️严重 |
| 1.12 | 若存在 `metadata` 字段，其值是否为键值对映射（缩进子字段）？ | 检查是否存在缩进的子键值对 | ⚠️严重 |
| 1.13 | 若存在 `allowed-tools` 字段，其值是否为非空字符串？ | 若 `allowed-tools` 字段不存在，此项标记为 ⊘ 不适用；若存在，检查字段值是否为空字符串 | ℹ️一般 |
| 1.14 | 若存在 `license` 字段，其值是否为非空字符串？ | 若 `license` 字段不存在，此项标记为 ⊘ 不适用；若存在，检查字段值是否为空字符串 | ℹ️一般 |
| 1.15 | 全文中以下成对符号是否均正确闭合：`[]`（方括号）、`<>`（尖括号）、`（）`（中文圆括号）、`「」`（直角引号）、`“”`（中文双引号）、`""`（英文双引号）、`()`（英文圆括号）、`{}`（花括号）？ | 每类符号分别计数，开闭数量不相等即为未闭合。本项仅检查符号闭合性，不检查占位符符号规范性（由 1.16 覆盖） | ⚠️严重 |
| 1.16 | 占位符是否使用了正确的符号（方括号 `[]` 或尖括号 `<>`）包裹？ | 见下文「符号使用检查细则」。本项仅检查占位符符号规范性，不检查符号闭合性（由 1.15 覆盖） | ⚠️严重 |

##### 符号使用检查细则

**检查范围：** frontmatter 中所有字符串值、正文中所有指示路径/占位符/参数名的位置。

**正确用法：**
- 需要标注「此处应替换为实际值」的占位符，必须使用方括号或尖括号包裹：`[占位符名称]` 或 `<占位符名称>`
- 例如：`[输出目录]`、`[项目名称]`、`[文件路径]`、`[用户输入]`或者`<输出目录>`、`<项目名称>`、`<文件路径>`、`<用户输入>`
- 普通字符串值不需要任何额外包裹符号

**错误用法（标记为不合规）：**
- 用反引号包裹占位符：`` `输出目录` ``（反引号用于代码/命令，不表示占位）
- 用双引号包裹占位符：`"输出目录"`（双引号是普通字符串，不表示占位）
- 用单引号包裹占位符：`'输出目录'`
- 用方头括号包裹占位符：`「输出目录」`
- 用书名号包裹占位符：`《输出目录》`
- 用花括号包裹占位符：`{输出目录}`（花括号不表示占位）
- 在应当使用占位符的位置直接写死具体路径或名称而没有做占位处理

**判定方法：** 逐行扫描正文，识别所有指示「此处应替换」或「用户需提供」的位置，检查是否使用了 `[xxx]` 或 `<xxx>` 格式作为占位符，而非其他符号。

---

#### 维度 2：Name 与目录对应关系

| 编号 | 检查项 | 判定标准 | 严重级别 |
|------|--------|---------|---------|
| 2.1 | `name` 值是否与父目录名称一致？ | 若 Skill 以目录形式提供，提取父目录名与 `name` 比对 | ❌致命 |

> 注：若 Skill 以单独 SKILL.md 文件形式提供（无父目录），此项标注为「⊘ 无法检查」。

---

#### 维度 3：渐进式加载适配

检查 Skill 是否支持「元数据 → 指令 → 资源」三层渐进式加载。

| 编号 | 检查项 | 判定标准 | 严重级别 |
|------|--------|---------|---------|
| 3.1 | `name` + `description` 的合计字符数是否 ≤600，可作为元数据层独立加载？ | 计算 name + description 的字符总数，≤600 为合规 | ℹ️一般 |
| 3.2 | SKILL.md 正文是否控制在 500 行以内？ | 统计 `---` 闭合后到文件末尾的行数 | ℹ️一般 |
| 3.3 | 详细参考内容是否已移至独立的 `references/` 目录下文件？ | 检查正文中是否有大量（超过 100 行连续）参考性内容，若有则建议拆分 | ℹ️一般 |
| 3.4 | 正文中引用外部文件时是否使用相对路径？ | 搜索正文中的文件引用，检查是否为相对路径（不以 `/` 或盘符开头） | ⚠️严重 |
| 3.5 | 文件引用链深度是否不超过 1 层？ | 检查引用的外部文件中是否又引用了其他文件（若无法检查，标注原因） | ℹ️一般 |

---

#### 维度 4：可选目录与文件结构

| 编号 | 检查项 | 判定标准 | 严重级别 |
|------|--------|---------|---------|
| 4.1 | 若存在 `scripts/` 目录，其中的脚本是否包含依赖说明或自包含的错误处理？ | 检查脚本文件开头是否有注释说明依赖，或脚本中包含 `try-except`（Python）/ `set -e`（Bash）等错误处理 | ℹ️一般 |
| 4.2 | 若存在 `references/` 目录，其中的文件是否聚焦单一主题？ | 检查文件篇幅和标题数量，建议每文件 ≤ 1 个 H1 标题 | ℹ️一般 |
| 4.3 | 若存在 `assets/` 目录，其中的文件是否为静态资源（模板、图片、数据文件等）？ | 检查文件类型是否属于静态资源范畴 | ℹ️一般 |

> 注：若 Skill 仅包含 SKILL.md 而无可选目录（scripts/、references/、assets/），维度 4 所有检查项标注为「⊘ 不适用」。

---

#### 维度 5：字段自洽与逻辑正确性

检查被校验 Skill 中是否存在同一概念或同一事物有多种不一致的表述，以及是否存在重复约定、语义重叠等问题。

| # | 检查项 | 判定标准 | 严重级别 |
|---|--------|---------|---------|
| 5.1 | `description` 描述的功能范围是否与正文指令逻辑一致？ | 提取 description 中声明的能力关键词，与正文实际功能逐项比对 | ⚠️严重 |
| 5.2 | 若存在 `allowed-tools`，声明的工具名称是否与正文指令中实际调用的工具完全匹配？ | 提取 allowed-tools 中的工具名列表，搜索正文中是否出现未声明的工具名 | ⚠️严重 |
| 5.3 | 是否存在自相矛盾的规则？ | 逐条对比所有规则语句：对同一条件/场景，是否存在两个规则给出了不同或冲突的处理方式 | ❌致命 |
| 5.4 | 正文指令的执行步骤是否清晰连贯、无逻辑跳跃？ | 检查步骤编号是否连续，前后步骤是否有因果/数据依赖关系 | ⚠️严重 |
| 5.5 | 正文中是否存在对同一概念、同一事物或同一事务使用了多种不同名称的情况（术语不一致）？ | 提取所有名词短语和术语，检查是否存在两个或多个不同的词指代同一事物。例如："最终文档"与"最终文稿"交替使用、"输入文件"与"源文件"混用。若发现此种情况，以表格形式输出「原文 → 不一致术语 → 统一建议」对照。本项仅检查术语/名称层面的不一致（一词多义或多词一义） | ⚠️严重 |
| 5.6 | 正文中是否存在重复约定、重复描述、语义重叠或描述不一致、语义歧义处？ | 全文逐段比对，检查是否存在两个或多个段落对同一规则、同一概念、同一事物进行重复描述但用词不一致；检查是否存在同一术语在不同位置有不同解释。若发现此类问题，以表格形式输出「原文 → 重复/歧义类型 → 修正方案」对照。本项仅检查属性/特征/约束等非名称性描述的不一致，术语/名称层面的不一致由 5.5 覆盖 | ⚠️严重 |
| 5.7 | 正文中是否存在两个不同检查项或规则职责范围重叠（包含语义重叠）？ | 提取所有规则/检查项的判定标准，比对是否存在两个判定标准覆盖了完全相同或高度重叠的判定对象。若发现，标注重叠项编号和重叠范围 | ⚠️严重 |
| 5.8 | 正文中的示例是否与正式规则逻辑一致、无冲突？ | 将示例中的输入代入正式规则，检查输出是否与示例输出一致。若不一致，以表格形式输出「原文 → 规则要求 → 修正方案」对照 | ⚠️严重 |
| 5.9 | 正文中的所有功能模块、规则、文案、示例自身或相互之间是否存在逻辑矛盾？ | 专指规则逻辑层面的矛盾：逐项比对规则间、规则与示例间是否一致。本项不重复检查术语/名称层面的不一致（由 5.5 覆盖） | ⚠️严重 |
| 5.10 | 各功能模块（含子模块）的排列与组织逻辑顺序是否合理、顺畅？是否存在可优化的空间？ | 检查正文的章节顺序和嵌套层级是否符合「从整体到局部」「从前提条件到执行步骤」「从正常流程到异常处理」的认知逻辑；检查模块之间是否存在跳转不连贯或逻辑断层。若发现问题，提出详细的修正方案 | ⚠️严重 |

---

#### 维度 6：参数体系规范性

| 编号 | 检查项 | 判定标准 | 严重级别 |
|------|--------|---------|---------|
| 6.1 | 正文中定义的每个参数是否都明确标注了参数名称？ | 检查参数表中每个参数是否有"参数名"列的值 | ⚠️严重 |
| 6.2 | 正文中定义的每个参数是否都明确标注了数据类型？ | 检查参数表中每个参数是否有"类型"列的值（如字符串、整数、布尔值等） | ⚠️严重 |
| 6.3 | 正文中定义的每个参数是否都明确标注了必填/选填属性？ | 检查参数表中每个参数是否有"必填"列的值 | ⚠️严重 |
| 6.4 | 正文中定义的每个参数是否都说明了功能？ | 检查参数表中每个参数是否有"说明"列的值 | ℹ️一般 |
| 6.5 | 正文中使用的所有可变变量是否均已在参数定义中声明（一）？ | 提取正文中所有占位符（`[xxx]` 或 `<xxx>` 格式），与参数表逐一比对 | ❌致命 |
| 6.6 | 正文中使用的所有可变变量是否均已在参数定义中声明(二)？ | 搜索正文中以 `[变量名]`、`<变量名>` 等形式出现的变量，逐一核对是否在参数表中定义 | ❌致命 |
| 6.7 | 参数定义中是否存在未被正文指令使用的冗余参数？ | 提取参数表中所有参数名，搜索正文是否出现该参数引用 | ℹ️一般 |

---

#### 维度 7：可执行性与无歧义

| 编号 | 检查项 | 判定标准 | 严重级别 |
|------|--------|---------|---------|
| 7.1 | 正文指令中是否不含模糊性词汇及循环自指表述？ | 搜索以下词汇及表述，出现即为不合规：「酌情处理」「合理设置」「适当调整」「必要时」「等相关」「等」「是否引发 AI 歧义」「是否直观」「是否合理」 | ⚠️严重 |
| 7.2 | 每个判定条件是否都给出了可客观验证的是非标准？ | 逐个检查指令中的条件判断语句，是否能用"是/否"或数值比较来判定 | ⚠️严重 |
| 7.3 | 是否覆盖了入参非法的异常处理规则？ | 搜索"异常""错误""失败""非法""为空"等关键词，检查覆盖度 | ❌致命 |
| 7.4 | 是否覆盖了结果为空或无法判定的处理规则？ | 搜索"结果为空""无结果""无法判定"等关键词 | ⚠️严重 |
| 7.5 | 是否覆盖了超出能力范围的处理规则？ | 搜索"不支持""无法处理""超出"等关键词 | ⚠️严重 |
| 7.6 | 输出格式是否明确定义，无歧义？ | 检查是否有明确的输出格式说明章节 | ⚠️严重 |

---

#### 维度 8：通用性与无硬编码

| 编号 | 检查项 | 判定标准 | 严重级别 |
|------|--------|---------|---------|
| 8.1 | 正文指令中是否不含仅在特定业务场景下有效的具体数值？ | 搜索正文中的数字，判断是否可配置（如字符限制应声明为参数而非固定值） | ℹ️一般 |
| 8.2 | 正文指令中是否不含特定项目名称、产品名称、人名、地名？ | 搜索是否出现特定品牌名、产品名、公司名、人名、地名等 | ⚠️严重 |
| 8.3 | 所有可变内容是否均通过参数传入，未直接写死在指令逻辑中？ | 检查指令逻辑中使用的值是否都来自参数或可由参数派生 | ⚠️严重 |
| 8.4 | 逻辑抽象是否不依赖特定业务场景，可直接复用于同类型的其他场景？ | 将 Skill 描述的场景替换为同类型的另一个场景，检查指令是否仍可执行 | ℹ️一般 |

---

#### 维度 9：示例有效性

| 编号 | 检查项 | 判定标准 | 严重级别 |
|------|--------|---------|---------|
| 9.1 | 是否包含至少 1 个典型场景示例？ | 搜索"示例"章节，计数 | ⚠️严重 |
| 9.2 | 是否包含至少 1 个边界/异常场景示例？ | 检查示例中是否包含异常输入或边界情况 | ⚠️严重 |
| 9.3 | 示例的输入是否与参数定义完全对应？ | 将示例输入与参数表逐字段比对。若不对应，以表格形式输出「示例输入字段 → 参数定义 → 修正方案」对照 | ⚠️严重 |
| 9.4 | 示例的输出是否与定义的输出规范一致？ | 将示例输出与输出格式说明比对。若不一致，以表格形式输出「示例输出 → 输出规范要求 → 修正方案」对照 | ⚠️严重 |
| 9.5 | 示例逻辑是否与正式规则无冲突？ | 将示例代入正式规则推导，检查是否一致 | ❌致命 |
| 9.6 | 示例是否为完整内容而非省略的摘要（不含 `...` 省略）？ | 搜索示例中是否出现 `...` 或「省略」等字样 | ℹ️一般 |

---

#### 维度 10：触发匹配合理性

| 编号 | 检查项 | 判定标准 | 严重级别 |
|------|--------|---------|---------|
| 10.1 | `name` 是否清晰体现 Skill 核心能力？ | 从 name 是否能直接推断 Skill 功能；name 是否与实际功能一致 | ⚠️严重 |
| 10.2 | `description` 是否同时描述了核心功能、业务关键词、触发条件、适用场景和不适用场景（五要素）？ | 检查 description 中是否包含上述五个方面的描述。若缺失任何一项，判定为不合规。特别地：<br>- 核心功能：是否说明 Skill 做什么<br>- 业务关键词：是否有与核心功能直接相关的专业术语<br>- 触发条件：是否有“当用户……时触发此技能”或“当用户输入包含……”等表述<br>- 适用场景：是否说明典型使用情境<br>- 不适用场景：是否包含“不适用于”等负向描述 | ❌致命 |
| 10.3 | `description` 是否包含至少一个明确的业务关键词（即与核心功能直接相关的专业术语）？ | 检查是否出现业务领域术语（如“文档撰写”“合规校验”“前端界面”等） | ⚠️严重 |
| 10.4 | `description` 是否明确说明了不适用场景（必须包含“不适用于”或“不用于”等负向描述）？ | 搜索“不适用”“不用于”等词汇，若未出现则不合规 | ⚠️严重 |
| 10.5 | 触发条件是否至少包含两种不同的表述形式（如“当用户……时”和“当用户输入包含……时”）？ | 检查触发条件的句式多样性，至少有两种明确的触发条件描述 | ℹ️一般 |
| 10.6 | 触发条件是否具体、无歧义，避免了“等场景”等模糊词汇？ | 搜索“等场景”“等需求”等模糊结尾，出现则不合规 | ⚠️严重 |
| 10.7 | `description` 是否以单行形式书写，没有使用 YAML 多行格式？ | 检查 `description:` 后是否紧接管道符 `\|` 或折叠符 `>`（此检查与 1.10 重复，但在此处复核） | ℹ️一般 |

---

#### 维度 11：模块组织与结构质量

检查被校验 Skill 的各功能模块排列、组织逻辑是否合理顺畅，是否存在可优化的空间。

| 编号 | 检查项 | 判定标准 | 严重级别 |
|------|--------|---------|---------|
| 11.1 | 正文各模块的排列顺序是否遵循合理的逻辑顺序？ | 检查章节排列是否符合「概述→运行模式→输入参数→执行指令→异常处理→输出要求→示例」或其他可论证的逻辑顺序（如按工作流先后排列）。若章节顺序导致读者需要频繁前后跳转才能理解，即为不合规 | ⚠️严重 |
| 11.2 | 模块间是否存在逻辑断裂或衔接不畅？ | 检查相邻模块之间是否有清晰的过渡关系；前一个模块的产出是否为后一个模块的输入或前置条件。若发现模块之间缺乏关联说明或跳跃过大，标注具体位置 | ℹ️一般 |
| 11.3 | 是否存在可合并、可拆分或可重排的模块优化空间？ | 检查是否存在以下情况：(a) 两个模块内容高度重叠（建议合并）；(b) 一个模块承载多个独立主题（建议拆分）；(c) 模块顺序导致读者需要前后跳转才能理解（建议重排）。若发现，给出优化建议 | ℹ️一般 |

---

### 异常处理规则

以下异常按触发阶段排列：

1. `skill_content` 为空或仅含空白字符 → 输出 `❌ 校验失败：输入内容为空，无法进行 Skill 定义校验。`
2. `skill_content` 无法解析出 YAML frontmatter（文件开头前 20 行以内不以 `---` 开头或无闭合 `---`）→ 标注为致命问题，终止校验，输出 `❌ 校验失败：输入内容不包含有效的 Skill 定义结构（缺少 YAML frontmatter）。`
3. `skill_content` 字符数超过 `max_content_length` → 输出 `⚠️ 警告：输入内容过长（当前 N 字符，上限 M 字符），可能影响校验完整性，建议分批校验。` 但继续执行校验。
4. 入参包含无法解析为文本的内容 → 标注为致命问题，终止校验，输出 `❌ 校验失败：输入内容无法解析为有效文本。`
5. 输出严格遵循标准 Markdown 结构化格式，标题层级清晰、表格规范。

---

### 输出要求与格式模板

校验完成后，按以下顺序输出两部分内容：

#### 第一部分：校验报告

按维度 1 到维度 11 的顺序依次输出校验结果。每个维度下分三部分：

1. **合规项**：列出该维度中检查结果为 `✅` 的条目
2. **问题项**：列出该维度中检查结果为 `❌` 或 `⚠️` 的条目，按严重级别降序排列
3. **修改建议**：针对每个问题项，给出具体的修改建议

#### 第二部分：修正后的完整 Skill 定义

根据第一部分中发现的所有问题，输出一份修正后的完整 Skill 定义。修正规则：

- 修复所有 ❌致命 和 ⚠️严重 问题
- ℹ️一般 问题在修正版中一并修复
- 修正后的 Skill 定义必须是可直接使用的完整内容（完整 YAML frontmatter + 完整 Markdown 正文）
- 使用代码块包裹修正后的 Skill 定义
- 若原始 Skill 完全合规，此部分仅输出「✅ 无需修改 Skill」

#### 输出格式模板

```markdown
# Skill 校验报告

## 基本信息
- Skill 名称：[name 值，若无法提取则标注"未知"]
- 校验时间：[当前时间]
- 校验模式：[strict_mode 值]
- 输入长度：[字符数]

## 校验结果汇总
- 总检查项数：N
- 通过（✅）：N
- 致命问题（❌）：N
- 严重问题（⚠️）：N
- 一般问题（ℹ️）：N
- 不适用（⊘）：N
- 最终判定：[✅ 校验通过 / ⚠️ 存在问题 / ❌ 校验失败]

## 逐维度检查结果

### 维度 1：Frontmatter 结构完整性

**合规项：**
- [检查项编号]：[描述]

**问题项：**
| 编号 | 严重级别 | 问题描述 |
|------|---------|----------|
| X.X | ❌/⚠️/ℹ️ | [具体问题] |

**修改建议：**
- [编号 X.X]：[具体建议]

...（维度 2 至维度 11 同理）

## 问题汇总

| 编号 | 严重级别 | 维度 | 问题描述 | 修改建议 |
|------|---------|------|---------|----------|
| X.X | ❌/⚠️/ℹ️ | [维度名] | [描述] | [建议] |

---

# 修正后的完整 Skill 定义

  以下是修正后的完整 Skill 定义：

  ```markdown
  [修正后的完整 Skill 定义内容]
  ```
```

---

### 校验结果汇总说明

| 统计项 | 说明 |
|--------|------|
| 最终判定 | 与异常处理中的终止消息一致：存在致命问题且无法继续时判定为「❌ 校验失败」；仅有严重/一般问题时判定为「⚠️ 存在问题」；无任何问题时判定为「✅ 校验通过」 |
| 严重级别 | 文字叙述中统一用「致命问题」；表格严重级别列统一用「❌致命」；汇总统计统一用「致命问题（❌）」。严重问题、一般问题同理 |
| 合规状态 | 汇总统计用「通过（✅）」；维度内小节标题用「合规项」；判定结果用「合规」/「不合规」 |
| 检查项编号 | 文字描述中用「检查项编号」；表格列标题统一用「编号」 |
| 不适用标记 | 统一用「⊘ 不适用」或「⊘ 无法检查」 |

完整校验示例见下文「示例」章节。

---

## 示例

### 示例 1：典型合规 Skill

**输入：**
```markdown
---
name: pdf-extract
description: 从 PDF 文件中提取文本和表格，用于文档处理场景。不适用于 PDF 创建和编辑。
license: MIT
---

# PDF 文本提取器

## 输入参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| file_path | 字符串 | 是 | PDF 文件的路径 |

## 执行指令

1. 读取 [file_path] 指定的 PDF 文件
2. 提取全部文本内容
3. 识别并提取表格数据
4. 以 Markdown 格式返回结果

## 异常处理

- [file_path] 不存在：输出错误信息「文件不存在：[file_path]」
- [file_path] 非 PDF 格式：输出格式错误提示「文件格式错误：仅支持 PDF 格式」
- 文件无法读取：输出权限错误提示「文件无法读取：请检查文件权限」
- 文件为加密 PDF：输出错误提示「不支持加密 PDF，请先解密后再试」
- 文件为纯图像 PDF（扫描件）：输出错误提示「不支持纯图像 PDF，请先执行 OCR 识别」
- PDF 文件无文本内容：输出提示信息「文件不包含可提取的文本内容」
```

**输出：**
```markdown
# Skill 校验报告

## 基本信息
- Skill 名称：pdf-extract
- 校验时间：2026-06-22T00:00:00
- 校验模式：非严格
- 输入长度：618

## 校验结果汇总
- 总检查项数：47
- 通过（✅）：35
- 致命问题（❌）：0
- 严重问题（⚠️）：0
- 一般问题（ℹ️）：4
- 不适用（⊘）：8
- 最终判定：✅ 校验通过

## 逐维度检查结果

### 维度 1：Frontmatter 结构完整性

**合规项：**
- 1.1：文件以 `---` 开头并存在闭合 `---`
- 1.2：包含必填字段 `name`
- 1.3：包含必填字段 `description`
- 1.4：`name` 仅含小写字母、数字和连字符
- 1.5：`name` 长度为 11，在 1–64 范围内
- 1.6：`name` 不以连字符开头
- 1.7：`name` 不以连字符结尾
- 1.8：`name` 不含连续连字符
- 1.9：`description` 长度在 1–1024 范围内
- 1.10：`description` 为单行字符串
- 1.14：`license` 值为非空字符串 "MIT"
- 1.15：符号闭合检查通过
- 1.16：占位符使用了正确的 `[]` 格式

**问题项：**
| 编号 | 严重级别 | 问题描述 |
|------|---------|----------|
| 1.13 | ⊘ | `allowed-tools` 未声明，不适用 |

**修改建议：**
无。

### 维度 2：Name 与目录对应关系

**合规项：**
- 2.1：⊘ 无法检查（Skill 以单独内容提交，无父目录信息）

### 维度 3：渐进式加载适配

**合规项：**
- 3.1：name + description 合计 52 字符，≤600 为合规
- 3.2：正文 18 行，在 500 行以内

**问题项：**
| 编号 | 严重级别 | 问题描述 |
|------|---------|----------|
| 3.3 | ℹ️一般 | 详细参考内容未移至 references/ 目录 |

**修改建议：**
- 3.3：当前正文较短，无需拆分。若后续增加大量参考性内容，建议移至 references/ 目录下独立文件。

...（维度 4–10 各有部分 ⊘ 项和 ℹ️ 项，此处省略以保持示例可读性。维度 3、8、10、11 各含 1 个 ℹ️ 项，维度 4 全部 ⊘）

### 维度 11：模块组织与结构质量

**合规项：**
- 11.1：模块排列顺序合理（概述→参数→指令→异常处理），符合逻辑顺序
- 11.2：模块间衔接顺畅，异常处理紧接执行指令

**问题项：**
| 编号 | 严重级别 | 问题描述 |
|------|---------|----------|
| 11.3 | ℹ️一般 | 缺少独立的「示例」章节，建议补充 |

**修改建议：**
- 11.3：在当前 Skill 定义末尾增加「示例」章节，至少包含 1 个典型场景示例。

## 问题汇总

| 编号 | 严重级别 | 维度 | 问题描述 | 修改建议 |
|------|---------|------|---------|----------|
| 3.3 | ℹ️一般 | 渐进式加载适配 | 详细参考内容未拆分 | 若有详细参考内容，建议移至 references/ |
| 11.3 | ℹ️一般 | 模块组织与结构质量 | 缺少独立的示例章节 | 增加示例章节 |

---

# 修正后的完整 Skill 定义

  以下是修正后的完整 Skill 定义：

  ```markdown
  ---
  name: pdf-extract
  description: 从 PDF 文件中提取文本和表格，用于文档处理场景。不适用于 PDF 创建和编辑。
  license: MIT
  ---

  # PDF 文本提取器

  ## 输入参数

  | 参数名 | 类型 | 必填 | 说明 |
  |--------|------|------|------|
  | file_path | 字符串 | 是 | PDF 文件的路径 |

  ## 执行指令

  1. 读取 [file_path] 指定的 PDF 文件
  2. 提取全部文本内容
  3. 识别并提取表格数据
  4. 以 Markdown 格式返回结果

  ## 异常处理

  - [file_path] 不存在：输出错误信息「文件不存在：[file_path]」
  - [file_path] 非 PDF 格式：输出格式错误提示「文件格式错误：仅支持 PDF 格式」
  - 文件无法读取：输出权限错误提示「文件无法读取：请检查文件权限」
  - 文件为加密 PDF：输出错误提示「不支持加密 PDF，请先解密后再试」
  - 文件为纯图像 PDF（扫描件）：输出错误提示「不支持纯图像 PDF，请先执行 OCR 识别」
  - PDF 文件无文本内容：输出提示信息「文件不包含可提取的文本内容」

  ## 示例

  ### 示例：提取 PDF 文本

  **输入：** file_path = "report.pdf"（包含文本和表格的正常 PDF 文件）

  **输出：**
    ```markdown
    # PDF 提取结果

    ## 文本内容
    [提取的全部文本内容...]

    ## 表格数据
    | 列1 | 列2 | 列3 |
    |-----|-----|-----|
    | ... | ... | ... |
    ```
  ```
```

### 示例 2：边界案例（缺少必填字段）

**输入：**
```markdown
---
name: my-skill
---

# 我的 Skill

这是一个缺少 description 字段的 Skill。
```

**输出：**
```markdown
# Skill 校验报告

## 基本信息
- Skill 名称：my-skill
- 校验时间：2026-06-22T00:00:00
- 校验模式：非严格
- 输入长度：66

## 校验结果汇总
- 总检查项数：47
- 通过（✅）：18
- 致命问题（❌）：1
- 严重问题（⚠️）：0
- 一般问题（ℹ️）：0
- 不适用（⊘）：28
- 最终判定：❌ 校验失败

## 逐维度检查结果

### 维度 1：Frontmatter 结构完整性

**合规项：**
- 1.1：文件以 `---` 开头并存在闭合 `---`
- 1.2：包含必填字段 `name`
- 1.4：`name` 仅含小写字母和连字符
- 1.5：`name` 长度 9，在 1–64 范围内
- 1.6：`name` 不以连字符开头
- 1.7：`name` 不以连字符结尾
- 1.8：`name` 不含连续连字符

**问题项：**
| 编号 | 严重级别 | 问题描述 |
|------|---------|----------|
| 1.3 | ❌致命 | 缺少必填字段 `description` |

**修改建议：**
- 1.3：添加 `description` 字段，长度 1–1024 字符，描述 Skill 的功能和使用场景

...（维度 2–11 中大量检查项因内容不存在而标记为 ⊘ 不适用，此处省略以保持示例可读性）

## 问题汇总

| 编号 | 严重级别 | 维度 | 问题描述 | 修改建议 |
|------|---------|------|---------|----------|
| 1.3 | ❌致命 | Frontmatter 结构完整性 | 缺少必填字段 `description` | 添加 description 字段，长度 1–1024 字符 |

---

# 修正后的完整 Skill 定义

  以下是修正后的完整 Skill 定义：

  ```markdown
  ---
  name: my-skill
  description: [请补充 Skill 的功能描述和使用场景，1–1024 字符]
  ---

  # 我的 Skill

  这是一个缺少 description 字段的 Skill。
  ```
```

---

以下是用户本次输入的 Skill 定义，请严格根据上述指令检查。