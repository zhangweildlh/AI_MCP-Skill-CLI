# =====================================================================
# AI_MCP-Skill-CLI 仓库级 .gitignore
# 用途：私有仓库，用于日常生产环境 + 云端备份。
# 规则依据：用户 2026-07-17 需求
#   - Memory-Data 目录内「非 .md 文件」不入库（.md 文件保留入库）
#   - .workbuddy 为本地项目配置，不入库（仅本地读取）
#   - Syncfolders 同步数据库、*.7z 压缩包不入库
#   - 用户明确允许：anysearch-skill/.env、web-search/.env、ref-material-writing/.env
#     入库（含真实 API Key 与隐私），故不忽略。ref-material-writing 自 2026-08-03
#     起内嵌自有 AnySearch CLI 副本（scripts/anysearch_cli.py + scripts/shared/），
#     其 .env 与 web-search 同策略显式允许。
#   - 注：仓库级忽略规则仅匹配 env/ 目录与 .venv/ 目录，不忽略顶层 .env 文件，
#     故上述 .env 本就会被跟踪；此处仅为策略显式声明，便于维护者知晓。
# =====================================================================

# ---------- 用户明确排除 ----------
.workbuddy/
Memory-Data/*
!Memory-Data/*.md
Syncfolders_Database_db
*.7z
*.zip

# ---------- 标准构建 / 运行时产物 ----------
__pycache__/
*.pyc
*.pyo
*.bak
.venv/
venv/
env/
node_modules/
*.log
.sisyphus/

# ---------- 编辑器 / 系统文件 ----------
.vscode/
.idea/
*.swp
.DS_Store

/MCP-MemPalace-memory-data/chroma.sqlite3
/MCP-MemPalace-memory-data/.blob_seq_ids_migrated
/MCP-MemPalace-memory-data/.collection_type_fixed
