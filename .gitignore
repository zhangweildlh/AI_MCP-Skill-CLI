# =====================================================================
# AI_MCP-Skill-CLI 仓库级 .gitignore
# 用途：私有仓库（转公开中），用于日常生产环境 + 云端备份。
# 规则依据：用户 2026-07-17 需求 + 2026-08-19 转公开决策
#   - Memory-Data 直接子级「非 .md 文件」不入库（直接子级 .md 保留入库）；
#     Memory-Data/task-methodology/（p1/p2 方法论文档）有意不入库
#     （2026-08-19 决策：仅本地知识沉淀，不随仓库分发）
#   - .workbuddy 为本地项目配置，不入库（仅本地读取）
#   - Syncfolders 同步数据库、*.7z 压缩包不入库
#   - 2026-08-19 转公开决策（D-2026-0819-02）：密钥/凭据文件一律不入库、不留历史，
#     作废 D-2026-0811-01「ref-material-writing/.env 授权入库」例外。
#     须忽略并已从历史清除：ref-material-writing/.env、web-search/.env、
#     code-review-combo/config/providers.json。
#   - 允许入库的示例/脚本（无真实密钥）：web-search/anysearch-skill/.env.example、
#     scripts/smoke/tier0_secrets.py（扫描脚本）。
#   - 注：仓库级忽略规则仅匹配 env/ 与 .venv/ 目录，不忽略顶层 .env 文件，
#     故需忽略的顶层 .env 在下方「敏感凭据」段单独显式声明。
# =====================================================================

# ---------- 用户明确排除 ----------
# _gsdata_/ 为 GoodSync（U 盘同步）状态目录，仅本地工具使用，不入库
_gsdata_/
.workbuddy/
Memory-Data/*
!Memory-Data/*.md
Syncfolders_Database_db
*.7z
*.zip

# 方案 Y：worktree 统一挂载目录（仓库内一级子目录，不入库）
worktrees/

# ---------- 标准构建 / 运行时产物 ----------
__pycache__/
*.pyc
*.pyo
*.bak
.venv/
venv/
env/
# ---------- 敏感凭据（绝不入库，与 tier0_secrets.py MUST_IGNORE 一致）----------
ref-material-writing/.env
web-search/.env
code-review-combo/config/providers.json
node_modules/
*.log
.sisyphus/

# ---------- 编辑器 / 系统文件 ----------
.vscode/
.idea/
*.swp
.DS_Store

/MCP-MemPalace-memory-data/chroma.sqlite3
/MCP-MemPalace-memory-data/.blob_seq_ids_migrated
/MCP-MemPalace-memory-data/.collection_type_fixed

# ---------- 运行时二进制 / 含密钥配置（绝不入库）----------
# codebase-memory-mcp 运行时 exe 约 282MB，超 GitHub 单文件 100MB 硬上限，不入库（以占位符文件名提示下载）
codebase-memory/codebase-memory-mcp/codebase-memory-mcp.exe
# code-review-combo 本地 provider 配置含真实 API key（nvidia/sensenova），仅本地保留、绝不入库
code-review-combo/config/providers.json
