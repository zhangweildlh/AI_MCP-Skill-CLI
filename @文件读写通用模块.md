# 文件读写规范

**当需要将内容持久化、将内容输出到本地目录、将内容保存为本地文件、向已有文件追加内容，或者要求加载、调用其他 Skill，或者要求读取、加载、查看文件内容时，必须遵循本规范。**

**优先遵循本规范进行文件读写。仅仅只有当本规范方法连续读写同一个文件失败 2 次后，才能降级调用 `local_file_read` 或 `local_file_write` 工具读写文件。**

读写文件方法/工具优先级：本规范方法 > `local_file_read` 和 `local_file_write`

**执行 Python 程序时须严格遵循本规范「shell_exec 命令构造示例」的要求和「Python 程序执行规范」的要求**。

允许越过本规范，直接调用 `local_file_write` 工具将本规范中的 Python 代码写入本地[临时目录] `.py` 文件。

## 全局约束(强制执行)

**如果用户或 Skill 指定了"环境约束"规则（比如 输出目录或输出文件的文件名生成规则、保存路径或文件格式 ），则优先遵循该用户或 Skill 的指定。**

1. **[占位符]**：使用 `[]` 和 `<>` 作为占位符标识，比如 `[输出目录]`、`[文件名.docx]` 、`[需要输入的内容]`；在构造实际命令时替换为"实际输出路径"、"实际文件名"和"实际待写入的内容"。
2. **[当前工作目录]**：存放**待处理的输入文件**（如用户提供的 参考资料、文件等）的父目录。默认路径为： `D:\Documents\Downloads` 。
   - 如果用户明确指定了绝对路径的 "当前工作目录"，则**直接使用**该路径；如果用户明确指定了相对路径的 "当前工作目录"，则将其解析为：[当前工作目录]/[相对路径]；否则使用**默认路径**。
   - 这是本 Skill 执行任务时，读取用户提供的**输入文件**（如 参考资料、文件等）的根目录。此目录仅用于定位数据源和文件源，不用于查找 Skill 定义或可执行代码。
   - Shell 与 Powershell 工作目录**必须**切换到此路径。
3. **[输出目录]**：所有输出文件和最终交付成果的根目录；默认路径为： `D:\Documents\Downloads` 。
   - 如果用户明确指定了绝对路径的 "输出目录"，则**直接使用**该路径；如果用户明确指定了相对路径的 "输出目录" ，则将其解析为：[输出目录]/[相对路径]；否则使用**默认路径**。
4. **[Skill技能根目录]**： Skill 相关资产存放的根目录（SKILL.md 定义文件、子 Skill 定义文件、脚本、资源、模板等）；默认路径为： `D:\Documents\AI_MCP-Skill-CLI` 。
   - 如果用户明确指定了绝对路径的 "Skill技能目录" ，则**直接使用**该路径；如果用户明确指定了相对路径的 "Skill技能目录" ，则将其解析为：[Skill技能根目录]/[相对路径]；否则按下面第 5 条执行。
5. **[name]目录**：本 Skill 所有资产存放目录，即 YAML frontmatter 中的 `name` 。该目录位于[Skill技能根目录]中：[Skill技能根目录]/[name]。
   - 本 Skill 定义文件存放于此[name]目录中。
   - 本 Skill 使用**相对路径**（如 `./references/01-writing-standards.md` 或 `references/01-writing-standards.md`）加载、调用子 Skill或加载、阅读资源文件或者使用模板文件时，**必须**以此目录作为相对路径的解析根目录（如 `D:\Documents\AI_MCP-Skill-CLI\ref-material-writing\references\01-writing-standards.md` ），而不依赖于当前工作目录（CWD）。
6. **[临时目录]**：所有下载缓存、数据缓存、程序缓存（如本 Skill 构造的 .py 程序）等临时文件存放的父目录；默认路径为：当前系统环境变量 `TMP` (在本地 Powershell 中使用命令 `$env:TMP` 查询；在本地 CMD 中使用命令 `echo %TMP%` 查询)。
7. **所有 `shell_exec` 执行命令的 `cwd` 始终为 `[当前工作目录]`。但若命令中使用了相对路径，则一律先拼接为绝对路径后传入命令，不依赖 `cd` 操作。**
   - 若相对路径指向 Skill 内部资产（如 `references/`、`assets/` 等），则拼接根为 [Skill技能根目录]/[name]。
   - 若指向用户输入数据或输出文件，则拼接根为 [当前工作目录] 或 [输出目录]（视具体命令语义而定）。
8. **连接符限制（必须执行）**：本地 Powershell 环境限制，多命令组合运算时**仅允许**使用以下两个连接符：
   - **顺序执行**：使用英文分号 `;`（用于连接无数据传递的多个命令）。
   - **管道传递**：使用英文竖线 `|`（用于将前一个命令的 `Stdout` 传递为后一个命令的 `Stdin`）。
   - **严格禁止**使用的符号：`&&`、`||`、`&`。
9. **强制转换规则（必须执行）**：当用户输入或运行脚本中检测到 `&&`、`||`、`&` 符号时，必须依据命令意图：若原意是顺序执行多个无依赖的命令，替换为英文分号 `;`；若原意是将前一个命令的输出传递给后一个命令作为输入，替换为英文竖线 `|`。
   - **注意**：`&&` 和 `||` 原本具有条件逻辑，替换为 `;` 后将变为无条件顺序执行，请确保此逻辑变更符合用户意图，若冲突需提前告知用户。
10. **单次 shell_exec 命令字符串总长度不得超过 7000 字符（含空格和标点符号）。** 超过则必须拆分为多个自包含的 shell_exec 调用。
11. **`shell_exec` 每次调用启动独立的 PowerShell 会话**，因此无法跨 `shell_exec` 传递变量、数据。
12. **本 Skill 中所有 Python 程序执行**（包括但不限于：MD 文件写入、数据处理、摘要持久化、正文_过程稿持久化、.py 程序）均必须严格遵循「Python 程序执行规范」。
13. **[当前时间戳]**：格式："yyyy-MM-dd-HH-mm-ss"，取系统当前日期，**通过 PowerShell 命令 `Get-Date -Format 'yyyy-MM-dd-HH-mm-ss'` 获取**。
14. **文件内容读取**："阅读...文件" ≡ "读取...文件" ≡ "读取...文件的内容" ≡ "查看...文件的内容" — same content.
15. **文件扩展名补齐规则**：如果用户输入了完整的文件名（包含扩展名），则**直接使用**。否则，「模式一：覆盖写入」和「模式二：追加写入」自动添加扩展名 `.md`；「模式三：读取内容」**优先尝试**添加扩展名 `.md`，若文件不存在则将 `.md` 改为 `.txt` 再次读取；若 `.txt` 也不存在，则返回「❌ 错误：待读取文件不存在（含.md/.txt两个变体）」并告知用户具体路径。
16. **分片操作（强制前置步骤）**：所有写入操作执行前，你必须按照本规则的「分片策略」完成内容分片计算与切分；**禁止跳过本步骤直接整段写入**，无论内容长短均需先完成字符数校验与分片判定。

## Python 程序执行规范

1. 遵循「使用外部命令、工具 Tool 或者 CLI 前置工作流程」，调用 `shell_exec` 工具，查询 `uv` 命令和 `python` 命令是否可用。查询结果可在当前对话会话中复用（通过上下文记忆，无需额外存储），后续使用 `uv` 和 `python` 命令不必再次查询。
2. 若 `uv` 命令和 `python` 命令全部可用，则构造实际的 `uv run --project D:\Tools\Assembly\python\myenv python [路径 + *.py]` 命令，调用 `shell_exec` 工具执行。
3. 若第2步命令执行失败，则尝试构造实际的 `uv run --directory D:\Tools\Assembly\python\myenv python [路径 + *.py]` 命令，调用 `shell_exec` 工具执行。
4. 若 `uv` 命令不可用，但 `python` 命令可用，或者第2步和第3步的 `uv run` 命令均失败，则构造实际的 `python [路径 + *.py]` 命令，调用 `shell_exec` 工具执行。
5. **若 `uv` 命令和 `python` 命令全部不可用，则终止流程并告知用户。**

**选择优先级：`uv run --project` > `uv run --directory` > `python`**

**注意**：由于调用 `shell_exec` 工具每次都是启动独立的 PowerShell 会话，Python 程序文件需要先写入本地[临时目录] `.py` 文件，再调用 `shell_exec` 工具执行。

---

## 工作流程

### 1. 写入内容传递方式（强制执行）

- **所有动态内容和数据必须通过以下方式传递给 Python 程序。**
- **[输出目录]** 和 **[文件名]** 和 **[写入模式]**：通过 `shell_exec` 工具的 `command` 参数传递给 Python 程序。
- **[每次写入的内容]**（包括分片后的每一片内容）：通过 `shell_exec` 工具的 `env` 参数以环境变量 `WRITE_CONTENT` 传递给 Python 程序。
- **分片写入规则**：分片后的每一段内容，必须逐片独立调用 `shell_exec` 工具执行 Python 程序进行写入，禁止将多个分片合并为一次调用。

设计目的：
1. 同一个 Python 程序文件可以被多次复用（覆盖写入、追加写入、不同文件均可使用同一份脚本）。
2. 彻底避免内容中的特殊字符（引号、换行、`$`、`\` 等）与 PowerShell 命令解析或 JSON 解析发生冲突。

---

### 2. 通用写入 Python 程序代码

**本 Python 程序代码只需在首次使用时写入一次 `[临时目录]\file_writer_[当前时间戳].py`，后续所有写入操作均复用该程序代码，无需重复生成。**
**禁止使用 `python -c` 内联执行本「通用写入 Python 程序代码」。禁止使用 Here-String 方式构造字符串方式通过 Out-File、Set-Content 等命令将本「通用写入 Python 程序代码」一次性写入 py 文件，以及禁止使用 Base64 编码解码方式将本「通用写入 Python 程序代码」写入 py 文件。**

若输出目录或者文件不存在，Python 程序会自动创建。

```python
import os
import sys

def main():
    if len(sys.argv) < 4:
        print("用法: python file_writer.py <输出目录> <文件名> <写入模式: w|a>")
        sys.exit(1)

    output_dir = sys.argv[1]
    file_name = sys.argv[2]
    write_mode = sys.argv[3]          # "w" 或 "a"

    # 严格检查环境变量内容，防止空内容覆盖文件
    content = os.environ.get("WRITE_CONTENT")
    if content is None or content == "":
        print("错误：环境变量 WRITE_CONTENT 为空或未设置，无法执行写入操作。请检查调用参数。")
        sys.exit(1)

    file_path = os.path.join(output_dir, file_name)

    try:
        # 确保输出目录存在
        os.makedirs(output_dir, exist_ok=True)

        # 内容预处理：统一换行
        processed_content = content.replace("\r\n", "\n")

        # 写入文件
        with open(file_path, write_mode, encoding="utf-8") as f:
            f.write(processed_content)

        action = "覆盖写入" if write_mode == "w" else "追加写入"
        print(f"内容已{action}: {file_path}")
        print(f"本次写入内容字符数：{len(content)}")

    except PermissionError:
        print(f"错误：没有该目录 {file_path} 的文件写入权限！")
        sys.exit(2)
    except OSError as e:
        print(f"路径异常错误：{e}")
        sys.exit(3)
    except Exception as e:
        print(f"未知异常：{e}")
        sys.exit(1)

if __name__ == "__main__":
    main()
```

---

### 3. 写入内容字符数限制与分片策略（强制执行）

⚠️ **强制要求**：模式一（覆盖写入）与模式二（追加写入）在调用任何写入脚本前，必须先完成全量内容的字符数统计与分片切分。分片是写入操作的必经前置流程，不存在“可省略”的例外。

- **单段字符上限**：5500 字符
- **总分片数** = Ceiling（总字符数 / 5500）
- **分片策略**：你将本次需要写入的内容按「单段字符上限」依次序贯切分为 `[分片1]`、`[分片2]`、……、`[分片N]`（N = `[总分片数]`），每片字符数不超过 “单段字符上限”。切分时应尽量保持语义完整（如优先在换行符处切分），但若无法找到合适的自然切分点，则可硬切分。

#### 3.1 策略 A：总字符数 ≤ 5500

无需分片，按单分片（分片数=1）执行写入。

#### 3.2 策略 B：总字符数 > 5500

> **强制执行：必须完成分片后才能开始写入循环，严禁跳过。**
1. 按字符数依次序贯切分
2. **依次序贯**处理每一个分片
3. 每片写入成功后，再处理下一片

---

### 4. 三种模式

| 模式 | 触发场景 | 写入策略 |
|------|----------|----------|
| **模式一：覆盖写入** | "保存""创建""新建""覆盖写入"或表述模糊 | **必须先完成分片**；分片1的“写入模式”用 `w`，分片2-N的“写入模式”用 `a` |
| **模式二：追加写入** | "追加""补充""追加写入" | **必须先完成分片**；所有分片的“写入模式”均用 `a` |
| **模式三：读取内容** | "读取""提取""阅读""查看" | 小文件一次性读取；超长文件分片读取 |

#### 4.1 模式一：覆盖写入

1. 检查目标文件是否已存在：`Test-Path -LiteralPath "[输出目录]\[文件名]"`
2. 若文件存在，向用户发出确认提示
3. 若用户确认或60秒无回应，继续操作
4. **⚠️ 强制执行分片操作（不可跳过）：在执行任何写入命令之前，你必须先完成内容分片。**
   1. 统计待覆盖写入内容的总字符数，计算总分片数；
   2. 严格按照「分片策略」完成全部内容的切分，生成有序分片列表；
   3. 禁止未做分片计算、直接将完整内容传入 `WRITE_CONTENT` 执行覆盖写入；
   4. 仅当总字符数 ≤ 5500（即总分片数=1）时，可按单分片执行写入。
5. 按写入策略序贯执行每一分片的写入操作：分片1使用 `w` 模式，分片2至分片N使用 `a` 模式；每一片写入成功后，再处理下一片。

#### 4.2 模式二：追加写入

**⚠️ 强制执行分片操作（不可跳过））：在执行任何写入命令之前，你必须先完成内容分片。**
1. 统计待追加内容的总字符数，计算总分片数；
2. 严格按照「分片策略」完成全部内容的切分，生成有序分片列表；
3. 禁止未做分片计算、直接将完整内容传入 `WRITE_CONTENT` 执行追加写入；
4. 仅当总字符数 ≤ 5500（即总分片数=1）时，可按单分片执行写入。
5. 所有分片统一使用 `a` 模式，按顺序依次写入；每一片写入成功后，再处理下一片

#### 4.3 模式三：读取内容

**超长文件判定规则**：文件大小 ≥ 40KB 视为超长文件。

##### 小文件读取工作流程（文件大小 < 40KB）
```powershell
Get-Content -LiteralPath "[当前工作目录]/[文件名]" -Raw -Encoding UTF8
```

**结果提取强制规则**：必须从返回值的 `structuredContent.data.stdout` 字段提取完整文件内容；**禁止使用 `content[0].text` 字段**。

##### 超长文件读取工作流程（文件大小 ≥ 40KB）
- 单分片行数上限：500 行
- 单分片原始字节上限：40 KB
- **分片校验强制规则**：每读取完一片，必须检查 `structuredContent.data.truncated` 字段；若值为 `true` 说明已触发 Node 层字节限流，必须缩小分片行数后重试该分片。

**步骤1**：获取文件总行数
```powershell
(Get-Content -LiteralPath "[当前工作目录]/[文件名]" -Encoding UTF8 | Measure-Object -Line).Lines
```

**步骤2**：依次执行分片读取
```powershell
Get-Content -LiteralPath "[当前工作目录]/[文件名]" -Encoding UTF8 | Select-Object -Skip [跳过行数] -First [每片行数] | Out-String
```

**步骤3**：收集并拼接完整内容

1. 按分片顺序，从每一次 `shell_exec` 返回的 `structuredContent.data.stdout` 中提取文本片段；
2. 确认所有分片的 `truncated` 字段均为 `false`，若某分片触发截断，立即缩减行数重试该分片；
3. 将所有片段**直接拼接**（片段之间无需添加额外换行，`Out-String` 已为每片末尾保留换行符），组合成完整文件内容。
4. **成功时**：最终将拼接后的完整内容以单一代码块格式原样输出，并注明文件路径。**不允许**分段展示或遗漏任何片段。
5. **中途错误**：若某一片段读取失败或触发截断，停止后续分片调用，以自然语言告知用户具体失败原因（参考异常处理规则），并说明已完成的分片数。

**步骤4**：以单一 Markdown 代码块形式输出

---

## shell_exec 命令构造示例

> 注意：以下示例仅展示单分片（字符数 ≤ 5500）的调用格式。当内容超过5500字符时，必须按分片数量执行多次 `shell_exec` 调用，每次仅传递当前分片的内容，**严禁将全量内容一次性传入 `WRITE_CONTENT`**。

### 覆盖写入（单段）
```json
{
  "command": "uv run --project D:\\Tools\\Assembly\\python\\myenv python 'D:\\Documents\\AI_MCP-Skill-CLI\\ref-material-writing\\scripts\\file_writer.py' 'D:\\Documents\\Downloads' '示例文件.md' 'w'",
  "env": {
    "WRITE_CONTENT": "这是待写入的完整内容..."
  }
}
```

### 追加写入（单段）
```json
{
  "command": "uv run --project D:\\Tools\\Assembly\\python\\myenv python 'D:\\Documents\\AI_MCP-Skill-CLI\\ref-material-writing\\scripts\\file_writer.py' 'D:\\Documents\\Downloads' '示例文件.md' 'a'",
  "env": {
    "WRITE_CONTENT": "这是要追加的内容..."
  }
}
```

**说明**：
- `command` 中的 Python 解释器应根据「Python 程序执行规范」选择，如 `uv run --project`、`uv run --directory` 等），上述示例以 `uv run --project` 为例。
- `env` 中的 `WRITE_CONTENT` 值为当前分片的完整内容，**必须为字符串类型**。

---

## 异常处理规则

1. 权限不足 → 告知用户具体文件路径和所需权限
2. 磁盘空间不足 → 告知用户磁盘空间不足
3. 路径非法 → 告知用户路径包含非法字符
4. 文件不存在 → 告知用户待读取文件不存在
5. Python 环境不可用 → 告知用户不具备 Python 环境
6. 分片读取3次均失败 → 提示用户检查文件完整性
7. `WRITE_CONTENT` 为空 → 返回非零退出码
8. 未执行分片导致内容截断/写入不全 → 立即终止操作，重新执行完整分片流程后再写入

---

## 输出格式约束

1. 写入成功：以「✅ 成功」开头
2. 读取成功：以代码块格式原样输出
3. 操作失败：以「❌ 错误」开头
4. 所有输出使用中文